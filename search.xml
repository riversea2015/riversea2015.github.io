<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ SDImageCache]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-SDImageCache%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 5 篇，也是最后一篇，主要讨论处理缓存的类 SDImageCache 及相关类 SDMemoryCache 、SDImageCacheConfig 等。 正文先介绍 SDImageCache.h 中定义的 2 个枚举：SDImageCacheType 和 SDImageCacheOptions。 1234567891011121314151617typedef NS_ENUM(NSInteger, SDImageCacheType) &#123; // 不缓存，从网络下载数据 SDImageCacheTypeNone, // 磁盘缓存 SDImageCacheTypeDisk, // 内存缓存 SDImageCacheTypeMemory&#125;;typedef NS_OPTIONS(NSUInteger, SDImageCacheOptions) &#123; // 即使内存中有缓存，也要强制查询磁盘缓存 SDImageCacheQueryDataWhenInMemory = 1 &lt;&lt; 0, // 强制同步查询磁盘缓存 SDImageCacheQueryDiskSync = 1 &lt;&lt; 1, // 压缩大图 SDImageCacheScaleDownLargeImages = 1 &lt;&lt; 2&#125;; 包含几个重要属性： 1234567891011121314151617/// *** 缓存配置信息 ***@property (nonatomic, nonnull, readonly) SDImageCacheConfig *config;/// 内存缓存的最大消耗@property (assign, nonatomic) NSUInteger maxMemoryCost;/// 内存缓存的最大缓存数量@property (assign, nonatomic) NSUInteger maxMemoryCountLimit;/// *** 内存缓存 ***@property (strong, nonatomic, nonnull) SDMemoryCache *memCache;/// 磁盘缓存路径@property (strong, nonatomic, nonnull) NSString *diskCachePath;///@property (strong, nonatomic, nullable) NSMutableArray&lt;NSString *&gt; *customPaths;/// 读写操作的串行队列@property (strong, nonatomic, nullable) dispatch_queue_t ioQueue;/// 用于操作文件的 fileManager@property (strong, nonatomic, nonnull) NSFileManager *fileManager; 其中 2 个属性需要重点关注一下： ① config 所属类 SDImageCacheConfig 定义了很短属性，只提供了一个init方法，在里边给所有属性付了初值，详见下方法代码： 1234567891011121314151617181920static const NSInteger kDefaultCacheMaxCacheAge = 60 * 60 * 24 * 7; // 1 week@implementation SDImageCacheConfig- (instancetype)init &#123; if (self = [super init]) &#123; _shouldDecompressImages = YES; _shouldDisableiCloud = YES; _shouldCacheImagesInMemory = YES; _shouldUseWeakMemoryCache = YES; _diskCacheReadingOptions = 0; _diskCacheWritingOptions = NSDataWritingAtomic; _maxCacheAge = kDefaultCacheMaxCacheAge; _maxCacheSize = 0; _diskCacheExpireType = SDImageCacheConfigExpireTypeModificationDate; &#125; return self;&#125;@end ② memCache 它属于一个继承自 NSCache 的缓存类 SDMemoryCache， 他有一个关键属性： 12// weakCache 是 NSMapTable 类型@property (nonatomic, strong, nonnull) NSMapTable&lt;KeyType, ObjectType&gt; *weakCache; 下面观察一下 SDMemoryCache 的初始化及相关代码： 1234567891011121314151617181920212223242526- (instancetype)initWithConfig:(SDImageCacheConfig *)config &#123; self = [super init]; if (self) &#123; self.weakCache = [[NSMapTable alloc] initWithKeyOptions:NSPointerFunctionsStrongMemory valueOptions:NSPointerFunctionsWeakMemory capacity:0]; // 其他省略 ... [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(didReceiveMemoryWarning:) name:UIApplicationDidReceiveMemoryWarningNotification object:nil]; &#125; return self;&#125;- (void)didReceiveMemoryWarning:(NSNotification *)notification &#123; // 注意：此处是调用的 suoper 方法，所以并没有移除 weak cache，如果是调用 self 重写的 removeAllObjects 方法，就会移除 weak cache。 [super removeAllObjects];&#125;- (void)dealloc &#123; [[NSNotificationCenter defaultCenter] removeObserver:self name:UIApplicationDidReceiveMemoryWarningNotification object:nil];&#125; 当遇到内存警告的时候，缓存会被清除，但 weak cache 并不会被移除，如果手动清除的话，weak cache 当然会被移除。 这里 value 设置成 weak 可以避免可能的循环引用，虽然是 weak，不过，image 实例可以被其他对象持有，像 imageView，这种情况下，value 就不是 nil。 似乎扯远了O(∩_∩)O哈哈~，好了，我们还是切回来继续讨论 SDImageCache 提供的方法吧！首先，是 SDImageCache 的创建方法，它给我们提供了一个单例方法 + (nonnull instancetype)sharedImageCache，下边是他的方法实现： 12345678+ (nonnull instancetype)sharedImageCache &#123; static dispatch_once_t once; static id instance; dispatch_once(&amp;once, ^&#123; instance = [self new]; &#125;); return instance;&#125; 然后，看看初始化方法，我们发现所有初始化方法最后均调用了同一个核心方法 - (nonnull instancetype)initWithNamespace: diskCacheDirectory:，具体作用见下方代码注释。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950- (instancetype)init &#123; return [self initWithNamespace:@"default"];&#125;- (nonnull instancetype)initWithNamespace:(nonnull NSString *)ns &#123; NSString *path = [self makeDiskCachePath:ns]; return [self initWithNamespace:ns diskCacheDirectory:path];&#125;- (nonnull instancetype)initWithNamespace:(nonnull NSString *)ns diskCacheDirectory:(nonnull NSString *)directory &#123; if ((self = [super init])) &#123; NSString *fullNamespace = [@"com.hackemist.SDWebImageCache." stringByAppendingString:ns]; // 创建一个 IO 串行队列 (依次执行操作) _ioQueue = dispatch_queue_create("com.hackemist.SDWebImageCache", DISPATCH_QUEUE_SERIAL); // 初始化内存缓存 _config = [[SDImageCacheConfig alloc] init]; _memCache = [[SDMemoryCache alloc] initWithConfig:_config]; _memCache.name = fullNamespace; // 初始化磁盘缓存路径 if (directory != nil) &#123; _diskCachePath = [directory stringByAppendingPathComponent:fullNamespace]; &#125; else &#123; NSString *path = [self makeDiskCachePath:ns]; _diskCachePath = path; &#125; dispatch_sync(_ioQueue, ^&#123; self.fileManager = [NSFileManager new]; &#125;);#if SD_UIKIT // App 即将关闭的时候，清除过期缓存 [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(deleteOldFiles) name:UIApplicationWillTerminateNotification object:nil]; // App 即将进入后台的时候，清除过期缓存 [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(backgroundDeleteOldFiles) name:UIApplicationDidEnterBackgroundNotification object:nil];#endif &#125; return self;&#125; 记得 上一篇 介绍 SDWebImageManager 的时候，是这样使用 imageCache 的： 123456operation.cacheOperation = [self.imageCache queryCacheOperationForKey:key options:cacheOptions done:^(UIImage *cachedImage, NSData *cachedData, SDImageCacheType cacheType)&#123; // 查询完成后的操作在这里，可能查到了，也可能没查到 ...&#125; 现在，我们就来揭开这个方法的什么面纱。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778- (nullable NSOperation *)queryCacheOperationForKey:(nullable NSString *)key options:(SDImageCacheOptions)options done:(nullable SDCacheQueryCompletedBlock)doneBlock&#123; // 1.校验参数 if (!key) &#123; if (doneBlock) &#123; doneBlock(nil, nil, SDImageCacheTypeNone); &#125; return nil; &#125; // 2.查询内存缓存 (NSCache) UIImage *image = [self imageFromMemoryCacheForKey:key]; BOOL shouldQueryMemoryOnly = (image &amp;&amp; !(options &amp; SDImageCacheQueryDataWhenInMemory)); if (shouldQueryMemoryOnly) &#123; if (doneBlock) &#123; doneBlock(image, nil, SDImageCacheTypeMemory); &#125; return nil; &#125; NSOperation *operation = [NSOperation new]; // 3.将获取缓存及解压的 ‘耗时’ 操作封装成一个 block void(^queryDiskBlock)(void) = ^&#123; // 如果已经取消，不作任何处理，直接返回。 if (operation.isCancelled) &#123; return; &#125; @autoreleasepool &#123; NSData *diskData = [self diskImageDataBySearchingAllPathsForKey:key]; UIImage *diskImage; SDImageCacheType cacheType = SDImageCacheTypeDisk; if (image) &#123; // A &gt; 从 memery 取的 diskImage = image; cacheType = SDImageCacheTypeMemory; &#125; else if (diskData) &#123; // B &gt; 如果内存没有，但是从 disc 取到了，需要解压 diskImage = [self diskImageForKey:key data:diskData options:options]; if (diskImage &amp;&amp; self.config.shouldCacheImagesInMemory) &#123; // 缓存到内存 NSUInteger cost = SDCacheCostForImage(diskImage); // 计算大小 [self.memCache setObject:diskImage forKey:key cost:cost]; &#125; &#125; if (doneBlock) &#123; if (options &amp; SDImageCacheQueryDiskSync) &#123; // 同步执行完成回调 doneBlock(diskImage, diskData, cacheType); &#125; else &#123; // 异步执行完成回调 dispatch_async(dispatch_get_main_queue(), ^&#123; doneBlock(diskImage, diskData, cacheType); &#125;); &#125; &#125; &#125; &#125;; // 4.执行查询磁盘缓存的 block if (options &amp; SDImageCacheQueryDiskSync) &#123; queryDiskBlock(); &#125; else &#123; dispatch_async(self.ioQueue, queryDiskBlock); &#125; return operation;&#125; 如上边的代码所示，主要分了这么 4 步： ① 校验参数 — 如果 key 不存在，直接 doneBlock，返回 nil。 ② 查询内存缓存 NSCache — 如果内存中有，并且没有强制要求必须查询磁盘，则 执行 doneBlock，将 image 返回。 ③ 将获取缓存及解压的 ‘耗时’ 操作封装成一个 block — 这是为了最后执行异步操作的方便。 ④ 执行查询磁盘缓存的 block — 如果设置了 SDImageCacheQueryDiskSync，则同步执行；否则，默认是异步执行。 第 ③ 步中查询磁盘缓存的 queryDiskBlock 里边有两个比较重要的方法：- (nullable NSData *)diskImageDataBySearchingAllPathsForKey: 和 - (nullable UIImage *)diskImageForKey: data: options:，下边分别了解一下这两个方法： diskImageDataBySearchingAllPathsForKey: 这个方法用于查询磁盘缓存，实现及代码注释如下，拼接缓存路径的方法就不展开了，其中文件名的生成是对传入的 key 执行了一次 MD5。 12345678910111213141516171819202122232425262728293031- (nullable NSData *)diskImageDataBySearchingAllPathsForKey:(nullable NSString *)key &#123; // 1.尝试 通过默认路径查询磁盘缓存 NSString *defaultPath = [self defaultCachePathForKey:key]; NSData *data = [NSData dataWithContentsOfFile:defaultPath options:self.config.diskCacheReadingOptions error:nil]; if (data) &#123; return data; &#125; // 2.异常情况的处理：更换了路径再取一次，新路径是将默认路径的后缀去掉 (如果有的话) data = [NSData dataWithContentsOfFile:defaultPath.stringByDeletingPathExtension options:self.config.diskCacheReadingOptions error:nil]; if (data) &#123; return data; &#125; // 3.遍历所有用户自定义的路径，执行类似 1、2 的操作，查询磁盘缓存 NSArray&lt;NSString *&gt; *customPaths = [self.customPaths copy]; for (NSString *path in customPaths) &#123; NSString *filePath = [self cachePathForKey:key inPath:path]; NSData *imageData = [NSData dataWithContentsOfFile:filePath options:self.config.diskCacheReadingOptions error:nil]; if (imageData) &#123; return imageData; &#125; imageData = [NSData dataWithContentsOfFile:filePath.stringByDeletingPathExtension options:self.config.diskCacheReadingOptions error:nil]; if (imageData) &#123; return imageData; &#125; &#125; // 没查到的话，返回 nil return nil;&#125; diskImageForKey: data: options: 此方法的作用是对从 Disc 直接取的 data，进行 解码、解压操作，实现代码如下。 12345678910111213141516171819- (nullable UIImage *)diskImageForKey:(nullable NSString *)key data:(nullable NSData *)data options:(SDImageCacheOptions)options &#123; if (data) &#123; // 1.解码 UIImage *image = [[SDWebImageCodersManager sharedInstance] decodedImageWithData:data]; image = [self scaledImageForKey:key image:image]; // 2.解压 if (self.config.shouldDecompressImages) &#123; BOOL shouldScaleDown = options &amp; SDImageCacheScaleDownLargeImages; image = [[SDWebImageCodersManager sharedInstance] decompressedImageWithImage:image data:&amp;data options:@&#123;SDWebImageCoderScaleDownLargeImagesKey: @(shouldScaleDown)&#125;]; &#125; return image; &#125; else &#123; return nil; &#125;&#125; 此方法分别使用了 SDWebImageCodersManager 的 2 个重要方法： 解码方法 - (UIImage *)decodedImageWithData:data，其中 coder 可以理解为一个解码器，SDWebImage 提供了多种 coder，如 SDWebImageIOCoder、SDWebImageGIFCoder 分别用于解码某一种类型的图片，如果新增一种图片，可以将对应的 coder（需遵守协议：SDWebImageCoder） 添加到 coders 里即可。 123456789101112131415// SDWebImageCodersManager.m- (UIImage *)decodedImageWithData:(NSData *)data &#123; LOCK(self.codersLock); NSArray&lt;id&lt;SDWebImageCoder&gt;&gt; *coders = self.coders; UNLOCK(self.codersLock); for (id&lt;SDWebImageCoder&gt; coder in coders.reverseObjectEnumerator) &#123; if ([coder canDecodeFromData:data]) &#123; return [coder decodedImageWithData:data]; &#125; &#125; return nil;&#125; 解压方法 - (UIImage *)decompressedImageWithImage:image data:data options:optionsDict，这个方法和上边的解码方法都属于 SDWebImageCoder 这个协议，与解码方法类似，也是针对不同类型的 image 有不同的 coder。 1234567891011121314151617181920// SDWebImageCodersManager.m- (UIImage *)decompressedImageWithImage:(UIImage *)image data:(NSData *__autoreleasing _Nullable *)data options:(nullable NSDictionary&lt;NSString*, NSObject*&gt;*)optionsDict &#123; if (!image) &#123; return nil; &#125; LOCK(self.codersLock); NSArray&lt;id&lt;SDWebImageCoder&gt;&gt; *coders = self.coders; UNLOCK(self.codersLock); for (id&lt;SDWebImageCoder&gt; coder in coders.reverseObjectEnumerator) &#123; if ([coder canDecodeFromData:*data]) &#123; UIImage *decompressedImage = [coder decompressedImageWithImage:image data:data options:optionsDict]; decompressedImage.sd_imageFormat = image.sd_imageFormat; return decompressedImage; &#125; &#125; return nil;&#125; 那么，这些 coder 是什么时候加进去的，又是怎么添加的呢？其实，这些逻辑都在 SDWebImageCodersManager 的实现代码里。 1234567891011121314// SDWebImageCodersManager.m- (instancetype)init &#123; if (self = [super init]) &#123; // 初始化 coders NSMutableArray&lt;id&lt;SDWebImageCoder&gt;&gt; *mutableCoders = [@[[SDWebImageImageIOCoder sharedCoder]] mutableCopy];#ifdef SD_WEBP [mutableCoders addObject:[SDWebImageWebPCoder sharedCoder]];#endif _coders = [mutableCoders copy]; _codersLock = dispatch_semaphore_create(1); &#125; return self;&#125; 从初始化方法可以看出来，此时只给 coders 添加了一种 coder，即 SDWebImageImageIOCoder，它是用来对普通的 JPG、PNG 等图片解码的。 为了支持对其他类型图片（如 GIF）的解码，manager 给我们提供了下边这个添加 coder 的方法，代码逻辑很简单，就不多做解释了。 123456789101112131415// SDWebImageCodersManager.m- (void)addCoder:(nonnull id&lt;SDWebImageCoder&gt;)coder &#123; if (![coder conformsToProtocol:@protocol(SDWebImageCoder)]) &#123; return; &#125; LOCK(self.codersLock); NSMutableArray&lt;id&lt;SDWebImageCoder&gt;&gt; *mutableCoders = [self.coders mutableCopy]; if (!mutableCoders) &#123; mutableCoders = [NSMutableArray array]; &#125; [mutableCoders addObject:coder]; self.coders = [mutableCoders copy]; UNLOCK(self.codersLock);&#125; 小结关于缓存相关的类暂时就先介绍到这里，当然还有很多细节没来得及讨论，不过可以查看 demo 中的注释。 到此，关于 SDWebImage 的源码学习就告一段落了，目前的理解可能有点肤浅，以后随着理解的深入，会不定时的更新。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ SDWebImageDownloader]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-SDWebImageDownloader%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 4 篇，本篇将主要介绍 SDWebImageDownloader 这个负责下载的类，当然还有一些相关类及协议，如： SDWebImageDownloadToken、SDWebImageDownloaderOperation 和 SDWebImageDownloaderOperationInterface 等。 正文开启正文描述之前，依旧先看 2 个重要的枚举：SDWebImageDownloaderOptions 和 SDWebImageDownloaderExecutionOrder，具体含义见下方代码注释。 1234567891011121314151617181920212223242526272829// 控制下载过程的选项typedef NS_OPTIONS(NSUInteger, SDWebImageDownloaderOptions) &#123; // 降低下载任务在队列中的优先级 SDWebImageDownloaderLowPriority = 1 &lt;&lt; 0, // 图片将在下载过程中逐步展示，而不是等下载完成后才一次性展示 SDWebImageDownloaderProgressiveDownload = 1 &lt;&lt; 1, // 使用 NSURLCache，默认是不使用的 SDWebImageDownloaderUseNSURLCache = 1 &lt;&lt; 2, // 如果图片是从 NSURLCache 读取的，那么执行 completionHandler 的时候，回传 nil SDWebImageDownloaderIgnoreCachedResponse = 1 &lt;&lt; 3, // 后台继续执行任务 SDWebImageDownloaderContinueInBackground = 1 &lt;&lt; 4, // 允许处理存储在 NSHTTPCookieStore 中的 Cookie SDWebImageDownloaderHandleCookies = 1 &lt;&lt; 5, // 允许不受信任的 SSL 证书，生产环境慎用 SDWebImageDownloaderAllowInvalidSSLCertificates = 1 &lt;&lt; 6, // 提高下载任务在队列中的优先级 SDWebImageDownloaderHighPriority = 1 &lt;&lt; 7, // 缩放大图 SDWebImageDownloaderScaleDownLargeImages = 1 &lt;&lt; 8,&#125;;// 任务的执行顺序typedef NS_ENUM(NSInteger, SDWebImageDownloaderExecutionOrder) &#123; // 先进先出，也是队列的默认执行顺序 SDWebImageDownloaderFIFOExecutionOrder, // 后进先出，栈的执行顺序 SDWebImageDownloaderLIFOExecutionOrder&#125;; 接下来就到了 SDWebImageDownloader 这个类，我不准备一个属性一个方法地按顺序讨论，而是先说创建方法，然后通过一个主要方法将主体串起来。先看创建方法吧！ 对外其实只公开了一个创建单例的方法 sharedDownloader，仔细查看代码会发现，最终调用的是 - (nonnull instancetype)initWithSessionConfiguration: 这个初始化方法，主要做一些初始化工作，并创建一个新 session。如果使用单例方法创建 downloader，则只会有一个 session，而如果通过其他方法创建，则可能创建 session 之前已经有一个了，这时候就需要先将之前的 cancel 之后再创建新的。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263+ (nonnull instancetype)sharedDownloader &#123; static dispatch_once_t once; static id instance; dispatch_once(&amp;once, ^&#123; instance = [self new]; &#125;); return instance;&#125;- (nonnull instancetype)init &#123; return [self initWithSessionConfiguration:[NSURLSessionConfiguration defaultSessionConfiguration]];&#125;- (nonnull instancetype)initWithSessionConfiguration:(nullable NSURLSessionConfiguration *)sessionConfiguration &#123; if ((self = [super init])) &#123; // 执行下载任务的 operation _operationClass = [SDWebImageDownloaderOperation class]; // 要求解压图片 _shouldDecompressImages = YES; // 执行顺序，先进先出 _executionOrder = SDWebImageDownloaderFIFOExecutionOrder; // 设置下载操作的队列，由于最大并发数是 6，所以此 queue 是 并发队列，如果是 1，则为串行队列。 _downloadQueue = [NSOperationQueue new]; _downloadQueue.maxConcurrentOperationCount = 6; _downloadQueue.name = @"com.hackemist.SDWebImageDownloader"; _URLOperations = [NSMutableDictionary new]; // 请求头的字段，可接受的文件类型#ifdef SD_WEBP _HTTPHeaders = [@&#123;@"Accept": @"image/webp,image/*;q=0.8"&#125; mutableCopy];#else _HTTPHeaders = [@&#123;@"Accept": @"image/*;q=0.8"&#125; mutableCopy];#endif // 锁，这里使用了信号量 _operationsLock = dispatch_semaphore_create(1); _headersLock = dispatch_semaphore_create(1); // 超时时间 _downloadTimeout = 15.0; [self createNewSessionWithConfiguration:sessionConfiguration]; &#125; return self;&#125;// 创建新的 session- (void)createNewSessionWithConfiguration:(NSURLSessionConfiguration *)sessionConfiguration &#123; // 为避免影响，先取消可能存在的下载任务 [self cancelAllDownloads]; // cancel 之前的 session，然后创建一个新的 if (self.session) &#123; [self.session invalidateAndCancel]; &#125; sessionConfiguration.timeoutIntervalForRequest = self.downloadTimeout; self.session = [NSURLSession sessionWithConfiguration:sessionConfiguration delegate:self delegateQueue:nil];&#125; 然后，我们看一下主要方法 - (nullable SDWebImageDownloadToken *)downloadImageWithURL:url options:options progress:progressBlock completed:completedBlock。直接调用了添加进度与完成回调的方法，并将返回值作为结果返回。 添加进度与完成回调的方法我们稍后再议，先看一下调用时传入的 createCallback。就做了两件事：先创建一个 request，用于准备一些基础参数，然后，依据 request 创建 operation，详见代码注释 ☟。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465- (nullable SDWebImageDownloadToken *)downloadImageWithURL:(nullable NSURL *)url options:(SDWebImageDownloaderOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock &#123; __weak SDWebImageDownloader *wself = self; return [self addProgressCallback:progressBlock completedBlock:completedBlock forURL:url createCallback:^SDWebImageDownloaderOperation *&#123; __strong __typeof (wself) sself = wself; NSTimeInterval timeoutInterval = sself.downloadTimeout; if (timeoutInterval == 0.0) &#123; timeoutInterval = 15.0; &#125;// *** 1.创建 request // 为避免重复缓存 (NSURLCache + SDImageCache) ，如果没有明确要求使用 NSURLCache，我们默认忽略本地缓存 NSURLRequestCachePolicy cachePolicy = options &amp; SDWebImageDownloaderUseNSURLCache ? NSURLRequestUseProtocolCachePolicy : NSURLRequestReloadIgnoringLocalCacheData; NSMutableURLRequest *request = [[NSMutableURLRequest alloc] initWithURL:url cachePolicy:cachePolicy timeoutInterval:timeoutInterval]; // The default is YES - in other words, cookies are sent from and stored to the cookie manager by default. request.HTTPShouldHandleCookies = (options &amp; SDWebImageDownloaderHandleCookies); request.HTTPShouldUsePipelining = YES; // 设置 header，headersFilter 是过滤头部参数的block if (sself.headersFilter) &#123; request.allHTTPHeaderFields = sself.headersFilter(url, [sself allHTTPHeaderFields]); &#125; else &#123; request.allHTTPHeaderFields = [sself allHTTPHeaderFields]; &#125;// *** 2.创建下载的 operation (这个 operationClass ，给他赋什么值，他就是什么，如果不设置，就是默认值：[SDWebImageDownloaderOperation class]) SDWebImageDownloaderOperation *operation = [[sself.operationClass alloc] initWithRequest:request inSession:sself.session options:options]; operation.shouldDecompressImages = sself.shouldDecompressImages; // NSURLCredential 身份认证 if (sself.urlCredential) &#123; operation.credential = sself.urlCredential; &#125; else if (sself.username &amp;&amp; sself.password) &#123; // NSURLCredentialPersistenceForSession: Credential should be stored only for this session. operation.credential = [NSURLCredential credentialWithUser:sself.username password:sself.password persistence:NSURLCredentialPersistenceForSession]; &#125; // 设置优先级 if (options &amp; SDWebImageDownloaderHighPriority) &#123; operation.queuePriority = NSOperationQueuePriorityHigh; &#125; else if (options &amp; SDWebImageDownloaderLowPriority) &#123; operation.queuePriority = NSOperationQueuePriorityLow; &#125; // 更改执行顺序：先进后出(可在此设置) or 先进先出(默认) if (sself.executionOrder == SDWebImageDownloaderLIFOExecutionOrder) &#123; // 通过反向设置依赖，指定了队列中任务的执行顺序先加进去的依赖于后加进去的，那就成了后进先出了😎 [sself.lastAddedOperation addDependency:operation]; sself.lastAddedOperation = operation; &#125; return operation; &#125;];&#125; 现在，我们来看看添加进度与完成回调的方法： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253- (nullable SDWebImageDownloadToken *)addProgressCallback:(SDWebImageDownloaderProgressBlock)progressBlock completedBlock:(SDWebImageDownloaderCompletedBlock)completedBlock forURL:(nullable NSURL *)url createCallback:(SDWebImageDownloaderOperation *(^)(void))createCallback &#123; // The URL will be used as the key to the callbacks dictionary so it cannot be nil. If it is nil immediately call the completed block with no image or data. if (url == nil) &#123; if (completedBlock != nil) &#123; completedBlock(nil, nil, nil, NO); &#125; return nil; &#125; LOCK(self.operationsLock); SDWebImageDownloaderOperation *operation = [self.URLOperations objectForKey:url]; // 如果是第 1 次进来，通过 url 是取不出 URLOperation 的，但是第 2 次就有可能找到，也就是想要重复发第 2 次请求的话，就可以取到。 // 第 2 次可以取到(并且已经完成的情况下)，则不会走括号里边，也就不会执行关键步骤：[self.downloadQueue addOperation:operation]; ，所以就不会发起请求了，因为将 operation 添加到队列的时候，系统会自动触发请求。 if (!operation || operation.isFinished) &#123; // 创建 operation operation = createCallback(); __weak typeof(self) wself = self; operation.completionBlock = ^&#123; __strong typeof(wself) sself = wself; if (!sself) &#123; return; &#125; LOCK(sself.operationsLock); [sself.URLOperations removeObjectForKey:url]; UNLOCK(sself.operationsLock); &#125;; [self.URLOperations setObject:operation forKey:url]; // 添加到队列，即开始执行！ // Add operation to operation queue only after all configuration done according to Apple's doc. // `addOperation:` does not synchronously execute the `operation.completionBlock` so this will not cause deadlock. [self.downloadQueue addOperation:operation]; &#125; UNLOCK(self.operationsLock); // 存放进度和完成回调的 数组 array id downloadOperationCancelToken = [operation addHandlersForProgress:progressBlock completed:completedBlock]; // 与下载任务关联的一个对象，用于取消操作的时候 SDWebImageDownloadToken *token = [SDWebImageDownloadToken new]; token.downloadOperation = operation; token.url = url; token.downloadOperationCancelToken = downloadOperationCancelToken; return token;&#125; 主要做了这么几件事： 开始依然是参数校验 然后从 self.URLOperation 里边取 operation 第一次进来当然取不到 operation，于是就会进入 if (!operation || operation.isFinished) { ... } 的代码块。先执行我们传入的 createCallback() 创建 operation，然后将 operation 加入到 self.URLOperations 里边，同时设置好 operation 的 completionBlock，到时将 operation 移除，最后将 operation 加入到操作队列里，就会自动开始执行了。 创建一个 token，他是 SDWebImageDownloadToken 的实例，将它与 operation、url、progressBlock 及 completedBlock 关联起来，用于后边之后的取消操作。其中 progressBlock 及 completedBlock 是通过 downloadOperationCancelToken 与 token 关联起来的，这里用到了 operation 中的一个方法： 12345678910111213141516171819// SDWebImageDownloaderOperation- (nullable id)addHandlersForProgress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock &#123; SDCallbacksDictionary *callbacks = [NSMutableDictionary new]; if (progressBlock) callbacks[kProgressCallbackKey] = [progressBlock copy]; if (completedBlock) callbacks[kCompletedCallbackKey] = [completedBlock copy]; LOCK(self.callbacksLock); [self.callbackBlocks addObject:callbacks]; UNLOCK(self.callbacksLock); return callbacks;&#125; 由此可知，这个 id downloadOperationCancelToken 是一个存放 progressBlock 和 completedBlock 的 dictionary。 这里 if 语句起到了一个非常重要的作用，即 避免重复下载相同数据，具体原因就不解释了，上边的代码注释里已经写了。 到这里是不是觉得少了点什么，是的，SDWebImageDownloaderOperation 和 SDWebImageDownloadToken 的具体实现还不知道呢，接下来我们就分别查看这 2 个类，先从简单的开始吧！ SDWebImageDownloadToken这个类只有 3 个属性，前边都用到了，属性声明如下： 123456// 下载任务对应的 url@property (nonatomic, strong, nullable) NSURL *url;// 实际是包含 progressBlock 和 completionBlock 的字典，是通过 `addHandlersForProgress:completed:` 返回的，用于取消操作@property (nonatomic, strong, nullable) id downloadOperationCancelToken;// 操作的 operation，继承自 NSOperation，不过又遵守了 `SDWebImageDownloaderOperationInterface` 这个协议，扩展了一些方法。@property (nonatomic, weak, nullable) NSOperation&lt;SDWebImageDownloaderOperationInterface&gt; *downloadOperation; 它只实现了一个协议方法 cancel（Protocol: SDWebImageOperation），其中 self.downloadOperationCancelToken就是存放 progressBlock 和 completionBlock 的字典，然后将这个 token 创递给了 operation 的 cancel: 方法（也是一个协议方法），这个方法的具体实现下边就会说到。 12345678- (void)cancel &#123; if (self.downloadOperation) &#123; SDWebImageDownloadToken *cancelToken = self.downloadOperationCancelToken; if (cancelToken) &#123; [self.downloadOperation cancel:cancelToken]; &#125; &#125;&#125; SDWebImageDownloaderOperationInterface在开始介绍 operation 之前，先看看他遵守的协议 SDWebImageDownloaderOperationInterface，声明了以下协议方法。如果想要使用自定义的 operation，则它必须继承自 NSOperation 并且遵守这个协议。这些方法的实现可以参考 SDWebImageDownloaderOperation。 12345678910111213141516// SDWebImageDownloaderOperationInterface// 初始化方法- (nonnull instancetype)initWithRequest:(nullable NSURLRequest *)request inSession:(nullable NSURLSession *)session options:(SDWebImageDownloaderOptions)options;// 保存进度和完成的回调- (nullable id)addHandlersForProgress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock;// 是否需要解压图片- (BOOL)shouldDecompressImages;- (void)setShouldDecompressImages:(BOOL)value;// 凭证或称证书信息- (nullable NSURLCredential *)credential;- (void)setCredential:(nullable NSURLCredential *)value;// 取消- (BOOL)cancel:(nullable id)token; SDWebImageDownloaderOperation现在开始讨论 SDWebImageDownloaderOperation 这个类，下边是初始化方法： 123456789101112131415161718192021- (nonnull instancetype)init &#123; return [self initWithRequest:nil inSession:nil options:0];&#125;// 也是协议方法- (nonnull instancetype)initWithRequest:(nullable NSURLRequest *)request inSession:(nullable NSURLSession *)session options:(SDWebImageDownloaderOptions)options &#123; if ((self = [super init])) &#123; _request = [request copy]; _shouldDecompressImages = YES; _options = options; _callbackBlocks = [NSMutableArray new]; _executing = NO; _finished = NO; _expectedSize = 0; _unownedSession = session; _callbacksLock = dispatch_semaphore_create(1); _coderQueue = dispatch_queue_create("com.hackemist.SDWebImageDownloaderOperationCoderQueue", DISPATCH_QUEUE_SERIAL); &#125; return self;&#125; 下面介绍 2 个比较重要的方法： 核心方法 start 这是重写父类 NSOperation 的 start 方法，添加了自定义的操作。这个方法不需要手动调用，在将 operation 添加到 operationQueue 中的时候，系统会自动调用其 start 方法。重写后的操作包括以下几点： ①检测操作是否已取消，如果取消了，重置数据后直接返回。12345if (self.isCancelled) &#123; self.finished = YES; [self reset]; return;&#125; ②如果需要 App 进入后台时，继续执行下载操作，需要开启后台任务。并设置 ExpirationHandler，取消下载任务，并结束后台操作。1234567891011121314151617// 如果需要App进入后台时，继续执行此操作，需要开启后台任务。Class UIApplicationClass = NSClassFromString(@"UIApplication");BOOL hasApplication = UIApplicationClass &amp;&amp; [UIApplicationClass respondsToSelector:@selector(sharedApplication)];if (hasApplication &amp;&amp; [self shouldContinueWhenAppEntersBackground]) &#123; __weak __typeof__ (self) wself = self; UIApplication * app = [UIApplicationClass performSelector:@selector(sharedApplication)]; self.backgroundTaskId = [app beginBackgroundTaskWithExpirationHandler:^&#123; __strong __typeof (wself) sself = wself; if (sself) &#123; [sself cancel]; [app endBackgroundTask:sself.backgroundTaskId]; sself.backgroundTaskId = UIBackgroundTaskInvalid; &#125; &#125;];&#125; ③获取或创建 session，然后创建 dataTask， ④若 dataTask 创建成功，启动下载任务 [self.dataTask resume];，然后执行一次 progressBlock，并发送开始下载的通知。 ④若 dataTask 创建失败，直接调用完成回调，构建 error 信息并返回，然后重置数据。 ⑤关闭可能存在的后台下载任务。 具体下载过程中的操作，都在 session 相关的那些协议方法里边，详见代码注释，这里就不啰嗦了。 取消操作 最后看一下取消操作，即协议方法 - (BOOL)cancel:(nullable id)token; 的实现，将取消过程中用到的所有方法都展开就是下边这样： 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263- (BOOL)cancel:(nullable id)token &#123; BOOL shouldCancel = NO; LOCK(self.callbacksLock); // 移除 token，即移除一个存储着 completionBlock 和 progressBlock 的字典 [self.callbackBlocks removeObjectIdenticalTo:token]; // 如果已经没有回调，就去执行整体的 cancel 操作 if (self.callbackBlocks.count == 0) &#123; shouldCancel = YES; &#125; UNLOCK(self.callbacksLock); if (shouldCancel) &#123; // *** 点开 ☟ [self cancel]; &#125; return shouldCancel;&#125;- (void)cancel &#123; @synchronized (self) &#123; // *** 点开 ☟ [self cancelInternal]; &#125;&#125;- (void)cancelInternal &#123; if (self.isFinished) return; [super cancel]; if (self.dataTask) &#123; // 取消下载任务，并发出停止的通知 [self.dataTask cancel]; __weak typeof(self) weakSelf = self; dispatch_async(dispatch_get_main_queue(), ^&#123; [[NSNotificationCenter defaultCenter] postNotificationName:SDWebImageDownloadStopNotification object:weakSelf]; &#125;); // As we cancelled the task, its callback won't be called and thus won't // maintain the isFinished and isExecuting flags. if (self.isExecuting) self.executing = NO; if (!self.isFinished) self.finished = YES; &#125; // *** 点开 ☟ [self reset];&#125;// 重置变量- (void)reset &#123; LOCK(self.callbacksLock); [self.callbackBlocks removeAllObjects]; UNLOCK(self.callbacksLock); self.dataTask = nil; if (self.ownedSession) &#123; [self.ownedSession invalidateAndCancel]; self.ownedSession = nil; &#125;&#125; 小结SDWebImageDownloader 的内容就先介绍到这类，其他细节见 HHSDWebImageStudy 中的源码注释。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ SDWebImageManager]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-SDWebImageManager%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 3 篇，在前一篇中，我们了解了 SDWebImage 执行的基本流程，本篇就来介绍第一个核心类 SDWebImageMananger。 正文SDWebImageMananger.h 文件基本可以分为 3 各部分： ①定义了一个枚举 SDWebImageOptions，列举了可能会用到的一些场景。123456789101112131415161718192021222324252627282930313233343536typedef NS_OPTIONS(NSUInteger, SDWebImageOptions) &#123; // 重试已经失败的 url SDWebImageRetryFailed = 1 &lt;&lt; 0, // 低优先级，比如，在有 UI 交互的情况下，会延迟下载操作 SDWebImageLowPriority = 1 &lt;&lt; 1, // 下载完成后，仅做内存缓存，不做磁盘缓存 SDWebImageCacheMemoryOnly = 1 &lt;&lt; 2, // 下载过程中逐步加载图片，而不是完全下载完之后才展示 SDWebImageProgressiveDownload = 1 &lt;&lt; 3, // 刷新缓存 SDWebImageRefreshCached = 1 &lt;&lt; 4, // 当 App 进入后台时，继续下载任务，如果后台任务超时，操作将被自动取消 SDWebImageContinueInBackground = 1 &lt;&lt; 5, // 允许处理 Cookie SDWebImageHandleCookies = 1 &lt;&lt; 6, // 允许不受信任的 SSL 证书，生产环境慎用 SDWebImageAllowInvalidSSLCertificates = 1 &lt;&lt; 7, // 高优先级，即会把相应的图片放到最前边加载，而不是按照加入队列时的顺序执行 SDWebImageHighPriority = 1 &lt;&lt; 8, // 延迟 placeholder 的加载，即在下载完成时才加载 SDWebImageDelayPlaceholder = 1 &lt;&lt; 9, // 对动图也执行 transform 操作 SDWebImageTransformAnimatedImage = 1 &lt;&lt; 10, // 图片下载完成后，不直接自动给 imageView 赋值，给用户调整图片的机会 SDWebImageAvoidAutoSetImage = 1 &lt;&lt; 11, // 依据设备内存缩放图片，如果设置了 `SDWebImageProgressiveDownload` ，此设置无效 SDWebImageScaleDownLargeImages = 1 &lt;&lt; 12, // 在有内存缓存的情况下，依然需要查询磁盘缓存，建议与 SDWebImageQueryDiskSync 配合使用 SDWebImageQueryDataWhenInMemory = 1 &lt;&lt; 13, // 同步查询磁盘缓存 SDWebImageQueryDiskSync = 1 &lt;&lt; 14, // 仅加载缓存图片 SDWebImageFromCacheOnly = 1 &lt;&lt; 15, // 对内存和磁盘中的 image 也执行 transition 的操作 SDWebImageForceTransition = 1 &lt;&lt; 16&#125;; ②定义了一个协议 SDWebImageManagerDelegate，这里提供了以下 3 个协议方法：12345678// 缓存中没有指定图片时，是否需要下载- (BOOL)imageManager:(nonnull SDWebImageManager *)imageManager shouldDownloadImageForURL:(nullable NSURL *)imageURL;// 是否需要将制定 URL 标记为失败的 URL- (BOOL)imageManager:(nonnull SDWebImageManager *)imageManager shouldBlockFailedURL:(nonnull NSURL *)imageURL withError:(nonnull NSError *)error;// 允许在刚刚下载到 image 并且未做缓存之前，对图片执行 transform，返回处理后的 image- (nullable UIImage *)imageManager:(nonnull SDWebImageManager *)imageManager transformDownloadedImage:(nullable UIImage *)image withURL:(nullable NSURL *)imageURL; ③SDWebImageManager 的头文件，有几个重要属性，他们的作用见下边的注释。 12345678910// 代理对象@property (weak, nonatomic, nullable) id &lt;SDWebImageManagerDelegate&gt; delegate;// 处理缓存的对象@property (strong, nonatomic, readonly, nullable) SDImageCache *imageCache;// 处理下载工作的对象@property (strong, nonatomic, readonly, nullable) SDWebImageDownloader *imageDownloader;// 一个用户定义的 block，用于生成 cacheKey@property (nonatomic, copy, nullable) SDWebImageCacheKeyFilterBlock cacheKeyFilter;// 一个用户定义的 block，用于序列化下载到的数据@property (nonatomic, copy, nullable) SDWebImageCacheSerializerBlock cacheSerializer; 下面是 2 个常用的创建方法：+ (nonnull instancetype)sharedManager; 和 - (nonnull instancetype)initWithCache:(nonnull SDImageCache *)cache downloader:(nonnull SDWebImageDownloader *)downloader;，其实最终都是调用了后者 。 123456789101112131415161718192021222324252627282930313233// 单例+ (nonnull instancetype)sharedManager &#123; static dispatch_once_t once; static id instance; dispatch_once(&amp;once, ^&#123; instance = [self new]; &#125;); return instance;&#125;// 初始化，创建处理缓存和下载任务的对象 cache 和 downloader- (nonnull instancetype)init &#123; SDImageCache *cache = [SDImageCache sharedImageCache]; SDWebImageDownloader *downloader = [SDWebImageDownloader sharedDownloader]; return [self initWithCache:cache downloader:downloader];&#125;// 核心的初始化方法，为各属性赋初值- (nonnull instancetype)initWithCache:(nonnull SDImageCache *)cache downloader:(nonnull SDWebImageDownloader *)downloader &#123; if ((self = [super init])) &#123; // 处理缓存和下载任务的对象 _imageCache = cache; _imageDownloader = downloader; // 用于存储请求失败的 URL 的集合及操作时用的锁 (信号量) _failedURLs = [NSMutableSet new]; _failedURLsLock = dispatch_semaphore_create(1); // 存储运行中 operation 的集合，通过判断他的 count 是否为 0，判断操作是否在进行中：BOOL isRunning = (self.runningOperations.count &gt; 0); _runningOperations = [NSMutableSet new]; // 操作时用的锁 (信号量) _runningOperationsLock = dispatch_semaphore_create(1); &#125; return self;&#125; 另外几个方法，就不单独介绍了，用到的时候再继续讨论。此处，我们只看一个核心方法 - (nullable id &lt;SDWebImageOperation&gt;)loadImageWithURL:url options:options progress:progressBlock completed:completedBlock;，下面我们来一步步讨论这个方法的具体实现。 1.校验参数依次做如下处理：如果传入的 completedBlock 为空，就直接报错；如果传入的参数是 NSString * 类型的，需要将其转换成 NSURL；最后，如果 url 还不是 NSURL 类型，那就只能将其置为 nil，以免造成后边 Crash。123456789NSAssert(completedBlock != nil, @"If you mean to prefetch the image, use -[SDWebImagePrefetcher prefetchURLs] instead");if ([url isKindOfClass:NSString.class]) &#123; url = [NSURL URLWithString:(NSString *)url];&#125;if (![url isKindOfClass:NSURL.class]) &#123; url = nil;&#125; 2.生成总的 operation他是 SDWebImageCombinedOperation 实例对象，也是当前方法要返回的结果，并将当前类赋值给 operation 的一个 weak 属性(避免循环引用)。 12SDWebImageCombinedOperation *operation = [SDWebImageCombinedOperation new];operation.manager = self; // 肯定是 weak 属性 SDWebImageCombinedOperation 的声明与实现文件均在当前类 SDWebImageManager 的实现文件里边，简单看一下他的 .h/.m 文件吧。 1234567891011121314151617181920212223242526272829303132333435363738394041// SDWebImageCombinedOperation.h@interface SDWebImageCombinedOperation : NSObject &lt;SDWebImageOperation&gt;// 标识是否已取消@property (assign, nonatomic, getter = isCanceled) BOOL cancelled;// downloadToken 这是一个继承自 NSObject 的类，他有一个继承自 NSOperation 的属性，也就是真正执行下载操作时的 operation，cancel 时会用到@property (strong, nonatomic, nullable) SDWebImageDownloadToken *downloadToken;// 查询缓存时的 operation，用于标识当前 operation 是否已经被取消。其实查询缓存时，首先查看 operation.isCanceled，如果没被取消了，就会再去查询了。cancel 时会将其 isCanceled 属性置为 YES。@property (strong, nonatomic, nullable) NSOperation *cacheOperation;// manager@property (weak, nonatomic, nullable) SDWebImageManager *manager;@end// SDWebImageCombinedOperation.m#pragma mark - 代理方法实现@implementation SDWebImageCombinedOperation- (void)cancel &#123; @synchronized(self) &#123; self.cancelled = YES; // 取消查询缓存的 Operation，此时 isCanceled 会被置为 YES。 if (self.cacheOperation) &#123; [self.cacheOperation cancel]; self.cacheOperation = nil; &#125; // 取消下载操作 if (self.downloadToken) &#123; [self.manager.imageDownloader cancel:self.downloadToken]; &#125; // 将当前 operation 从 manager 中运行着的 operation 数组中移除。 [self.manager safelyRemoveOperationFromRunning:self]; &#125;&#125;@end// SDWebImageOperation 协议的定义@protocol SDWebImageOperation &lt;NSObject&gt;- (void)cancel;@end 可以看到，SDWebImageCombinedOperation 这个类的主要作用就是 cancel 操作，包括 cancel 查询缓存 和 cancel 下载数据。 3.再次检测一下 url如果是曾经失败的 url，而且不允许重试，或者 url 为空时，执行 completionBlock，并返回当前 operation。 123456789101112131415161718// self.failedURLs 是一个保存曾经失败过的 URL 的数组，用于检测当前 URL 是不是曾经请求失败过的URL.另外，搜索一个个元素的时候，NSSet 比 NSArray 查询更快。 BOOL isFailedUrl = NO; if (url) &#123; LOCK(self.failedURLsLock); isFailedUrl = [self.failedURLs containsObject:url]; UNLOCK(self.failedURLsLock); &#125; // 若出现以下两种情况就不再往下走了，直接执行 CompletionBlock：① URL 是空的；② 此 URL 是曾经请求失败的 URL，并且规定不允许重新请求曾经失败的 URL。 if (url.absoluteString.length == 0 || (!(options &amp; SDWebImageRetryFailed) &amp;&amp; isFailedUrl)) &#123; [self callCompletionBlockForOperation:operation completion:completedBlock error:[NSError errorWithDomain:NSURLErrorDomain code:NSURLErrorFileDoesNotExist userInfo:nil] url:url]; return operation; &#125; 4.保存 operation 至 self.runningOperations后者是一个数组，这里使用了信号量来确保线程安装。 123LOCK(self.runningOperationsLock);[self.runningOperations addObject:operation];UNLOCK(self.runningOperationsLock); 5.查询缓存。123456789101112131415NSString *key = [self cacheKeyForURL:url];SDImageCacheOptions cacheOptions = 0;if (options &amp; SDWebImageQueryDataWhenInMemory) cacheOptions |= SDImageCacheQueryDataWhenInMemory;if (options &amp; SDWebImageQueryDiskSync) cacheOptions |= SDImageCacheQueryDiskSync;if (options &amp; SDWebImageScaleDownLargeImages) cacheOptions |= SDImageCacheScaleDownLargeImages;__weak SDWebImageCombinedOperation *weakOperation = operation;operation.cacheOperation = [self.imageCache queryCacheOperationForKey:key options:cacheOptions done:^(UIImage *cachedImage, NSData *cachedData, SDImageCacheType cacheType)&#123; // 查询完成后的操作在这里，可能查到了，也可能没查到...&#125; 这里先准备了 2 个参数，查询的依据 key 和一些条件 cacheOptions。key 的获取是通过一个私有方法 (如下)，如果自定义了 key 的生成规则 self.cacheKeyFilter，就用自定义的，如果没有，就直接取 url.absoluteString。cacheOptions 是一个用 NS_OPTIONS 定义的枚举类型 (前边已介绍过)，可组合多种情况，在这里综合了 2 个查询的要求和 1 个缩放图片的要求。 1234567891011- (nullable NSString *)cacheKeyForURL:(nullable NSURL *)url &#123; if (!url) &#123; return @""; &#125; if (self.cacheKeyFilter) &#123; return self.cacheKeyFilter(url); &#125; else &#123; return url.absoluteString; &#125;&#125; 6.查询的具体过程详情将会在 SDImageCache 中介绍，下面讨论一下查询缓存结束后的操作。 7.移除当前 operation从 self.runningOperations 这个数组中移除当前 operation。 1234if (!strongOperation || strongOperation.isCancelled) &#123; [self safelyRemoveOperationFromRunning:strongOperation]; return;&#125; 8.判断是否需要下载当同时满足 3 个要求时，就需要下载新数据了：①没要求只能从缓存获取数据，即当缓存找不到时，可以去下载；②找不到缓存 或 要求必须更新缓存；③当 self.delegate 没有遵守协议， 或者 协议方法返回 YES。 123BOOL shouldDownload = (!(options &amp; SDWebImageFromCacheOnly)) &amp;&amp; (!cachedImage || options &amp; SDWebImageRefreshCached) &amp;&amp; (![self.delegate respondsToSelector:@selector(imageManager:shouldDownloadImageForURL:)] || [self.delegate imageManager:self shouldDownloadImageForURL:url]); 9.若需要下载 首先依然要做一个判断，即 如果有缓存数据并且要求刷新缓存数据时，需要先调用一次 CompletionBlock，将缓存数据返回去，然后再开始下载新数据，代码如下： 123if (cachedImage &amp;&amp; options &amp; SDWebImageRefreshCached) &#123; [self callCompletionBlockForOperation:strongOperation completion:completedBlock image:cachedImage data:cachedData error:nil cacheType:cacheType finished:YES url:url];&#125; 然后，准备下载数据时所需的一些基本选项，可以参考篇头介绍的枚举 SDWebImageOptions。 开始下载，调用了 SDWebImageDownloader 的下载方法，留待 SDWebImageDownloader 介绍，这里只讨论下载完成之后的操作。 12345678__weak typeof(strongOperation) weakSubOperation = strongOperation; strongOperation.downloadToken = [self.imageDownloader downloadImageWithURL:url options:downloaderOptions progress:progressBlock completed:^(UIImage *downloadedImage, NSData *downloadedData, NSError *error, BOOL finished)&#123; // 下载完成后的操作...&#125; 下载完成后，可以分这么几种情况：a.当前 operation 已经被取消，这种情况下不作任何操作，包括回调。 b.下载出错，先将失败的 error 信息返回，然后决定是否需要将当前 URL 存入失败的 URL 数组。 c.下载成功，此时要做的工作还有许多： ①如果设置了失败重发，则将当前 URL 从失败的 URL 数组中移除。12345if ((options &amp; SDWebImageRetryFailed)) &#123; LOCK(self.failedURLsLock); [self.failedURLs removeObject:url]; UNLOCK(self.failedURLsLock);&#125; ②对于自定义的 manager，需要执行另外一套缩放标准。123456if (self != [SDWebImageManager sharedManager] &amp;&amp; self.cacheKeyFilter &amp;&amp; downloadedImage)&#123; downloadedImage = [self scaledImageForKey:key image:downloadedImage];&#125; ③若需要更新缓存，但是未下载到图片，且缓存中本来有值的情况下，什么也不做，因为下载之前早已经缓存数据返回了。123if (options &amp; SDWebImageRefreshCached &amp;&amp; cachedImage &amp;&amp; !downloadedImage) &#123;// 需要更新缓存，但是未下载到图片，且缓存中本来有值的情况下，什么也不做，因为下载之前早已经缓存数据返回了&#125; ④如果下载到了图片，并且要求 transform 图片的情况下，异步执行 transform 和缓存图片的工作，然后回到主线程执行 completionBlock。12345678910111213141516171819202122232425262728293031323334dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_HIGH, 0), ^&#123; UIImage *transformedImage = [self.delegate imageManager:self transformDownloadedImage:downloadedImage withURL:url]; if (transformedImage &amp;&amp; finished) &#123; BOOL imageWasTransformed = ![transformedImage isEqual:downloadedImage]; NSData *cacheData; // pass nil if the image was transformed, so we can recalculate the data from the image if (self.cacheSerializer) &#123; cacheData = self.cacheSerializer(transformedImage, (imageWasTransformed ? nil : downloadedData), url); &#125; else &#123; cacheData = (imageWasTransformed ? nil : downloadedData); &#125; // *** 存盘：注意是存的 imageData [self.imageCache storeImage:transformedImage imageData:cacheData forKey:key toDisk:cacheOnDisk completion:nil]; &#125; [self callCompletionBlockForOperation:strongSubOperation completion:completedBlock image:transformedImage data:downloadedData error:nil cacheType:SDImageCacheTypeNone finished:finished url:url]; &#125;); ⑤如果下载到了图片，并且下载完成的话，则存盘并执行 completionBlock。存盘调用了 SDImageCache 的方法，随后介绍。 最后将当前 operation 移除。 12345678910111213141516171819202122232425262728293031323334353637383940dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_HIGH, 0), ^&#123; UIImage *transformedImage = [self.delegate imageManager:self transformDownloadedImage:downloadedImage withURL:url]; if (transformedImage &amp;&amp; finished) &#123; BOOL imageWasTransformed = ![transformedImage isEqual:downloadedImage]; NSData *cacheData; // pass nil if the image was transformed, so we can recalculate the data from the image if (self.cacheSerializer) &#123; cacheData = self.cacheSerializer(transformedImage, (imageWasTransformed ? nil : downloadedData), url); &#125; else &#123; cacheData = (imageWasTransformed ? nil : downloadedData); &#125; // *** 存盘：注意是存的 imageData [self.imageCache storeImage:transformedImage imageData:cacheData forKey:key toDisk:cacheOnDisk completion:nil]; &#125; [self callCompletionBlockForOperation:strongSubOperation completion:completedBlock image:transformedImage data:downloadedData error:nil cacheType:SDImageCacheTypeNone finished:finished url:url]; &#125;);// ...if (finished) &#123; [self safelyRemoveOperationFromRunning:strongSubOperation];&#125; 10.若不需要下载，并且有缓存此时，执行 completionBlock 将缓存数据返回，然后移除当前 operation。 12345678910[self callCompletionBlockForOperation:strongOperation completion:completedBlock image:cachedImage data:cachedData error:nil cacheType:cacheType finished:YES url:url];[self safelyRemoveOperationFromRunning:strongOperation]; 11.其它，即没有缓存，且不需要下载和上边的操作类似，只不过传回的 image 和 data 均为 nil。12345678910[self callCompletionBlockForOperation:strongOperation completion:completedBlock image:nil data:nil error:nil cacheType:SDImageCacheTypeNone finished:YES url:url];[self safelyRemoveOperationFromRunning:strongOperation]; 最后将 operation 返回。 小结以上就是 SDWebImageManager 这个类的主要功能，其中关于缓存和下载的内容，详见后边几篇的讨论。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ 结构及基本流程]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-%E7%BB%93%E6%9E%84%E5%8F%8A%E5%9F%BA%E6%9C%AC%E6%B5%81%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[一、前言这是本系列的第 2 篇，本篇的目的如题所示，就是了解 SDWebImage 这个常用框架的文件结构，然后通过案例梳理流程，作为后边章节的主线。 二、目录结构为了查看 SDWebImage 的完整目录结构，首先需要导入 SDWebImage，由 上一篇 我们了解到，新版 SDWebImage 总共分了 4 个子 pod，默认只导入了 Core，为了将它们全部导入 demo 中，Podfile 文件需要这么来写： 1234567891011source 'https://github.com/CocoaPods/Specs.git'platform :ios, '9.0'target 'HHSDWebImageStudy' dopod 'SDWebImage', '4.4.2' // 为了讨论方便，这里选定了一个比较新的版本pod 'SDWebImage/WebP'pod 'SDWebImage/GIF'pod 'SDWebImage/MapKit'end 执行 pod install 之后得到的目录结构如下图所示，点此查看完整目录结构 。 三、示例：用 UIImageView 展示一张静态网络图片下面是使用时的代码，加载图片时调用的是 UIImageView+WebCache 中的方法 sd_setImageWithURL:。 123456// 1.创建 UIImageViewUIImageView *imgV = [[UIImageView alloc] initWithFrame:CGRectMake(125, 70, 160, 160)];[self.view addSubview:imgV];// 2.加载网络图片[imgV sd_setImageWithURL:[NSURL URLWithString:@"https://img.zcool.cn/community/01c81558a2723ca801219c77a1e34e.jpg"]]; 查看 sd_setImageWithURL: 的实现，内部调用了一个参数很全的方法 sd_setImageWithURL: placeholderImage: options: progress: completed:，只不过其他参数已经给了默认值。123- (void)sd_setImageWithURL:(nullable NSURL *)url &#123; [self sd_setImageWithURL:url placeholderImage:nil options:0 progress:nil completed:nil];&#125; 其实，还有很多类似方法，都是对这个方法不同程度的封装，即给一些参数提供了默认值，或者增加了一些额外操作，比如下边这几个方法： 123456789101112131415161718192021222324252627282930313233343536- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:0 progress:nil completed:nil];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:options progress:nil completed:nil];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url completed:(nullable SDExternalCompletionBlock)completedBlock &#123; [self sd_setImageWithURL:url placeholderImage:nil options:0 progress:nil completed:completedBlock];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder completed:(nullable SDExternalCompletionBlock)completedBlock &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:0 progress:nil completed:completedBlock];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options completed:(nullable SDExternalCompletionBlock)completedBlock &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:options progress:nil completed:completedBlock];&#125;- (void)sd_setImageWithPreviousCachedImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock &#123; // 1.取出本地缓存 NSString *key = [[SDWebImageManager sharedManager] cacheKeyForURL:url]; UIImage *lastPreviousCachedImage = [[SDImageCache sharedImageCache] imageFromCacheForKey:key]; // 2.调用本类中的 参数很多的方法，并将缓存数据 (没有时用 placeholder) 传给 placeholder [self sd_setImageWithURL:url placeholderImage:lastPreviousCachedImage ?: placeholder options:options progress:progressBlock completed:completedBlock];&#125; 再来看看这个参数巨多的方法 sd_setImageWithURL: placeholderImage: options: progress: completed: 究竟是如何实现的： 1234567891011121314- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock &#123; // 跳转 UIView+WebCache 中的 sd_internalSetImageWithURL: 方法执行，若 &lt;= 3.x.x 时，sd_internalSetImageWithURL: 在本类内部 [self sd_internalSetImageWithURL:url placeholderImage:placeholder options:options operationKey:nil setImageBlock:nil progress:progressBlock completed:completedBlock];&#125; 还是继续调用别的方法，不过这次是调用父类分类 (UIView+WebCache) 中的方法，之所以要调用直接或间接父类的方法，是为了让其他控件 (如 UIButton 等) 可以复用加载网络图片的方法，他们间的关系如下： 现在去父类的分类 UIView+WebCache 看看吧，为了缩减篇幅，以下代码做了适当精简。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081- (void)sd_internalSetImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options operationKey:(nullable NSString *)operationKey setImageBlock:(nullable SDSetImageBlock)setImageBlock progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock &#123; return [self sd_internalSetImageWithURL:url placeholderImage:placeholder options:options operationKey:operationKey setImageBlock:setImageBlock progress:progressBlock completed:completedBlock context:nil];&#125;// *** 核心方法- (void)sd_internalSetImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options operationKey:(nullable NSString *)operationKey setImageBlock:(nullable SDSetImageBlock)setImageBlock progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock context:(nullable NSDictionary&lt;NSString *, id&gt; *)context &#123; // 1.取消当前 validOperationKey 对应的 loadOperation NSString *validOperationKey = operationKey ?: NSStringFromClass([self class]); [self sd_cancelImageLoadOperationWithKey:validOperationKey]; // 创建 manager（两种：1.用户自定义 2.此库自带的单例） SDWebImageManager *manager; if ([context valueForKey:SDWebImageExternalCustomManagerKey]) &#123; manager = (SDWebImageManager *)[context valueForKey:SDWebImageExternalCustomManagerKey]; &#125; else &#123; manager = [SDWebImageManager sharedManager]; &#125; // 2.下载 id &lt;SDWebImageOperation&gt; operation = [manager loadImageWithURL:url options:options progress:combinedProgressBlock completed:^(UIImage *image, NSData *data, NSError *error, SDImageCacheType cacheType, BOOL finished, NSURL *imageURL) &#123; __strong __typeof (wself) sself = wself; // ... SDWebImageNoParamsBlock callCompletedBlockClojure = ^&#123; if (!sself) &#123; return; &#125; if (!shouldNotSetImage) &#123; [sself sd_setNeedsLayout]; &#125; if (completedBlock &amp;&amp; shouldCallCompletedBlock) &#123; completedBlock(image, error, cacheType, url); &#125; &#125;; // ... dispatch_main_async_safe(^&#123;#if SD_UIKIT || SD_MAC [sself sd_setImage:targetImage imageData:targetData basedOnClassOrViaCustomSetImageBlock:setImageBlock transition:transition cacheType:cacheType imageURL:imageURL];#else [sself sd_setImage:targetImage imageData:targetData basedOnClassOrViaCustomSetImageBlock:setImageBlock];#endif callCompletedBlockClojure(); &#125;); &#125;];// 3.为 UIImageView 绑定新的 operation，即上边这个 operation [self sd_setImageLoadOperation:operation forKey:validOperationKey]; &#125; else &#123; // 如果图片 URL 不存在，执行 completedBlock，返回错误提示 dispatch_main_async_safe(^&#123; if (completedBlock) &#123; NSError *error = [NSError errorWithDomain:SDWebImageErrorDomain code:-1 userInfo:@&#123;NSLocalizedDescriptionKey : @"Trying to load a nil url"&#125;]; completedBlock(nil, error, SDImageCacheTypeNone, url); &#125; &#125;); &#125;&#125; 可以看到，父类最终调用了 - (void)sd_internalSetImageWithURL: ... 这个核心方法，虽然实现代码较多，其实主要就做了这么 3 件事： ① 取消 validOperationKey 对应的已经存在的 loadOperation。因为传进来的参数 validOperationKey 是 nil，所以 validOperationKey 实际取的是当前类名对应的字符串 NSStringFromClass([self class])。 ② 执行 SDWebImageManager 的方法 - (id &lt;SDWebImageOperation&gt;)loadImageWithURL: ... 下载图片，并返回一个 operation 对象，其实是这个类 SDWebImageCombinedOperation 的实例，与上一步 cancel 的对象是同一类型，下面就会用到。 ③ 保存 validOperationKey 与刚刚生成的 operation 之间的映射关系，以备取消时使用（如 ①）。 对于 ② 用到的方法，其实现代码太长，此处代码也做了精简： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556// SDWebImageManager- (id &lt;SDWebImageOperation&gt;)loadImageWithURL:(nullable NSURL *)url options:(SDWebImageOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDInternalCompletionBlock)completedBlock&#123; // ... // 1.创建 operation SDWebImageCombinedOperation *operation = [SDWebImageCombinedOperation new]; operation.manager = self; // 肯定是 weak 属性 __weak typeof(strongOperation) weakSubOperation = strongOperation; // 2.使用 imageCache 的方法查询缓存 operation.cacheOperation = [self.imageCache queryCacheOperationForKey:key options:cacheOptions done:^(UIImage *cachedImage, NSData *cachedData, SDImageCacheType cacheType) &#123;// 以下都是查询结束后 (查到/没查到) 的操作 if (shouldDownload) &#123;// 3. 需要下载 __weak typeof(strongOperation) weakSubOperation = strongOperation; strongOperation.downloadToken = [self.imageDownloader downloadImageWithURL:url options:downloaderOptions progress:progressBlock completed:^(UIImage *downloadedImage, NSData *downloadedData, NSError *error, BOOL finished) &#123; // 缩放图片(如果可以的话) // 缓存图片 // 执行完成的回调 &#125; &#125; else if (cachedImage) &#123;// 4. 如果取到了缓存 // 执行完成的回调 [self callCompletionBlockForOperation:strongOperation completion:completedBlock image:cachedImage data:cachedData error:nil cacheType:cacheType finished:YES url:url]; // 从正在运行的 operation 数组中移除当前 operation [self safelyRemoveOperationFromRunning:strongOperation]; &#125; else &#123;// 5. 没取到缓存 &amp;&amp; 不允许下载 // 执行完成的回调 // Image not in cache and download disallowed by delegate [self callCompletionBlockForOperation:strongOperation completion:completedBlock image:nil data:nil error:nil cacheType:SDImageCacheTypeNone finished:YES url:url]; // 从正在运行的 operation 数组中移除当前 operation [self safelyRemoveOperationFromRunning:strongOperation]; &#125; &#125; return operation;&#125; 该方法主要是在创建 SDWebImageCombinedOperation 对象，大概的操作见上边的代码注释。只对其中 2 个自己认为更重要点的方法做一个简要说明，详细的讨论可以查看后边的相关篇章。 SDImageCache 中查询缓存的方法 - (nullable NSOperation *)queryCacheOperationForKey: ...。这个方法的作用是查询二级缓存，也就依次从内存、磁盘两处缓存查询我们需要的图片数据，前者查不到时，才在后者中查找。 1234// SDImageCache- (nullable NSOperation *)queryCacheOperationForKey:(nullable NSString *)key options:(SDImageCacheOptions)options done:(nullable SDCacheQueryCompletedBlock)doneBlock; SDWebImageDownloader 中下载图片的方法 - (nullable SDWebImageDownloadToken *)downloadImageWithURL: ...。此方法的实现通过 operation 和 operationQueue 配合使用来执行下载操作的，即 先分别创建 operation 和 operationQueue，然后将 operation 添加到 operationQueue 中，就自动启动任务了。 12345// SDWebImageDownloader- (nullable SDWebImageDownloadToken *)downloadImageWithURL:(nullable NSURL *)url options:(SDWebImageDownloaderOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock; 无论是查询缓存还是请求网络数据，最终都会给 imageView 赋值，也就是我们通常希望达到的效果。 四、小结至此，我们大概理了一下 使用 SDWebImage 加载网络图片的主要流程，最后借用 SDWebImage 作者提供的一张时序图做个简单总结吧。 源码注释及 demoHHSDWebImageStudy]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage 源码学习笔记·前传 ☞ 升级 4.x.x 后适配 WebP/GIF]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%C2%B7%E5%89%8D%E4%BC%A0-%E2%98%9E-%E5%8D%87%E7%BA%A7-4-x-x-%E5%90%8E%E9%80%82%E9%85%8D-WebP-GIF%2F</url>
    <content type="text"><![CDATA[一、前言前段时间对项目中使用的 SDWebImage 进行了一次升级 (3.7.6 → 4.4.2)，升级过程中遇到了一些问题，于是又把源码读了一遍，为了记录读码过程，就有了这个系列： SDWebImage 源码学习笔记·前传 ☞ 升级 4.x.x 后适配 WebP/GIF SDWebImage源码学习笔记 ☞ 结构及基本流程 SDWebImage源码学习笔记 ☞ SDWebImageManager SDWebImage源码学习笔记 ☞ SDWebImageDownloader SDWebImage源码学习笔记 ☞ SDImageCache 本篇是第 1 篇，主要记述了升级过程中的遇到的问题及处理方案。 二、升级现在转回到本篇的主要内容，首先当然是升级了，不过最新版的 SDWebImage 里边分了几个子 pod ，默认下载不全。为了满足我们的日常使用要求，还需要加上 WebP 和 GIF 这两个子 pod： 12345platform :ios, '7.0'pod 'SDWebImage', '~&gt; 4.0'pod 'SDWebImage/WebP'pod 'SDWebImage/GIF' 不过，在执行 pod install 的时候，发现 pod &#39;SDWebImage/WebP&#39; 卡在了 Installing libwebp (0.6.0) 的地方。 据说 WebP 的解析库是 Google 的开源库，所以需要使用 VPN。笔者使用了蓝灯 Lantern，然后发现 浏览器可以正常访问 Google 网站，可是命令行 pod install 依然会卡住，可以通过下边两条命令检测命令行是否已添加 VPN：1curl www.google.com 执行上述命令后，如果返回了网页信息，就说明 VPN 已经添加成功。 于是猛查资料，才有了下文： 下载 Lantern 安装后，打开 Lantern，页面如下，查询自己 VPN 软件 (Lantern) 的http代理： 可以看出 Lantern 的 http 代理运行在 52425(选中部分) 端口，不同机器上的端口号也会不同。为什么说是 http 而不是 https 呢？将地址栏中的域名复制出来贴到任意文本编辑器，就会发现变成了 http://localhost:52425/axxx 3.在命令行输入以下指令，查看端口使用情况：1sudo lsof -i -P 可以看出来基本使用的只有 2 个代理端口，52425 和 52427，结合第一步可以得出，52425 是 http 的代理端口，52427 是 https 的代理端口 😎 4.配置命令行代理端口，有两种方式：A、给本地 git 设置 http/https 代理123456789// 添加 http/https 代理的指令 https://127.0.0.1:XXXX (XXXX代表端口号)git config --global http.proxy https://127.0.0.1:52425git config --global https.proxy https://127.0.0.1:52427// 取消 http/https 代理的指令git config --global --unset http.proxy git config --global --unset https.proxy// 这种方法可以 pod install 成功，不过 curl www.google.com 不成功，即打不出 Google 的网页信息。 B、设置系统全局变量 http/https 代理1234567891011121314切换到etc目录下 cd /etc 以管理员身份编辑 sudo vim bashrc 输入密码进入后, G $ 定位到文件末尾 按下键盘字母 i 开始输入，在最下面位置粘贴下面两句export http_proxy=127.0.0.1:52425export https_proxy=127.0.0.1:52427 然后退出保存 按Esc :wq接下来让修改文件生效 source bashrc最后一步 验证成功: curl www.google.com 如果收到 google 的 html 数据代表已通。 完成后 pod 内部的结构如下： 三、使用为叙述方便，此处将图片的 URL 定义成了宏： 1234#define URL_Normal [NSURL URLWithString:@"此处是 普通静态图 地址"]#define URL_WebP_Normal [NSURL URLWithString:@"此处是 静态 WebP 地址"]#define URL_WebP_Dynamic [NSURL URLWithString:@"此处是 动态 WebP 地址"]#define URL_GIF [NSURL URLWithString:@"此处是 GIF 地址"] 第一种方案 png/jpg，直接使用 SDWebImage 提供的普通方法即可。 WebP，只要导入了 WebP 的子 pod，直接使用普通方法即可正常加载静态、动态的 WebP。 GIF，此处使用了 FLAnimatedImageView 代替 UIImageView 来展示 GIF。因为 SDWebImage4.0 以后，如果继续 直接 使用 UIImageView 将只展示 GIF 的第一帧，可以使用推荐的 FLAnimatedImageView 替换 UIImageView。 12345678910111213141516171819202122232425 // 普通静态图 UIImageView *imgV = [[UIImageView alloc] initWithFrame:CGRectMake(125, 70, 160, 160)]; imgV.backgroundColor = [UIColor lightGrayColor]; [self.view addSubview:imgV]; [imgV sd_setImageWithURL:URL_Normal]; // WebP UIImageView *imgVB = [[UIImageView alloc] initWithFrame:CGRectMake(125, 70+160+10, 160, 160)]; imgVB.backgroundColor = [UIColor lightGrayColor]; imgVB.contentMode = UIViewContentModeScaleAspectFill; [self.view addSubview:imgVB]; // WebP 静态图// [imgVB sd_setImageWithURL:URL_WebP_Normal]; // WebP 动态图 [imgVB sd_setImageWithURL:URL_WebP_Dynamic]; // GIF FLAnimatedImageView *imgView = [[FLAnimatedImageView alloc] initWithFrame:CGRectMake(125, 70+160+10+160+10, 160, 200)]; imgView.backgroundColor = [UIColor lightGrayColor]; imgView.contentMode = UIViewContentModeScaleAspectFill; [self.view addSubview:imgView]; [imgView sd_setShowActivityIndicatorView:YES]; [imgView sd_setImageWithURL:URL_GIF placeholderImage:[UIImage imageNamed:@"placeholder"] options:1]; 第二种方案统一都使用 UIImageView 展示图片，不过和上边的第一种方案有以下不同： 使用 SDWebImageManager 的 loadImageWithURL: options: progress: completed: 方法下载图片； 根据得到的 data 判断图片类型: SDImageFormat format = [NSData sd_imageFormatForImageData:data];，基本原理是根据 data 的第一个字节来判断； WebP 使用专用方法 sd_imageWithWebPData: 解码； GIF 也使用专用方法 sd_animatedGIFWithData: 解码。 12345678910111213141516171819202122232425262728UIImageView *gifView = [[UIImageView alloc] initWithFrame:CGRectMake(10, 70+160+10+160+10+200+10, kScreenW-20, 200)]; gifView.backgroundColor = [UIColor lightGrayColor]; [self.view addSubview:gifView]; self.imgView = gifView; SDWebImageManager *mgr = [SDWebImageManager sharedManager]; __weak typeof(self) weakSelf = self; [mgr loadImageWithURL:URL_GIF // URL_WebP_Dynamic // URL_WebP_Normal // URL_Normal options:1 progress:nil completed:^(UIImage * _Nullable image, NSData * _Nullable data, NSError * _Nullable error, SDImageCacheType cacheType, BOOL finished, NSURL * _Nullable imageURL) &#123; SDImageFormat format = [NSData sd_imageFormatForImageData:data]; switch (format) &#123; case SDImageFormatGIF: weakSelf.imgView.image = [UIImage sd_animatedGIFWithData:data]; break; case SDImageFormatWebP: weakSelf.imgView.image = [UIImage sd_imageWithWebPData:data]; break; case SDImageFormatPNG: case SDImageFormatJPEG: weakSelf.imgView.image = image; break; default: break; &#125; &#125;]; 第三种方案自定义 HHImageView，在它的 init 系列方法中添加了对 GIF 的解析： 1[[SDWebImageCodersManager sharedInstance] addCoder:[SDWebImageGIFCoder sharedCoder]]; 这种方案对最终的调用方来说，非常简洁，不需要对各种图片区别对待。 123456789101112 HHImageView *customView = [[HHImageView alloc] initWithFrame:CGRectMake(125, 70, 160, 160)]; customView.backgroundColor = [UIColor lightGrayColor]; [self.view addSubview:customView]; [customView sd_setShowActivityIndicatorView:YES]; // 静态图// [customView sd_setImageWithURL:URL_Normal]; // WebP 动图// [customView sd_setImageWithURL:URL_WebP_Normal]; // WebP 静态图// [customView sd_setImageWithURL:URL_WebP_Dynamic]; // GIF [customView sd_setImageWithURL:URL_GIF]; 本文完整示例代码见: HHSDWebImageStudy 参考 Mac 命令行（终端）设置Lantern vpn代理 iOS SDWebImage加载webP]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[MBProgressHUD 源码学习笔记]]></title>
    <url>%2F2018%2F12%2F16%2FMBProgressHUD-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[1.前言MBProgressHUD 是 iOS 开发中经常会用到的一个加载动画库，本文就来简单学习一下源码。 2.视图层级在开始学习源码之前，先大概了解一下整个视图的层级结构吧，主要分这几个视图： backgroundView，位于最底层，是一个遮罩层，在 HUD 显示时，我们之所以无法点击后边的视图，都是因为它。 bezelView，位于 backgroundView 之上，承载着下边要说的几个视图，下边 4 个视图位于同一层级。 indicator，是 bezelView 的子视图，类型不定，他自己提供了 2 种类型 MBRoundProgressView 和 MBBarProgressView，不过也可以使用用户自定义视图 customView。 label，indicator 下方的提示标签。 detailLabel，label 下方的补充提示标签。 button，最底部的按钮，可以响应点击事件。 当然，以上这些视图不一定要全部显示，可以由用户（我们自己 O(∩_∩)O ）自由选择。 3.源码言归正传，现在开始读源码。 3.1 类结构虽然 MBProgressHUD 的文件数量非常少，只有 2 个：MBProgressHUD.h 和 MBProgressHUD.m，但还有几个相关的类和协议： 3.2 MBProgressHUD我们主要研究主类 MBProgressHUD，首先看几个重要的属性： 123456@property (assign, nonatomic) NSTimeInterval graceTime; // 显示的宽限时间，即从显示方法被调用到真正显示之间的时间差@property (assign, nonatomic) NSTimeInterval minShowTime; // HUD 最小的展示时间 @property (nonatomic, weak) NSTimer *graceTimer; // 延迟显示的 timer@property (nonatomic, weak) NSTimer *minShowTimer; // 保证 HUD 最短显示维持时间 的timer@property (nonatomic, weak) NSTimer *hideDelayTimer; // 延迟隐藏的 timer 接下来看看 MBProgressHUD 为我们提供的 3 个类方法用于展示和隐藏 HUD 的方法： 123456// 展示 HUD+ (instancetype)showHUDAddedTo:(UIView *)view animated:(BOOL)animated;// 隐藏 HUD+ (BOOL)hideHUDForView:(UIView *)view animated:(BOOL)animated;// 返回最顶层未结束的 HUD+ (nullable MBProgressHUD *)HUDForView:(UIView *)view; 我们从展示的方法实现开始探究，实现代码如下： 1234567+ (instancetype)showHUDAddedTo:(UIView *)view animated:(BOOL)animated &#123; MBProgressHUD *hud = [[self alloc] initWithView:view]; hud.removeFromSuperViewOnHide = YES; [view addSubview:hud]; [hud showAnimated:animated]; return hud;&#125; 我们发现，在 initWithView: 方法中对入参 view 做了空判断，这里使用了 NSAsset，如果为空，会报错： 1234- (id)initWithView:(UIView *)view &#123; NSAssert(view, @"View must not be nil."); return [self initWithFrame:view.bounds];&#125; 接着看 showAnimated: 的方法实现。 123456789101112131415161718192021222324252627282930313233- (void)showAnimated:(BOOL)animated &#123; // 1.确保在主线程执行 MBMainThreadAssert(); // 2.关闭之前可能存在的 minShowTimer [self.minShowTimer invalidate]; self.useAnimation = animated; self.finished = NO; // 3.是否延缓 HUD 的展示 // 如果设置了 graceTime，则延缓 HUD 的展示 if (self.graceTime &gt; 0.0) &#123; NSTimer *timer = [NSTimer timerWithTimeInterval:self.graceTime target:self selector:@selector(handleGraceTimer:) userInfo:nil repeats:NO]; [[NSRunLoop currentRunLoop] addTimer:timer forMode:NSRunLoopCommonModes]; self.graceTimer = timer; &#125; else &#123; // 如果没设置 graceTime，则立即展示 HUD [self showUsingAnimation:self.useAnimation]; &#125;&#125;// 定时器 graceTimer 的响应方法- (void)handleGraceTimer:(NSTimer *)theTimer &#123; // 只有当任务还在进行的时候才会显示 HUD，否则什么也不做 if (!self.hasFinished) &#123; [self showUsingAnimation:self.useAnimation]; &#125;&#125; 此处做了 3 件事： ① 首先，为了确保在主线程执行，能够及时更新 UI ，这里使用了 NSAsset，如果当前线程不是主线程，就会报错，MBMainThreadAssert() 的定义如下。 1#define MBMainThreadAssert() NSAssert([NSThread isMainThread], @"MBProgressHUD needs to be accessed on the main thread."); ② 然后，移除了可能存在的 minShowTimer。 ③ 最后，根据 graceTime 有无值来决定是否需要延缓 HUD 的显示，如果有值，则启动 graceTimer，待时间到时再执行显示的操作；如果无值，则直接去显示 HUD。 graceTime 应该是针对耗时比较少的操作准备的，定时器 graceTimer 时间到的时候，操作有可能已经完成了 (self.hasFinished == YES)，这时就不需要展示 HUD 了，以免影响用户体验。 另外，minShowTime 是在隐藏的时候使用的，通过 minShowTimer 保证 HUD 展示时间 &gt;= minShowTime 从而避免 HUD 显示时间过短的问题，也是为了提升用户体验。 做完 ③ 的判断就该去显示了，即 showUsingAnimation: 的实现，它主要也做了 3 件事： 123456789101112131415161718192021- (void)showUsingAnimation:(BOOL)animated &#123; [self.bezelView.layer removeAllAnimations]; [self.backgroundView.layer removeAllAnimations]; [self.hideDelayTimer invalidate]; self.showStarted = [NSDate date]; self.alpha = 1.f; [self setNSProgressDisplayLinkEnabled:YES]; if (animated) &#123; [self animateIn:YES withType:self.animationType completion:NULL]; &#125; else &#123;#pragma clang diagnostic push#pragma clang diagnostic ignored "-Wdeprecated-declarations" self.bezelView.alpha = self.opacity;#pragma clang diagnostic pop self.backgroundView.alpha = 1.f; &#125;&#125; ① 取消之前可能存在的 animations 并停止 ‘延缓隐藏’ 的 timer。 ② 根据是否需要展示进度做相应的处理。此处传的是 YES，即需要展示：创建一个 CADisplayLink对象并启动（启动的代码写在了 progressObjectDisplayLink 的 setter 里边），在响应方法里将外界传入的 progressObject（NSProgress）的值 progressObject.fractionCompleted 赋给 progress（CGFloat），然后在 progress 的 setter 里更新控件的值。 12345678910111213141516171819202122232425262728293031323334353637// 创建 CADisplayLink 对象，用于更新- (void)setNSProgressDisplayLinkEnabled:(BOOL)enabled &#123; if (enabled &amp;&amp; self.progressObject) &#123; if (!self.progressObjectDisplayLink) &#123; self.progressObjectDisplayLink = [CADisplayLink displayLinkWithTarget:self selector:@selector(updateProgressFromProgressObject)]; &#125; &#125; else &#123; self.progressObjectDisplayLink = nil; &#125;&#125;// 为 progressObjectDisplayLink 赋值，并启动 CADisplayLink- (void)setProgressObjectDisplayLink:(CADisplayLink *)progressObjectDisplayLink &#123; if (progressObjectDisplayLink != _progressObjectDisplayLink) &#123; [_progressObjectDisplayLink invalidate]; _progressObjectDisplayLink = progressObjectDisplayLink; [_progressObjectDisplayLink addToRunLoop:[NSRunLoop mainRunLoop] forMode:NSDefaultRunLoopMode]; &#125;&#125;// 给 progress 赋值- (void)updateProgressFromProgressObject &#123; self.progress = self.progressObject.fractionCompleted;&#125;// progress 的 setter，并给展示进度的控件赋值- (void)setProgress:(float)progress &#123; if (progress != _progress) &#123; _progress = progress; UIView *indicator = self.indicator; if ([indicator respondsToSelector:@selector(setProgress:)]) &#123; [(id)indicator setValue:@(self.progress) forKey:@"progress"]; &#125; &#125;&#125; 外界给 _progressObject 赋值的方法实现如下。这里加了 if (progressObject != _progressObject) 的判断，避免了重复赋值。 123456- (void)setProgressObject:(NSProgress *)progressObject &#123; if (progressObject != _progressObject) &#123; _progressObject = progressObject; [self setNSProgressDisplayLinkEnabled:YES]; &#125;&#125; ③ 如果需要过渡动画，无论是 show 还是 hide 都会调用 animateIn: withType: completion: 这个方法，代码实现如下，详见注释。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061- (void)animateIn:(BOOL)animatingIn withType:(MBProgressHUDAnimation)type completion:(void(^)(BOOL finished))completion &#123; // 确定缩放动画的类型 if (type == MBProgressHUDAnimationZoom) &#123; type = animatingIn ? MBProgressHUDAnimationZoomIn : MBProgressHUDAnimationZoomOut; &#125; CGAffineTransform small = CGAffineTransformMakeScale(0.5f, 0.5f); // x、y 方向的缩放倍数均为 0.5 CGAffineTransform large = CGAffineTransformMakeScale(1.5f, 1.5f); // x、y 方向的缩放倍数均为 1.5 UIView *bezelView = self.bezelView;// * 设置初始状态的值（show 的 过渡动画 的初值，hide 的初值就不用设置了） if (animatingIn &amp;&amp; bezelView.alpha == 0.f &amp;&amp; type == MBProgressHUDAnimationZoomIn) &#123; bezelView.transform = small; &#125; else if (animatingIn &amp;&amp; bezelView.alpha == 0.f &amp;&amp; type == MBProgressHUDAnimationZoomOut) &#123; bezelView.transform = large; &#125; // 执行动画的 block，作为后边方法的参数 dispatch_block_t animations = ^&#123;// * show 的过渡动画 的 终值 if (animatingIn) &#123; bezelView.transform = CGAffineTransformIdentity;// * 下边 2 个 if 均为 hide 的过渡动画 的 终值 &#125; else if (!animatingIn &amp;&amp; type == MBProgressHUDAnimationZoomIn) &#123; bezelView.transform = large; &#125; else if (!animatingIn &amp;&amp; type == MBProgressHUDAnimationZoomOut) &#123; bezelView.transform = small; &#125;#pragma clang diagnostic push#pragma clang diagnostic ignored "-Wdeprecated-declarations" bezelView.alpha = animatingIn ? self.opacity : 0.f;#pragma clang diagnostic pop self.backgroundView.alpha = animatingIn ? 1.f : 0.f; &#125;; // 此方法 iOS 7.0 之后才支持#if __IPHONE_OS_VERSION_MAX_ALLOWED &gt;= 70000 || TARGET_OS_TV if (kCFCoreFoundationVersionNumber &gt;= kCFCoreFoundationVersionNumber_iOS_7_0) &#123; [UIView animateWithDuration:0.3 delay:0. usingSpringWithDamping:1.f initialSpringVelocity:0.f options:UIViewAnimationOptionBeginFromCurrentState animations:animations completion:completion]; return; &#125;#endif // iOS 4.0 就开始支持 [UIView animateWithDuration:0.3 delay:0. options:UIViewAnimationOptionBeginFromCurrentState animations:animations completion:completion];&#125; 隐藏的逻辑与此类似，可自行参看代码，这里简单绘制了一张方法调用流程图作为小结，如下所示：]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>MBProgressHUD</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello World]]></title>
    <url>%2F2018%2F12%2F15%2Fhello-world%2F</url>
    <content type="text"><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub. Quick StartCreate a new post1$ hexo new "My New Post" More info: Writing Run server1$ hexo server More info: Server Generate static files1$ hexo generate More info: Generating Deploy to remote sites1$ hexo deploy More info: Deployment]]></content>
  </entry>
</search>
