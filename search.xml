<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[MJRefresh 源码学习笔记]]></title>
    <url>%2F2019%2F01%2F04%2FMJRefresh-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[1.前言MJRefresh 是日常 iOS 开发中使用频率比较高的一款下拉刷新/上拉加载更多的第三方控件，平时似乎没有完整查看过源码，此处就记录一下探究源码的过程吧。 2.使用示例官方给的 Example 里边提供了很多种刷新样式 ，本文我们只以其中 2 种样式（UITableView + 下拉刷新 动画图片 和 UITableView + 上拉刷新动画图片 ）为例展开讨论。 示例1：UITableView + 下拉刷新 动画图片1234567891011121314151617181920- (void)exampleA&#123; // 1.设置 header self.tableView.mj_header = [MJChiBaoZiHeader headerWithRefreshingTarget:self refreshingAction:@selector(loadNewData)]; // 2.马上进入刷新状态 [self.tableView.mj_header beginRefreshing];&#125;- (void)loadNewData &#123; // 3.下载数据的操作 __weak typeof(self) weakSelf = self; [self requestNetDataWithCompletionBlock:^(NSArray *result, BOOL isSuccess) &#123; // 处理返回的数据（略） // 4.刷新表格，并结束刷新状态 [weakSelf.tableView reloadData]; // 5.拿到当前的下拉刷新控件， [weakSelf.tableView.mj_header endRefreshing]; &#125;];&#125; 如上边注释所述，大概分 5 个步骤，其中 1、2、4、5 都是 MJRefresh 的相关操作。 示例2：UITableView + 上拉刷新 动画图片123456789101112131415161718- (void)exampleB&#123; // 1.设置 footer self.tableView.mj_footer = [MJChiBaoZiFooter footerWithRefreshingTarget:self refreshingAction:@selector(loadMoreData)];&#125;- (void)loadNewData &#123; // 2.下载数据的操作 __weak typeof(self) weakSelf = self; [self requestNetDataWithCompletionBlock:^(NSArray *result, BOOL isSuccess) &#123; // 将返回数据追加到表格的数据源（略） // 3.刷新表格，并结束刷新状态 [weakSelf.tableView reloadData]; // 4.拿到当前的上拉加载更多控件， [weakSelf.tableView.mj_footer endRefreshing]; &#125;&#125; 与下拉类似，这里的 1、3、4 是 MJRefresh 的相关操作。 3. 整体思路在开始分析源码之前，我觉得应该先大概了解一下这个库的基本实现思路，这样看源码的时候才不至于晕头转向，不知所云。下面以下拉刷新为例，做一个简单介绍，先看下图。 首先，当我们给 tableView.mj_header 赋值时，实际在 tableView 上添加一个子视图即刷新控件，但是并不是添加到 tableView 的 header 里边，因此就不会占用 tableView 的 header。 然后，对 tableView 进行监听 (KVO)，当 tableView 的 contentOffset 发生变化时，刷新控件会截获到这个时机，根据 contentOffset 的 y 值更新刷新控件的显示及 tableView 的 contentInset.top，整个流程见上图。下面说说这张图吧 O(∩_∩)O： ① 刚把刷新控件加到 tableView 上的时候，设置刷新控件的 y 值为自身高度的负值，此时改控件会被导航挡住，当然也可以再设置其透明度为0。 ② 下拉 tableView，当刷新控件完全显示出来（临界点）之前，是一种状态，此时松手的话，会直接弹回去。 ③ 过了临界点，再往下拉的时候，更新控件的显示，此时松手的话就开始刷新。 ④ 放手刷新的时候，控件会回弹，可以加动画，不至于那么生硬。同时执行调用方传入的 block，一般是请求网络数据的操作。 ⑤ 刷新过程中，要显示该刷新控件，即不让其弹回到导航后边，就给 tableView.contentInset.top 增加一个控件的高度，当然是负值。 ⑥ 当调用方请求完数据后，手动调用 刷新控件的 endRefreshing 方法，在这个方法类里边更新控件 UI 至初始状态，并将 tableView.contentInset.top 减少一个控件的高度，当然也是负值，至此，刷新结束。 以上就基本实现逻辑，下面开始看源码吧。 4. 源码分析下面分别探究一下下拉刷新和上拉加载更多的源码实现。 通过 示例1 和 示例2 可以推测，这个框架可以大概分 2 部分，一部分是刷新控件的载体 (UIScrollView及其子类，即 tableView 和 collectionView)，另一部分就是刷新控件本身，也就是所谓的 header 和 footer。 4.1 刷新控件的载体载体主要集中在下边这几个分类里边 123UIScrollView+MJExtensionUIScrollView+MJRefreshUIView+MJExtension UIView+MJExtensionUIView+MJExtension 只是为公共基类 UIView 的 frame 提供了便捷的访问方式，包括刷新控件也会用到。 UIScrollView+MJRefreshUIScrollView+MJRefresh 是列表基类的分类，这个文件里边实际包含了 3 个分类，依次为 ① NSObject (MJRefresh)：分别提供了交换类方法和交换实例方法的工具方法： 12345678910/// 交换实例方法+ (void)exchangeInstanceMethod1:(SEL)method1 method2:(SEL)method2&#123; method_exchangeImplementations(class_getInstanceMethod(self, method1), class_getInstanceMethod(self, method2));&#125;/// 交换类方法+ (void)exchangeClassMethod1:(SEL)method1 method2:(SEL)method2&#123; method_exchangeImplementations(class_getClassMethod(self, method1), class_getClassMethod(self, method2));&#125; ② UIScrollView (MJRefresh)：依次为基类 UIScrollView 添加了 header、footer 和 mj_reloadDataBlock 这三个属性，并利用关联对象添加了对应的 setter 和 getter 实现，关于在既有类中使用关联对象存放自定义数据的方法，可以查阅《Effective Objective-C 2.0》中第 10 条的介绍。 其中，在 mj_reloadDataBlock 的 setter 中设置关联对象前后分别添加了 willChangeValueForKey: 和 didChangeValueForKey: 这 2 个方法，意在可以添加 KVO 监听。 123456- (void)setMj_reloadDataBlock:(void (^)(NSInteger))mj_reloadDataBlock&#123; [self willChangeValueForKey:@"mj_reloadDataBlock"]; // KVO objc_setAssociatedObject(self, &amp;MJRefreshReloadDataBlockKey, mj_reloadDataBlock, OBJC_ASSOCIATION_COPY_NONATOMIC); [self didChangeValueForKey:@"mj_reloadDataBlock"]; // KVO&#125; 然后提供了一个执行 mj_reloadDataBlock 的方法 executeReloadDataBlock:1234- (void)executeReloadDataBlock&#123; !self.mj_reloadDataBlock ? : self.mj_reloadDataBlock(self.mj_totalDataCount);&#125; 即如果设置了 mj_reloadDataBlock ，就在此执行这个 block，我们注意到这个参数 mj_totalDataCount，点开后发现，原来它指的是 UITableView 或 UICollectionView 的总行数。 123456789101112131415161718- (NSInteger)mj_totalDataCount&#123; NSInteger totalCount = 0; if ([self isKindOfClass:[UITableView class]]) &#123; UITableView *tableView = (UITableView *)self; for (NSInteger section = 0; section&lt;tableView.numberOfSections; section++) &#123; totalCount += [tableView numberOfRowsInSection:section]; &#125; &#125; else if ([self isKindOfClass:[UICollectionView class]]) &#123; UICollectionView *collectionView = (UICollectionView *)self; for (NSInteger section = 0; section&lt;collectionView.numberOfSections; section++) &#123; totalCount += [collectionView numberOfItemsInSection:section]; &#125; &#125; return totalCount;&#125; ③④ UITableView (MJRefresh) 和 UICollectionView (MJRefresh)，他们都提供了下边两个方法： 12345678910+ (void)load&#123; [self exchangeInstanceMethod1:@selector(reloadData) method2:@selector(mj_reloadData)];&#125;- (void)mj_reloadData&#123; [self mj_reloadData]; [self executeReloadDataBlock];&#125; 也就是说，在程序启动时执行 load 方法时将列表的 reloadData 方法与自定义的 mj_reloadData 方法交换，在新方法中增加了一步操作 [self executeReloadDataBlock];，即执行上文提到的 mj_reloadDataBlock，这样，当我们执行 tableView 的 reloadData 方法时，实际执行的就是 mj_reloadData 这个方法了。 那么这个 block 是什么时候设置的呢，全局搜索了一下，发现只有在 MJRefreshFooter 的 willMoveToSuperview: 方法中设置过，也就是将 footer 添加到 tableView 上的时候。 12345678910111213141516- (void)willMoveToSuperview:(UIView *)newSuperview&#123; [super willMoveToSuperview:newSuperview]; // 当前视图被添加到父视图上时，设置后边的 block，即当列表（UICollectionView 或 UITableView）行数为 0 时，隐藏当前视图。 if (newSuperview) &#123; // 监听scrollView数据的变化 if ([self.scrollView isKindOfClass:[UITableView class]] || [self.scrollView isKindOfClass:[UICollectionView class]]) &#123; [self.scrollView setMj_reloadDataBlock:^(NSInteger totalDataCount) &#123; if (self.isAutomaticallyHidden) &#123; self.hidden = (totalDataCount == 0); &#125; &#125;]; &#125; &#125;&#125; willMoveToSuperview: 是将视图添加到父视图或从父视图中移除时调用的，if (newSuperview) { ... } 说明是将 footer 添加到父视图上的时候设置这个 block 的，block 的具体实现是：如果需要自动隐藏，则当数据的总条数为 0 时，隐藏 footer，否则展示。 UIScrollView+MJExtensionUIScrollView+MJExtension 是关于下边几个属性的便捷访问方式： 123contentInset / adjustedContentInsetcontentOffsetcontentSize 其中，adjustedContentInset 是 iOS 11 新引入的一个 属性，在 iOS 11 中决定 tableView 的内容与边缘距离的是 adjustedContentInset 属性，而不是 contentInset。 4.2下拉刷新控件（refreshHeader）先来看一张图： 上图就是 header 的继承关系，示例中的 MJChiBaoZiHeader 就是继承自 MJRefreshGifHeader。为了描述更有条理，我们从基类开始讨论吧。 MJRefreshComponentMJRefreshComponent 是所有 header 和 footer 的基类，这里定义了表示刷新状态的枚举 MJRefreshState 和 3 种不同的回调。 1234567891011121314151617181920/** 刷新控件的状态 */typedef NS_ENUM(NSInteger, MJRefreshState) &#123; /** 普通闲置状态 */ MJRefreshStateIdle = 1, /** 松开就可以进行刷新的状态 */ MJRefreshStatePulling, /** 正在刷新中的状态 */ MJRefreshStateRefreshing, /** 即将刷新的状态 */ MJRefreshStateWillRefresh, /** 所有数据加载完毕，没有更多的数据了 */ MJRefreshStateNoMoreData&#125;;/** 进入刷新状态的回调 */typedef void (^MJRefreshComponentRefreshingBlock)(void);/** 开始刷新后的回调(进入刷新状态后的回调) */typedef void (^MJRefreshComponentbeginRefreshingCompletionBlock)(void);/** 结束刷新后的回调 */typedef void (^MJRefreshComponentEndRefreshingCompletionBlock)(void); 实际上这个类的文件里有两个类，除了自己之外还有一个 UILabel 的分类，提供了一个创建定制好的 Label 的类方法 mj_label 和获取文本宽度的实例方法 mj_textWith。 123456789101112131415161718192021222324252627282930+ (instancetype)mj_label&#123; UILabel *label = [[self alloc] init]; label.font = MJRefreshLabelFont; label.textColor = MJRefreshLabelTextColor; label.autoresizingMask = UIViewAutoresizingFlexibleWidth; label.textAlignment = NSTextAlignmentCenter; label.backgroundColor = [UIColor clearColor]; return label;&#125;- (CGFloat)mj_textWith &#123; CGFloat stringWidth = 0; CGSize size = CGSizeMake(MAXFLOAT, MAXFLOAT); if (self.text.length &gt; 0) &#123;#if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) &amp;&amp; __IPHONE_OS_VERSION_MAX_ALLOWED &gt;= 70000 stringWidth =[self.text boundingRectWithSize:size options:NSStringDrawingUsesLineFragmentOrigin attributes:@&#123;NSFontAttributeName:self.font&#125; context:nil].size.width;#else stringWidth = [self.text sizeWithFont:self.font constrainedToSize:size lineBreakMode:NSLineBreakByCharWrapping].width;#endif &#125; return stringWidth;&#125; 下面来看看 MJRefreshComponent 这个类吧，依照惯例，从初始化方法开始: 123456789101112131415161718- (instancetype)initWithFrame:(CGRect)frame&#123; if (self = [super initWithFrame:frame]) &#123; // 准备工作 [self prepare]; // 默认是普通状态 self.state = MJRefreshStateIdle; &#125; return self;&#125;- (void)prepare&#123; // 基本属性 self.autoresizingMask = UIViewAutoresizingFlexibleWidth; self.backgroundColor = [UIColor clearColor];&#125; 初始化了几个变量： ①初始状态设置为普通状态，既没有触发刷新的情况； ②prepare方法中设置了两个基本属性，backgroundColor 和 autoresizingMask，autoresizingMask 的初值 UIViewAutoresizingFlexibleWidth 指的是：当父视图的 bounds 改变时，子视图 (即当前视图) 自动调整宽度，以保证左、右边距不变。 关于布局，这里重写了 layoutSubviews 方法，在调用 super 的方法之前增加了一步操作 placeSubviews，这个方法需要子类来实现。 12345678910- (void)layoutSubviews&#123; [self placeSubviews]; [super layoutSubviews];&#125;- (void)placeSubviews &#123;&#125; 重写了 willMoveToSuperview: 这个方法用于将当前视图添加到父视图或从父视图中移除时添加一些额外操作。 123456789101112131415161718192021222324252627- (void)willMoveToSuperview:(UIView *)newSuperview&#123; [super willMoveToSuperview:newSuperview]; // 如果不是UIScrollView，不做任何事情 if (newSuperview &amp;&amp; ![newSuperview isKindOfClass:[UIScrollView class]]) return; // 旧的父控件移除监听 [self removeObservers]; if (newSuperview) &#123; // 新的父控件 // 设置宽度 self.mj_w = newSuperview.mj_w; // 设置位置 self.mj_x = -_scrollView.mj_insetL; // 记录UIScrollView _scrollView = (UIScrollView *)newSuperview; // 设置永远支持垂直弹簧效果 _scrollView.alwaysBounceVertical = YES; // 记录UIScrollView最开始的contentInset _scrollViewOriginalInset = _scrollView.mj_inset; // 添加监听 [self addObservers]; &#125;&#125; 首先对 newSuperview 做了一层过滤，只有是 UIScrollView 及其子类才可以继续往下走。 然后，先将旧的监听移除。 如果是移除当前视图的操作，则会跳过下边的 if 代码，结束这个方法的执行。如果是将当前视图添加父视图上，即父视图 newSuperview 存在时，保存一些值，最后添加新的监听。 下面看看添加、移除监听的这波操作： 12345678910111213141516- (void)addObservers&#123; NSKeyValueObservingOptions options = NSKeyValueObservingOptionNew | NSKeyValueObservingOptionOld; [self.scrollView addObserver:self forKeyPath:MJRefreshKeyPathContentOffset options:options context:nil]; [self.scrollView addObserver:self forKeyPath:MJRefreshKeyPathContentSize options:options context:nil]; self.pan = self.scrollView.panGestureRecognizer; [self.pan addObserver:self forKeyPath:MJRefreshKeyPathPanState options:options context:nil];&#125;- (void)removeObservers&#123; [self.superview removeObserver:self forKeyPath:MJRefreshKeyPathContentOffset]; [self.superview removeObserver:self forKeyPath:MJRefreshKeyPathContentSize]; [self.pan removeObserver:self forKeyPath:MJRefreshKeyPathPanState]; self.pan = nil;&#125; 监听主要是针对 self.ScrollView 即父视图的 contentOffset、contentSize 和 父视图的 panGestureRecognizer 的 state。 12345678910111213141516171819- (void)observeValueForKeyPath:(NSString *)keyPath ofObject:(id)object change:(NSDictionary *)change context:(void *)context&#123; // 遇到这些情况就直接返回：不能交互 if (!self.userInteractionEnabled) return; // 这个就算看不见也需要处理 if ([keyPath isEqualToString:MJRefreshKeyPathContentSize]) &#123; [self scrollViewContentSizeDidChange:change]; &#125; // 看不见 if (self.hidden) return; if ([keyPath isEqualToString:MJRefreshKeyPathContentOffset]) &#123; [self scrollViewContentOffsetDidChange:change]; &#125; else if ([keyPath isEqualToString:MJRefreshKeyPathPanState]) &#123; [self scrollViewPanStateDidChange:change]; &#125;&#125; 当监听到变换时，会分别触发对应的处理方法（下边 3 个），其中 scrollViewContentOffsetDidChange: 在下拉刷新和上拉加载更多时都会用到，后边两个方法只在上拉加载更多时会用到。 123- (void)scrollViewContentOffsetDidChange:(NSDictionary *)change&#123;&#125;- (void)scrollViewContentSizeDidChange:(NSDictionary *)change&#123;&#125;- (void)scrollViewPanStateDidChange:(NSDictionary *)change&#123;&#125; 接下来是一批公共方法： ① 设置回调对象和回调方法，提供了一种内部的响应方式，即使用 target-Action 的方式，执行刷新回调 executeRefreshingCallback 时候用到，见下边的内部方法。 123456789101112131415161718192021222324252627- (void)setRefreshingTarget:(id)target refreshingAction:(SEL)action&#123; self.refreshingTarget = target; self.refreshingAction = action;&#125;#pragma mark - 内部方法- (void)executeRefreshingCallback&#123; MJRefreshDispatchAsyncOnMainQueue(&#123; if (self.refreshingBlock) &#123; self.refreshingBlock(); &#125; // #define MJRefreshMsgSend(...) ((void (*)(void *, SEL, UIView *))objc_msgSend)(__VA_ARGS__) // #define MJRefreshMsgTarget(target) (__bridge void *)(target) if ([self.refreshingTarget respondsToSelector:self.refreshingAction]) &#123; MJRefreshMsgSend(MJRefreshMsgTarget(self.refreshingTarget), self.refreshingAction, self); &#125; if (self.beginRefreshingCompletionBlock) &#123; self.beginRefreshingCompletionBlock(); &#125; &#125;)&#125; 接下来是一个很重要的 setter，子类可以重写该方法，在状态方法改变的时候，及时更新刷新控件。 1234567- (void)setState:(MJRefreshState)state&#123; _state = state; // 加入主队列的目的是等setState:方法调用完毕、设置完文字后再去布局子控件 MJRefreshDispatchAsyncOnMainQueue([self setNeedsLayout];)&#125; ② 进入刷新及结束刷新的方法，每种情况分别提供了一个带 block 和一个不带 block 的方法，后者保存了 block 之后，又调用了前者，这个 block 的作用是用来添加刷新结束后的附加操作的。 1234567891011121314151617181920212223242526272829303132333435363738394041#pragma mark 进入刷新状态- (void)beginRefreshing&#123; [UIView animateWithDuration:MJRefreshFastAnimationDuration animations:^&#123; self.alpha = 1.0; &#125;]; self.pullingPercent = 1.0; // 只要正在刷新，就完全显示 if (self.window) &#123; self.state = MJRefreshStateRefreshing; &#125; else &#123; // 预防正在刷新中时，调用本方法使得header inset回置失败 if (self.state != MJRefreshStateRefreshing) &#123; self.state = MJRefreshStateWillRefresh; // 刷新(预防从另一个控制器回到这个控制器的情况，回来要重新刷新一下) [self setNeedsDisplay]; &#125; &#125;&#125;- (void)beginRefreshingWithCompletionBlock:(void (^)(void))completionBlock&#123; self.beginRefreshingCompletionBlock = completionBlock; [self beginRefreshing];&#125;#pragma mark 结束刷新状态- (void)endRefreshing&#123; MJRefreshDispatchAsyncOnMainQueue(self.state = MJRefreshStateIdle;)&#125;- (void)endRefreshingWithCompletionBlock:(void (^)(void))completionBlock&#123; self.endRefreshingCompletionBlock = completionBlock; [self endRefreshing];&#125; ③ 最后是根据拖拽进度自动改变透明度的相关方法，即如果需要自动改变透明度，则会在拖拽过程中，将拖拽进度时时赋值给 self.alpha。 123456789101112131415161718192021222324252627282930313233343536373839#pragma mark 自动切换透明度- (void)setAutoChangeAlpha:(BOOL)autoChangeAlpha&#123; self.automaticallyChangeAlpha = autoChangeAlpha;&#125;- (BOOL)isAutoChangeAlpha&#123; return self.isAutomaticallyChangeAlpha;&#125;- (void)setAutomaticallyChangeAlpha:(BOOL)automaticallyChangeAlpha&#123; _automaticallyChangeAlpha = automaticallyChangeAlpha; if (self.isRefreshing) return; if (automaticallyChangeAlpha) &#123; self.alpha = self.pullingPercent; &#125; else &#123; self.alpha = 1.0; &#125;&#125;#pragma mark 根据拖拽进度设置透明度- (void)setPullingPercent:(CGFloat)pullingPercent&#123; _pullingPercent = pullingPercent; // 不是正在刷新的状态，而且要求自动改变透明度时，将 pullingPercent 的值给 alpha，否则不再往下执行。 if (self.isRefreshing) return; if (self.isAutomaticallyChangeAlpha) &#123; self.alpha = pullingPercent; &#125;&#125; MJRefreshHeaderMJRefreshHeader 是 MJRefreshComponent 的子类，但还不是最终可以使用的类，还在为其子类做准备。先来看看它提供的两个构造方法，在创建实例对象的同时，保存了响应的回调，一个采用 block，另一个采用 target-action 的方式。 123456789101112131415#pragma mark - 构造方法+ (instancetype)headerWithRefreshingBlock:(MJRefreshComponentRefreshingBlock)refreshingBlock&#123; MJRefreshHeader *cmp = [[self alloc] init]; cmp.refreshingBlock = refreshingBlock; return cmp;&#125;+ (instancetype)headerWithRefreshingTarget:(id)target refreshingAction:(SEL)action&#123; MJRefreshHeader *cmp = [[self alloc] init]; [cmp setRefreshingTarget:target refreshingAction:action]; return cmp;&#125; 下边是重写父类的方法，首先在 prepare 和 方法中设置存取刷新时刻用的 key 、自身高度 mj_h 及 自身的 y 坐标 mj_y。这里出现了一个 ignoredScrollViewContentInsetTop，推测是一个预留的 refreshHeader 和 tableView 之间的间隙值，默认为 0，需要用户设置才会有值。 123456789101112131415161718- (void)prepare&#123; [super prepare]; // 设置key self.lastUpdatedTimeKey = MJRefreshHeaderLastUpdatedTimeKey; // 设置高度 self.mj_h = MJRefreshHeaderHeight;&#125;- (void)placeSubviews&#123; [super placeSubviews]; // 设置y值(当自己的高度发生改变了，肯定要重新调整Y值，所以放到placeSubviews方法中设置y值) self.mj_y = - self.mj_h - self.ignoredScrollViewContentInsetTop;&#125; 然后是 2 个重要的方法，scrollViewContentOffsetDidChange: 和 setState:。 scrollViewContentOffsetDidChange: 方法主要是根据当前状态 (self.state) 和 contentOffset 更新 self.state，之所以要考虑当前状态，是为了避免频繁的更新 state 的值，详见代码注释。 setState: 方法针对 进入刷新状态 和 从刷新恢复正常状态 分别进行处理，前者话，将 scrollView 的 contentInset.top 加上一个 refreshHeader 的高度，对于后者，又需要将之前加上的高度减掉，以此来控制刷新控件的悬浮状态。另外， 从刷新恢复正常状态 时，保存了当前时刻，这个是为了显示上一次刷新时间用的。 最后是 2 个公共方法，一个用来获取保存在本地的上次刷新时间，另一个是 ignoredScrollViewContentInsetTop 的setter，同时更新了刷新控件的 y 值。 MJRefreshStateHeaderMJRefreshStateHeader 也属于这个继承体系中的一员，继承自 MJRefreshHeader，这里开始就到实用阶段了： 在 prepare 方法中将三种状态对应的标签保存到一个可变字典（stateTitles）中，以备后边展示。 1234567891011121314151617181920- (void)prepare &#123; [super prepare]; // 初始化间距 self.labelLeftInset = MJRefreshLabelLeftInset; // 初始化文字 // 初始未触发刷新的状态 [self setTitle:[NSBundle mj_localizedStringForKey:MJRefreshHeaderIdleText] forState:MJRefreshStateIdle]; // 拖拽状态 [self setTitle:[NSBundle mj_localizedStringForKey:MJRefreshHeaderPullingText] forState:MJRefreshStatePulling]; // 刷新状态 [self setTitle:[NSBundle mj_localizedStringForKey:MJRefreshHeaderRefreshingText] forState:MJRefreshStateRefreshing];&#125;- (void)setTitle:(NSString *)title forState:(MJRefreshState)state &#123; if (title == nil) return; self.stateTitles[@(state)] = title; self.stateLabel.text = self.stateTitles[@(self.state)];&#125; 用懒加载的方式为 header 添加了两个标签，分别用于展示状态提示文案和上次刷新的时间； 123456789101112131415/** 显示刷新状态的label */- (UILabel *)stateLabel &#123; if (!_stateLabel) &#123; [self addSubview:_stateLabel = [UILabel mj_label]]; &#125; return _stateLabel;&#125;/** 显示上一次刷新时间的label */- (UILabel *)lastUpdatedTimeLabel &#123; if (!_lastUpdatedTimeLabel) &#123; [self addSubview:_lastUpdatedTimeLabel = [UILabel mj_label]]; &#125; return _lastUpdatedTimeLabel;&#125; 在 setState: 方法中为 2 个标签分别赋值，stateLabel 根据 state 从之前保存的可变字典（stateTitles）中取值（这里作者做了本地化处理），lastUpdatedTimeLabel 的赋值有点特别，他是在 setLastUpdatedTimeKey: 方法中对时间进行格式化等相关处理后赋值给 lastUpdatedTimeLabel，所以每次更新 state 的时候要调用用一次 self.lastUpdatedTimeKey = self.lastUpdatedTimeKey。 12345678- (void)setState:(MJRefreshState)state&#123; MJRefreshCheckState // 设置状态文字 self.stateLabel.text = self.stateTitles[@(state)]; // 重新设置key（重新显示时间） self.lastUpdatedTimeKey = self.lastUpdatedTimeKey;&#125; MJRefreshGifHeaderMJRefreshGifHeader 继承自上一个类，并增加了一个用于展示动画图片的 imageView 及用于保存各种状态对应的动画图片和动画时间字典。123456__unsafe_unretained UIImageView *_gifView;/** 所有状态对应的动画图片 */@property (strong, nonatomic) NSMutableDictionary *stateImages;/** 所有状态对应的动画时间 */@property (strong, nonatomic) NSMutableDictionary *stateDurations; 为了获取这些动画图片和时间，并与对应的状态关联起来，提供了 2 个供外界调用的方法，图片必须提供，时间可以不传(第二种方法)，会去默认值 images.count * 0.1。123456789101112131415161718- (void)setImages:(NSArray *)images duration:(NSTimeInterval)duration forState:(MJRefreshState)state&#123; if (images == nil) return; self.stateImages[@(state)] = images; self.stateDurations[@(state)] = @(duration); /* 根据图片设置控件的高度 */ UIImage *image = [images firstObject]; if (image.size.height &gt; self.mj_h) &#123; self.mj_h = image.size.height; &#125;&#125;- (void)setImages:(NSArray *)images forState:(MJRefreshState)state&#123; [self setImages:images duration:images.count * 0.1 forState:state];&#125; 这里重写了父类的 setState: 方法，当拖拽或刷新的时候才会去设置动画图片，首先停止之前的动画，然后再设置新值，如果是单张图片，直接展示，多张情况才需要展示动画; 如果 state 是正常状态，则停止动画。 123456789101112131415161718192021- (void)setState:(MJRefreshState)state&#123; MJRefreshCheckState // 根据状态做事情 if (state == MJRefreshStatePulling || state == MJRefreshStateRefreshing) &#123; NSArray *images = self.stateImages[@(state)]; if (images.count == 0) return; [self.gifView stopAnimating]; if (images.count == 1) &#123; // 单张图片 self.gifView.image = [images lastObject]; &#125; else &#123; // 多张图片 self.gifView.animationImages = images; self.gifView.animationDuration = [self.stateDurations[@(state)] doubleValue]; [self.gifView startAnimating]; &#125; &#125; else if (state == MJRefreshStateIdle) &#123; [self.gifView stopAnimating]; &#125;&#125; MJChiBaoZiHeader先来看看 prepare 的实现，只重写了父类的一个方法： 123456789101112131415161718192021222324- (void)prepare&#123; // 0.执行父类的 prepare 方法 [super prepare]; // 1.设置普通状态的动画图片 NSMutableArray *idleImages = [NSMutableArray array]; for (NSUInteger i = 1; i&lt;=60; i++) &#123; UIImage *image = [UIImage imageNamed:[NSString stringWithFormat:@"dropdown_anim__000%zd", i]]; [idleImages addObject:image]; &#125; [self setImages:idleImages forState:MJRefreshStateIdle]; // 2.设置即将刷新状态的动画图片（一松开就会刷新的状态） NSMutableArray *refreshingImages = [NSMutableArray array]; for (NSUInteger i = 1; i&lt;=3; i++) &#123; UIImage *image = [UIImage imageNamed:[NSString stringWithFormat:@"dropdown_loading_0%zd", i]]; [refreshingImages addObject:image]; &#125; [self setImages:refreshingImages forState:MJRefreshStatePulling]; // 3.设置正在刷新状态的动画图片 [self setImages:refreshingImages forState:MJRefreshStateRefreshing];&#125; 具体实现见上边的代码注释，其中 prepare 和 setImages: forState: 两个方法均来自基类，上边已经介绍过了。 4.3 上拉加载更多控件（refreshFooter）与 header 类似，先看一下类的继承关系： 既然都是继承自 MJRefreshComponent，这里就直接从 MJRefreshFooter 开始讨论，然后准者继承体系一直讲到 MJChiBaoZiFooter，即示例 2 用到的 footer。 MJRefreshFooter观察这个类的源码就会发现，他和 MJRefreshHeader 有许多相似之处，比如都提供了两个构造方法，一个用 block ，一个用 target-Action。下面主要说下不一样的地方。 有一个自动根据有无数据来显示和隐藏 footer 的属性 automaticallyHidden，不过作者不建议使用，而且后期可能会移除。不过，还是假名单介绍一下吧。 1@property (assign, nonatomic, getter=isAutomaticallyHidden) BOOL automaticallyHidden 在 viewWillMoveToSuperView: 中设置 footer 隐藏与否的时候会用到这个属性，详见前边 4.1刷新控件的载体 的介绍，这里不再复述。 最后看 2 个公共方法，废弃的那个就不列出来了O(∩_∩)O： 123456789/** 提示没有更多的数据 */- (void)endRefreshingWithNoMoreData &#123; MJRefreshDispatchAsyncOnMainQueue(self.state = MJRefreshStateNoMoreData;)&#125;/** 重置没有更多的数据（消除没有更多数据的状态） */- (void)resetNoMoreData &#123; MJRefreshDispatchAsyncOnMainQueue(self.state = MJRefreshStateIdle;)&#125; MJRefreshAutoFooterMJRefreshAutoFooter 是 MJRefreshFooter 的直接子类，并不是可以直接使用的类，还是在为子类提供方便，这里需要重点介绍一下。 提供了几个公开的属性，其作用见下方注释。 123456789101112131415#pragma mark - 公开的属性/** 是否自动刷新(默认为YES，即达到一定的触发条件就会自动开始刷新) */@property (assign, nonatomic, getter=isAutomaticallyRefresh) BOOL automaticallyRefresh;/** 当底部控件出现多少时就自动刷新(默认为1.0，也就是底部控件完全出现时，才会自动刷新) */@property (assign, nonatomic) CGFloat triggerAutomaticallyRefreshPercent;/** 是否每一次拖拽只发一次请求，手没有离开屏幕的情况下反复拖拽的话，不会触发多次刷新 */@property (assign, nonatomic, getter=isOnlyRefreshPerDrag) BOOL onlyRefreshPerDrag;#pragma mark - 私有的属性/** 是否是一个新的拖拽 */@property (assign, nonatomic, getter=isOneNewPan) BOOL oneNewPan; 重写了 willMoveToSuperView 方法，当添加到父控件上时，给 scrollView.contentInset.bottom 添加一个 footer 本身的高度，反之，从父控件上移除时，又要将之前加上的再减掉。 1234567891011121314151617- (void)willMoveToSuperview:(UIView *)newSuperview &#123; [super willMoveToSuperview:newSuperview]; if (newSuperview) &#123; // 添加到父控件上 if (self.hidden == NO) &#123; self.scrollView.mj_insetB += self.mj_h; &#125; self.mj_y = _scrollView.mj_contentH; // 设置位置 &#125; else &#123; // 被移除了 if (self.hidden == NO) &#123; self.scrollView.mj_insetB -= self.mj_h; &#125; &#125;&#125; 在准备数据阶段设置了这么几个初值： 123456789101112- (void)prepare &#123; [super prepare]; // 默认底部控件100%出现时才会自动刷新 self.triggerAutomaticallyRefreshPercent = 1.0; // 设置为默认状态 self.automaticallyRefresh = YES; // 默认是当offset达到条件就发送请求（可连续） self.onlyRefreshPerDrag = NO;&#125; 下面是对 scrollView 的监听触发的 3 个事件： 当 scrollView 的 contentSize 发生变化的时候，计时更新 footer 的 y 值，保证它一直贴着 scrollView 的下边沿。 123456- (void)scrollViewContentSizeDidChange:(NSDictionary *)change &#123; [super scrollViewContentSizeDidChange:change]; // 设置位置 self.mj_y = self.scrollView.mj_contentH;&#125; 当滑动 scrollView 产生 contentOffset 的时候，控制当底部刷新控件完全出现的时候，才能刷新。 12345678910111213141516171819- (void)scrollViewContentOffsetDidChange:(NSDictionary *)change&#123; [super scrollViewContentOffsetDidChange:change]; if (self.state != MJRefreshStateIdle || !self.automaticallyRefresh || self.mj_y == 0) return; if (_scrollView.mj_insetT + _scrollView.mj_contentH &gt; _scrollView.mj_h) &#123; // 内容超过一个屏幕 // 这里的_scrollView.mj_contentH替换掉self.mj_y更为合理 if (_scrollView.mj_offsetY &gt;= _scrollView.mj_contentH - _scrollView.mj_h + self.mj_h * self.triggerAutomaticallyRefreshPercent + _scrollView.mj_insetB - self.mj_h) &#123; // 防止手松开时连续调用 CGPoint old = [change[@"old"] CGPointValue]; CGPoint new = [change[@"new"] CGPointValue]; if (new.y &lt;= old.y) return; // 当底部刷新控件完全出现时，才刷新 [self beginRefreshing]; &#125; &#125;&#125; 当手势的状态发生变化的时候，针对 UIGestureRecognizerStateEnded 和 UIGestureRecognizerStateBegan 两种状态进行处理，对于前者，根据 contentOffset.y 决定开始刷新的时机，后者的话，就认为是一个新的手势开始了。 123456789101112131415161718192021- (void)scrollViewPanStateDidChange:(NSDictionary *)change&#123; [super scrollViewPanStateDidChange:change]; if (self.state != MJRefreshStateIdle) return; UIGestureRecognizerState panState = _scrollView.panGestureRecognizer.state; if (panState == UIGestureRecognizerStateEnded) &#123;// 手松开 if (_scrollView.mj_insetT + _scrollView.mj_contentH &lt;= _scrollView.mj_h) &#123; // 不够一个屏幕 if (_scrollView.mj_offsetY &gt;= - _scrollView.mj_insetT) &#123; // 向上拽 [self beginRefreshing]; &#125; &#125; else &#123; // 超出一个屏幕 if (_scrollView.mj_offsetY &gt;= _scrollView.mj_contentH + _scrollView.mj_insetB - _scrollView.mj_h) &#123; [self beginRefreshing]; &#125; &#125; &#125; else if (panState == UIGestureRecognizerStateBegan) &#123; self.oneNewPan = YES; &#125;&#125; 当然也会重写 setState 方法，如果是刷新状态，就执行刷新的回调；如果是从刷新状态变成没有更多数据或停止刷新的状态，则执行停止刷新完成的 block。 12345678910111213141516171819- (void)setState:(MJRefreshState)state&#123; MJRefreshCheckState if (state == MJRefreshStateRefreshing) &#123; // 刷新状态，执行刷新的回调 [self executeRefreshingCallback]; &#125; else if (state == MJRefreshStateNoMoreData || state == MJRefreshStateIdle) &#123; // 从 刷新状态 进入 没有更多数据或者正常状态 时，如果有完成后的回调，则执行之 if (MJRefreshStateRefreshing == oldState) &#123; if (self.endRefreshingCompletionBlock) &#123; self.endRefreshingCompletionBlock(); &#125; &#125; &#125;&#125; 最后是 setHidden: 方法，根据显示隐藏的变化，调整 contentInset 、 state 和 self.frame.origin.y。 123456789101112131415161718192021- (void)setHidden:(BOOL)hidden&#123; BOOL lastHidden = self.isHidden; [super setHidden:hidden]; // 从显示变成隐藏状态 if (!lastHidden &amp;&amp; hidden) &#123; self.state = MJRefreshStateIdle; self.scrollView.mj_insetB -= self.mj_h; &#125; else if (lastHidden &amp;&amp; !hidden) &#123; // 从隐藏变成显示状态 self.scrollView.mj_insetB += self.mj_h; // 设置位置 self.mj_y = _scrollView.mj_contentH; &#125;&#125; MJRefreshAutoStateFooterMJRefreshAutoStateFooter 继承自 MJRefreshAutoFooter 与 MJRefreshStateHeader 类似，从这里开始介入具体的 UI，既可以直接使用了，这里只介绍与 MJRefreshStateHeader 不同的地方。 只有一个显示刷新状态的 stateLabel 和 保存不同状态下文案的可变字典 stateTitles 12345/** 显示刷新状态的label */__unsafe_unretained UILabel *_stateLabel;/** 所有状态对应的文字 */@property (strong, nonatomic) NSMutableDictionary *stateTitles; 重写父类 prepare 方法时，除了保存各种状态的本地化文案外，还给 stateLabel 添加了点击手势。 12345678910111213141516- (void)prepare&#123; [super prepare]; // 初始化间距 self.labelLeftInset = MJRefreshLabelLeftInset; // 初始化文字 [self setTitle:[NSBundle mj_localizedStringForKey:MJRefreshAutoFooterIdleText] forState:MJRefreshStateIdle]; [self setTitle:[NSBundle mj_localizedStringForKey:MJRefreshAutoFooterRefreshingText] forState:MJRefreshStateRefreshing]; [self setTitle:[NSBundle mj_localizedStringForKey:MJRefreshAutoFooterNoMoreDataText] forState:MJRefreshStateNoMoreData]; // 监听label self.stateLabel.userInteractionEnabled = YES; [self.stateLabel addGestureRecognizer:[[UITapGestureRecognizer alloc] initWithTarget:self action:@selector(stateLabelClick)]];&#125; 点击 stateLabel 的时候，如果是正常未刷新的状态，则开始刷新。 12345- (void)stateLabelClick &#123; if (self.state == MJRefreshStateIdle) &#123; [self beginRefreshing]; &#125;&#125; 重写 setState: 方法的时候，增加刷新过程中对 stateLabel 显示与隐藏的控制。 12345678910- (void)setState:(MJRefreshState)state&#123; MJRefreshCheckState if (self.isRefreshingTitleHidden &amp;&amp; state == MJRefreshStateRefreshing) &#123; self.stateLabel.text = nil; &#125; else &#123; self.stateLabel.text = self.stateTitles[@(state)]; &#125;&#125; MJRefreshAutoGifFooterMJRefreshAutoGifFooter 继承自 MJRefreshAutoStateFooter，仔细查看其实现代码，就会发现与 MJRefreshGifHeader 非常类似，都是在父类基础上加了一个 gifView（UIImageView） 用于展示动画，其他操作也基本一致，只是增加了 MJRefreshStateNoMoreData 状态及 gifView 与 stateLabel 的显隐控制。 MJChiBaoZiFooterMJChiBaoZiFooter 里边也重写了父类的 prepare 方法，调用了父类的 setImages: forState: 方法用于设置创新状态时的动画图片，至于其方法实现，见父类。 1234567891011121314- (void)prepare&#123; [super prepare]; // 设置正在刷新状态的动画图片 NSMutableArray *refreshingImages = [NSMutableArray array]; for (NSUInteger i = 1; i&lt;=3; i++) &#123; UIImage *image = [UIImage imageNamed:[NSString stringWithFormat:@"dropdown_loading_0%zd", i]]; [refreshingImages addObject:image]; &#125; [self setImages:refreshingImages forState:MJRefreshStateRefreshing];&#125; 5.小结本文只是对 MJRefresh 源码的一个简单讨论，很多细节还没有讲的很透，后期会及时更新这部分内容。 6.参考 《Effective Objective-C 2.0》 iOS 11 安全区域适配总结]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>MJRefresh</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[使用 CocoaPods 创建并管理私有仓库]]></title>
    <url>%2F2018%2F12%2F18%2F%E4%BD%BF%E7%94%A8-CocoaPods-%E5%88%9B%E5%BB%BA%E5%B9%B6%E7%AE%A1%E7%90%86%E7%A7%81%E6%9C%89%E4%BB%93%E5%BA%93%2F</url>
    <content type="text"><![CDATA[CocoaPods 是一个比较常用的 iOS 依赖管理工具，我们目前的项目中就在使用，今天抽空做一个小结，记录一下在 Git 环境下如何使用 CocoaPods 创建并管理我们私有仓库。 创建私有仓库的主要步骤： 创建私有的 Spec Repo。(从未创建过私有库时需要，只需创建一次) 创建(具体的 Pod 需要的)项目工程文件。(2 和 3 二选一) 创建(具体的 Pod 对应的) xxx.podspec 文件。(2 和 3 二选一) 本地测试 xxx.podspec 文件及 Pod 库 功能。 向私有的 Spec Repo 中提交 xxx.podspec。 使用创建好的 Pod 库。 更新维护 xxx.podspec。 创建私有 Spec Repo 首先介绍一下 Spec Repo，它是所有的 Pods 的一个索引，实际也是一个 Git 仓库，远端在 GitHub、其他代码托管平台或自家的服务器上，当使用了 CocoaPods 后会被clone到本地的 ~/.cocoapods/repos 目录下，进入此目录后看到的 master 文件夹就是官方的 Spec Repo，本文中自己创建的私有库会在与 master 平级的位置。master 目录的结构大概是这样的（缩减了几个层级）： 1234567891011121314151617.├── CocoaPods-version.yml├── README.md└── Specs ├── [SPEC_NAME_A] │ ├── [VERSION_1] │ │ └── [SPEC_NAME_A].podspec │ └── [VERSION_2] │ └── [SPEC_NAME_A].podspec ├── [SPEC_NAME_B] │ ├── [VERSION_1] │ │ └── [SPEC_NAME_B].podspec │ └── [VERSION_2] │ └── [SPEC_NAME_B].podspec └── [SPEC_NAME_C] └── [VERSION_1] └── [SPEC_NAME_C].podspec （注：目录结构是用 tree -L [level] 命令打印的, tree 可以通过 Homebrew 的命令 brew install tree 安装的） 我们需要创建一个类似于 master 的私有 Spec Repo，这里自己创建一个 Git 仓库，这个仓库你可以创建私有的也可以创建公开的(此处我创建了公开的)，对于私有仓库，如果项目中有其他同事共同开发的话，你还要给他这个 Git 仓库的权限。这里我使用了 码云。 在服务端创建，创建完成之后在 Terminal 中执行如下命令： 12# pod repo add [Private Repo Name] [remote repository clone URL]$ pod repo add riversea2015_mobile_iphone_repo_local https://gitee.com/riversea2015/riversea2015_mobile_iphone_repo.git 说明 1：riversea2015_mobile_iphone_repo_local 是本地显示的名字，可以和后边链接里的 riversea2015_mobile_iphone_repo 名称不同(⊙o⊙)哦！ 说明 2：如果有其他合作人员共同使用这个私有 Spec Repo 的话，在他有对应 Git 仓库的权限的前提下，执行相同的命令添加这个 Spec Repo 即可。 至此第一步创建私有 Spec Repo 完成。 说明 3：如果有现有的组件项目，并且在 Git 的版本管理下，那么可以直接进行第 3 步了；如果你的组件还在你冗余庞大的项目中，需要拆分出来或者需要自己从零开始创建一个组件库，建议使用 CocoaPods 提供的一个工具创建工程文件，详见第 2 步。 创建 Pod 项目工程文件创建项目（ pod + Example）此处使用 CocoaPods 提供的一个工具创建单独的 pod 项目文件，工具的文档介绍是 Using Pod Lib Create， 就拿我创建的 HHPodTestLibrary 为例介绍一下具体操作： 12345# 先cd到要创建项目的目录然后执行$ cd /Users/sea/Documents# 然后开始创建$ pod lib create HHPodTestLibrary 之后他会问你5个问题 (具体介绍见官方文档，箭头后边是我的选择): 1234567891011121314151617What platform do you want to use?? [ iOS / macOS ] // 选择平台 &gt; iosWhat language do you want to use?? [ Swift / ObjC ] // 选择编程语言 &gt; objcWould you like to include a demo application with your library? [ Yes / No ] // 是否需要一个例子工程 &gt; yesWhich testing frameworks will you use? [ Specta / Kiwi / None ] // 选择一个测试框架 &gt; spectaWould you like to do view based testing? [ Yes / No ] // 是否要做 UI 测试 &gt; yesWhat is your class prefix? // 定义类前缀 &gt; HH 回答完以上5个问题，会自动执行 pod install 命令创建项目并生成依赖，然后会自动使用 Xcode 打开工程。 添加库文件，并提交远端仓库首先在 Pod 的 Classes 中添加了 4 个文件，然后进入 Example 文件夹执行 pod update 命令，再打开项目工程可以看到，刚刚添加的组件已经在 Pods 子工程下的 Development Pods/HHPodTestLibrary 中了。 测试无误后需要将该项目添加并推送到远端仓库。 通过 CocoaPods 创建出来的目录本身就在本地的 Git 管理下，我们需要做的就是给它添加远端仓库，同样去 GitHub 或其他的 Git 服务提供商那里创建一个私有的仓库，拿到 SSH 或 HTTPS 地址，然后 cd 到 PodTestLibrary 目录，提交修改: 1234$ git add .$ git commit -s -m "Initial Commit of Library"$ git remote add origin https://gitee.com/riversea2015/HHPodTestLibrary.git # 添加远端仓库（SSH或Https）$ git push origin master # 提交到远端仓库 补充说明:1.如果一开始在远端创建的仓库里有文件，执行 git push origin master 的时候是推不上去的，应该会提示你执行 git pull origin master 命令拉取并合并远端代码，不过，当你执行此命令时，又会报下边的错： 1fatal: refusing to merge unrelated histories 此时，可以执行 git pull origin master --allow-unrelated-histories 命令强制合并 2 个不相干的 git 仓库，然后使用正常的 git push origin master 就可以了推上去了。 其实，还有一种更加简便也更加合理的方式解决这个问题，那就是在远端创建仓库时一个文件都不要加，如下图： 然后在终端已经存在的 git 仓库中执行以下操作（上图虚线框中的命令）就可以推上去了： 12bogon:untitled sea$ git remote add origin https://github.com/riversea2015/test.gitbogon:untitled sea$ git push -u origin master 2.建议将整个目录 (pod 和 Example) 上传到私有仓库，构成一个完整的工程，这样后期为 pod 添加新功能的时候，就可以顺便在 Example 里边进行验证。 3.每当你向 Pod 中添加了新的文件或者以后更新了 xxx.podspec 中的版本号时，都需要重新执行一遍 pod update 命令。 配置/修改 xxx.podspec 文件执行完上边的操作之后，就该编辑 HHPodTestLibrary.podspec 文件了，这是一个 Ruby 的文件，把编辑器的格式改成 Ruby，就能看到语法高亮，下面我贴上我的 podspec 文件，并在后面以注释的形式说明每个字段的含义，没有涉及到的字段可以去官方文档查阅。(此处我的文件没有删除注释，实际操作时可以删掉注释) 12345678910111213141516171819202122232425262728293031323334353637383940## Be sure to run `pod lib lint HHPodTestLibrary.podspec' to ensure this is a# valid spec before submitting.## Any lines starting with a # are optional, but their use is encouraged# To learn more about a Podspec see https://guides.cocoapods.org/syntax/podspec.html#Pod::Spec.new do |s| s.name = 'HHPodTestLibrary' # 名称 s.version = '0.1.0' # 版本号 s.summary = 'This is the first pod repo test library.' # 简介 s.description = 'This is the first pod repo test library!' # 详细介绍 s.homepage = 'https://gitee.com/riversea2015/HHPodTestLibrary' # 主页,尽量填写可以访问到的地址，否则验证不通过 # s.screenshots = 'www.example.com/screenshots_1', 'www.example.com/screenshots_2' # 屏幕截图 s.license = &#123; :type =&gt; 'MIT', :file =&gt; 'LICENSE' &#125; # 主页,这里要填写可以访问到的地址，不然验证不通过 s.author = &#123; 'hehai' =&gt; 'hehai682@126.com' &#125; # 作者信息 s.source = &#123; :git =&gt; 'https://gitee.com/riversea2015/HHPodTestLibrary.git', :tag =&gt; s.version.to_s &#125; # 项目地址，这里不支持 ssh 的地址，验证不通过，只支持 HTTP 和 HTTPS，最好使用 HTTPS # s.social_media_url = 'https://twitter.com/&lt;TWITTER_USERNAME&gt;' # 多媒体介绍地址 s.ios.deployment_target = '8.0' # 支持的平台版本，此处是 iOS8.0 以上 s.requires_arc = true # 是否使用 ARC，如果指定具体文件，则具体的问题使用 ARC s.source_files = 'HHPodTestLibrary/Classes/**/*.&#123;h,m&#125;' # 文件路径 # s.resource_bundles = &#123; # 'HHPodTestLibrary' =&gt; ['HHPodTestLibrary/Assets/*.png'] # 资源文件地址 # &#125; s.public_header_files = 'HHPodTestLibrary/Classes/**/*.h' # 公开的头文件地址 s.prefix_header_contents = '#ifdef __OBJC__','#import "其他pod中的头文件A.h"','#import "其他pod中的头文件B.h"','#import "其他pod中的头文件C.h"','#endif' # 相当于 pod 中的一个 pch 文件，可以保证等号右边的头文件会被此 podspec 文件对应的库中所有文件可见，而其中的 #ifdef __OBJC__ 保证只在 OC 环境中引入这些头文件，其它如 C++ 等则不会引入。 # s.frameworks = 'UIKit', 'MapKit' # 所需的 framework，多个用逗号隔开 # s.dependency 'AFNetworking', '~&gt; 2.3' # 依赖关系，该项目所依赖的其他库，如果有多个需要填写多个 # s.dependency 'SDWebImage'end 编辑完 HHPodTestLibrary.podspec 文件后，需要验证一下这个文件是否可用，如果有任何 WARNING 或者 ERROR 都是不可以的，它就不能被添加到 Spec Repo 中，不过 Xcode 的 WARNING 是可以存在的（即，如果你只是本地使用，允许存在个别 error 和 warning），验证需要执行以下命令： 1$ pod lib lint 当你看到 12-&gt; HHPodTestLibrary (0.1.0)HHPodTestLibrary passed validation. 就说明验证通过了，不过这只是这个 podspec 文件是合格的，不一定说明这个 Pod 是可以用的，后边还需要在本地做一下验证。 提交修改（包括 HHPodTestLibrary.podSpec 文件） 123$ git add .$ git commit -m '编辑 podspec 文件'$ git push origin master * 从此处跳转执行 4 的操作，完成 4 的验证操作后(有可能需要回来修改 pod 中的文件，并提交),再接着执行下边的打 Tag 操作。 因为 HHPodTestLibrary.podspec 文件中获取 Git 版本控制的项目还需要 tag 号，所以我们要打上一个 tag，并将 tag 推送到 remote repository： 12$ git tag -m 'first release' '0.1.0'$ git push --tags * 至此，可以跳转至 5 执行后边的操作了。 创建 具体的Pod 对应的 xxx.podspec 文件。(“步骤二” 和 “步骤三” 2选1)如果执行了 “步骤二” 的操作，请绕行至 “步骤四” O(∩_∩)O~ 如果已经有了现成的项目，那么就需要给这个项目创建一个 podspec 文件，创建它需要执行 CocoaPods 的另外一个命令，官方文档在这里 12pod spec create &lt;spec file name&gt; [对应私有仓库的地址，需要实现创建好，否则会失败]$ pod spec create HHPodTestLibrary https://gitee.com/riversea2015/HHPodTestLibrary.git 执行完之后，就创建了一个 podspec 文件，他其中会包含很多内容，可以按照我之前介绍的进行编辑，没用的删掉。编辑完成之后使用验证命令验证一下 1$ pod lib lint 验证无误就可以进入下一步了。 * 提交代码，并 push 到 remote repository * 跳转执行 &quot;步骤四&quot; 的操作，完成 &quot;步骤四&quot; 的验证操作后(有可能需要回来修改 pod 中的文件，并提交),再执行打 Tag 并 push tag 的操作。 * 至此，可以跳转至 &quot;步骤五&quot; 执行后边的操作了。 本地测试 xxx.podspec 文件及 Pod 功能方案一：可以创建一个新的 Demo，在这个项目的 Podfile 文件中直接指定刚才创建编辑好的 podspec 文件，看是否可用。 在 Podfile 中我们可以这样编辑： 1234platform :ios, '7.0'# 以下2种方式均可：pod 'HHPodTestLibrary', :path =&gt; '&lt;指定本地 HHPodTestLibrary 目录的路径&gt;'pod 'HHPodTestLibrary', :podspec =&gt; '&lt;指定本地 .podspec 文件路径，含文件名&gt;' 方案二：如果前边是使用 “步骤二” 创建的项目，就会自动生成 Example 项目，只需要 cd 到 Example 目录下，执行： 123pod install# 或者pod update --no-repo-update 在 Example 中编写代码，测试库功能无误后就可以开始 5 了，提交 podspec 到 Spec Repo 中。 * 如果在 Example 的 Podfile 中写了 use_frameworks! 则可能导致无法引用 pod 中的文件，而且 pod 之间也就互不可见 向 Spec Repo 提交 xxx.podspec向 Spec Repo 提交 podspec 需要完成两点： ①是 podspec 必须通过验证无误； ②就是删掉无用的注释（这个不是必须的，为了规范还是删掉吧）。 向我们的私有 Spec Repo 提交 podspec 只需要一个命令： 12# pod repo push &lt;本地 repo 的名字&gt; &lt;.podspec 名字&gt;$ pod repo push riversea2015_mobile_iphone_repo_local HHPodTestLibrary.podspec #前面是本地 Repo 名字 后面是 .podspec 名字 完成之后这个组件库就添加到我们的私有 Spec Repo 中了，可以进入到 ~/.cocoapods/repos/riversea2015_mobile_iphone_repo_local 目录下查看 123456.├── LICENSE├── HHPodTestLibrary│ └── 0.1.0│ └── HHPodTestLibrary.podspec└── README.md 再去看我们的 Spec Repo 远端仓库，也有了一次提交，这个 podspec 也已经被 Push 上去了。 至此，我们的这个组件库就已经制作添加完成了，使用 pod search 命令就可以查到我们自己的库了 1234567$ pod search HHPodTestLibrary-&gt; HHPodTestLibrary (0.1.0)Just Testing.pod 'HHPodTestLibrary', '~&gt; 0.1.0'- Homepage: https://gitee.com/riversea2015/HHPodTestLibrary- Source: https://gitee.com/riversea2015/HHPodTestLibrary.git- Versions: 0.1.0 [riversea2015_mobile_iphone_repo repo] 这里说的是添加到我们自己的 Repo，如果要添加到 CocoaPods 的官方库了，可以使用 trunk 工具，具体可以查看 官方文档。 使用制作好的 Pod在完成这一系列步骤之后，我们就可以在正式项目中使用这个私有的 Pod 了只需要在项目的 Podfile 里增加下边这行代码即可 1$ pod 'HHPodTestLibrary', '~&gt; 0.1.0' 然后执行执行以下命令： 1pod update --no-repo-update # 更新库依赖 打开项目可以看到，我们自己的库文件已经出现在 Pods 子项目中的 Pods 子目录下了，而不再是 Development Pods。 注意：这里 Podfile 文件前两行应该是： 12source 'https://github.com/CocoaPods/Specs.git' # GitHub 的公有仓库source 'https://gitee.com/riversea2015/riversea2015_mobile_iphone_repo.git' # 码云上的私有仓库 更新维护 xxx.podspec最后再来说一下制作好的 HHPodTestLibrary.podspec 文件后续的更新维护工作，比如如何添加新的版本，如何删除 Pod。 现在已经制作好了 HHPodTestLibrary 的 0.1.0 版本，现在对他做一些更新维护工作： 添加更多的子 pod这次我添加了更多的模块到 PodTestLibrary 之中，包括工具类，底层 Model 及 UIKit 扩展等，这里又尝试了一下 subspec 功能，给 PodTestLibrary 创建了多个子分支。 具体做法是先将源文件添加到 Pod/Classes 中，然后按照不同的模块对文件目录进行整理，因为我有四个模块，所以在 Pod/Classes 下有创建了四个子目录，完成之后继续编辑之前的 PodTestLibrary.podspec，这次增加了 subspec 特性 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556## Be sure to run `pod lib lint HHPodTestLibrary.podspec' to ensure this is a# valid spec before submitting.## Any lines starting with a # are optional, but their use is encouraged# To learn more about a Podspec see https://guides.cocoapods.org/syntax/podspec.html#Pod::Spec.new do |s| s.name = 'HHPodTestLibrary' s.version = '0.1.3' s.summary = 'This is the first pod repo test library.' s.description = &lt;&lt;-DESCThis is the first pod repo test library! DESC s.homepage = 'https://gitee.com/riversea2015/HHPodTestLibrary' # s.screenshots = 'www.example.com/screenshots_1', 'www.example.com/screenshots_2' s.license = &#123; :type =&gt; 'MIT', :file =&gt; 'LICENSE' &#125; s.author = &#123; 'hehai' =&gt; 'hehai682@126.com' &#125; s.source = &#123; :git =&gt; 'https://gitee.com/riversea2015/HHPodTestLibrary.git', :tag =&gt; s.version.to_s &#125; # s.social_media_url = 'https://twitter.com/&lt;TWITTER_USERNAME&gt;' s.ios.deployment_target = '8.0' s.requires_arc = true # s.source_files = 'HHPodTestLibrary/Classes/**/*.&#123;h.m&#125;' # s.public_header_files = 'HHPodTestLibrary/Classes/**/*.h' # s.resource_bundles = &#123; # 'HHPodTestLibrary' =&gt; ['HHPodTestLibrary/Assets/*.png'] # &#125; s.subspec 'First' do |first| first.source_files = 'HHPodTestLibrary/Classes/First/**/*.&#123;h,m&#125;' first.public_header_files = 'HHPodTestLibrary/Classes/First/**/*.h' first.dependency 'AFNetworking', '~&gt; 3.1' end s.subspec 'Second' do |second| second.source_files = 'HHPodTestLibrary/Classes/Second/**/*.&#123;h,m&#125;' second.public_header_files = 'HHPodTestLibrary/Classes/Second/**/*.h' second.dependency 'HHPodTestLibrary/First' end s.subspec 'Third' do |third| third.source_files = 'HHPodTestLibrary/Classes/Third/**/*.&#123;h,m&#125;' third.public_header_files = 'HHPodTestLibrary/Classes/Third/**/*.h' third.dependency 'FMDB', '~&gt;2.7.1' end # s.frameworks = 'UIKit', 'MapKit' # s.dependency 'AFNetworking', '~&gt; 2.3' # s.dependency 'OpenUDID', '~&gt; 1.0.0'end 注意： * .subspec &apos;First&apos; do |first| 中 子 pod 的别名必须以小写字母开头，否则 pod lib lint 时会报错，而且并不指明错误原因，会很崩溃的(⊙o⊙)哦 * 默认 pod 与 pod 之间是可以互相引用的，但是 pod 内部是无法直接引用外部文件的，所以需要我们来做这些事情。 因为我们创建了 subspec，所以项目整体的依赖 dependency、源文件 source_files、头文件 public_header_files、资源文件 resource 等都移动到了各自的 subspec 中，各个 subspec 之间也可以相互依赖，比如 Second 就依赖于 First。 编辑完成之后，在测试项目里执行 pod update，几个子 pod 都被加进工程了，写代码验证无误之后，就可以将这个工程 push 到远端仓库，并打上新的 tag -&gt; 0.1.3。 最后再次使用 pod lib lint 验证编辑好的 podsepc 文件，没有自身的 WARNING 或者 ERROR 之后，就可以再次提交到 Spec Repo 中了。 1$ pod repo push riversea2015_mobile_iphone_repo_local HHPodTestLibrary.podspec 之后再次到 ~/.cocoapods/repos/riversea2015_mobile_iphone_repo_local 目录下查看 123456789101112.├── HHPodTestLibrary│ ├── 0.1.0│ │ └── HHPodTestLibrary.podspec│ ├── 0.1.2│ │ └── HHPodTestLibrary.podspec│ └── 0.1.3│ └── HHPodTestLibrary.podspec├── LICENSE└── README.md4 directories, 5 files 已经有 3 个版本了，使用 pod search 查找得到的结果为 1234567891011-&gt; HHPodTestLibrary (0.1.3) This is the first pod repo test library. pod 'HHPodTestLibrary', '~&gt; 0.1.3' - Homepage: https://gitee.com/riversea2015/HHPodTestLibrary - Source: https://gitee.com/riversea2015/HHPodTestLibrary.git - Versions: 0.1.3, 0.1.2, 0.1.0 [riversea2015_mobile_iphone_repo_local repo] - Subspecs: - HHPodTestLibrary/First (0.1.3) - HHPodTestLibrary/Second (0.1.3) - HHPodTestLibrary/Third (0.1.3)(END) 完成这些之后，在实际项目中我们就可以选择使用整个组件库或者是组件库的某一子库了，对应的 Podfile 中添加的内容为 1234567platform :ios, '8.0'pod 'HHPodTestLibrary/Second', '~&gt;0.1.0' # 使用某一个部分（导入了2个子库）# pod 'HHPodTestLibrary/Third', '~&gt; 0.1.0' # 使用某一个部分（导入了1个子库）# pod 'HHPodTestLibrary', '~&gt; 0.1.0' # 使用整个库 注意：此处，因为 Second 依赖于 First，所以当 pod update 之后，实际导入项目中的子库总共有2个：First、Second。 删除、恢复私有 Spec Repo最后介绍一下如何删除一个私有 Spec Repo，只需要执行一条命令即可 1$ pod repo remove riversea2015_mobile_iphone_repo_local 这样，这个 Spec Repo 就在本地删除了，我们还可以通过下边这行命令，再把它给加回来。 1$ pod repo add riversea2015_mobile_iphone_repo_local https://gitee.com/riversea2015/riversea2015_mobile_iphone_repo.git 删除私有 Spec Repo 下的某一个 podspec如果我们要删除私有 Spec Repo 下的某一个 podspec 怎么操作呢，此时无需借助 CocoaPods，只需要切到 riversea2015_mobile_iphone_repo_local 目录下，删掉库目录： 12$ cd ~/.cocoapods/repos/riversea2015_mobile_iphone_repo_local$ rm -Rf HHPodTestLibrary 然后在将 Git 的变动 push 到远端仓库即可。 1234$ cd ~/.cocoapods/repos/riversea2015_mobile_iphone_repo_local$ git add --all .$ git ci -m "remove unuseful pods"$ git push origin master 错误处理常见的错误处理可以参考 这篇文章 的 “报错问题” 部分，个人觉得这位前辈写的已经比较全面了。 另外，如果是在码云上边创建的 私有仓库，那么执行 pod lib lint 的时候，可能会报以下错误： 1The URL (https://gitee.com/xxx/xxx) is not reachable. 最后在 stackoverflow 上找到了参考的解决方案 ：https://stackoverflow.com/questions/27303475 参考资料： private-cocoapods creating-a-private-cocoapod developing-private-in-house-libraries-with-cocoapods create-private-podspec refusing-to-merge-unrelated-histories]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>CocoaPods</tag>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[AFNetWorking 源码学习笔记 ☞ Reachability]]></title>
    <url>%2F2018%2F12%2F17%2FAFNetWorking-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-Reachability%2F</url>
    <content type="text"><![CDATA[前言本文是 AFNetWorking 源码学习笔记的第五篇，本篇将介绍第四部分 – Reachability 目录下的类，也就是 AFNetworkReachabilityManager。 虽然之前经历的几个项目里边都是对苹果提供的 Reachability 进行了一次封装，不过，还是有必要学习一下 AFNetWorking 中监听网络状态的类 AFNetworkReachabilityManager，也就是本文的重点。 使用示例先来看一个使用示例：123456789101112131415161718192021AFNetworkReachabilityManager *manager = [AFNetworkReachabilityManager sharedManager];[manager setReachabilityStatusChangeBlock:^(AFNetworkReachabilityStatus status) &#123; switch (status) &#123; case AFNetworkReachabilityStatusUnknown: NSLog(@"网络状态未知"); break; case AFNetworkReachabilityStatusNotReachable: NSLog(@"无网络"); break; case AFNetworkReachabilityStatusReachableViaWWAN: NSLog(@"手机自带网络"); break; case AFNetworkReachabilityStatusReachableViaWiFi: NSLog(@"WiFi"); break; &#125;&#125;];[manager startMonitoring]; 运行后发现，状态改变时会有相应的输出，在模拟器上可能不太准确，断网后再连网，监测不到，不过真机是正常的。 122018-11-28 16:42:40.825948+0800 HHAFNetworkingStudy[5703:831886] WiFi2018-11-28 16:42:51.052528+0800 HHAFNetworkingStudy[5703:831886] 无网络 正文现在开始查看源码，点开 AFNetworkReachabilityManager 的头文件，首先是 4 个属性： 1234@property (readonly, nonatomic, assign) AFNetworkReachabilityStatus networkReachabilityStatus; // 标识网络状态的枚举值@property (readonly, nonatomic, assign, getter = isReachable) BOOL reachable; // 网络是否可用@property (readonly, nonatomic, assign, getter = isReachableViaWWAN) BOOL reachableViaWWAN; // 手机自带网络是否可用@property (readonly, nonatomic, assign, getter = isReachableViaWiFi) BOOL reachableViaWiFi; // Wifi是否可用 后 3 个属性的 getter 如下，我们发现，他们都是通过第一个属性 networkReachabilityStatus 的值来做判断的。 1234567891011- (BOOL)isReachable &#123; return [self isReachableViaWWAN] || [self isReachableViaWiFi];&#125;- (BOOL)isReachableViaWWAN &#123; return self.networkReachabilityStatus == AFNetworkReachabilityStatusReachableViaWWAN;&#125;- (BOOL)isReachableViaWiFi &#123; return self.networkReachabilityStatus == AFNetworkReachabilityStatusReachableViaWiFi;&#125; 下方所示是创建及初始化方法，其中 new 和 init 方法被标识为不可用，查看实现文件得知，如果调用他们，会直接 crash。 123456789+ (instancetype)sharedManager; // 单例+ (instancetype)manager; // 针对默认地址（零地址）+ (instancetype)managerForDomain:(NSString *)domain; // 针对域名+ (instancetype)managerForAddress:(const void *)address; // 针对地址- (instancetype)initWithReachability:(SCNetworkReachabilityRef)reachability NS_DESIGNATED_INITIALIZER; // 最终调用的方法+ (instancetype)new NS_UNAVAILABLE; // 不可用- (instancetype)init NS_UNAVAILABLE; // 不可用 1234567// 如果直接或间接调用此方法会抛出异常 crash- (instancetype)init &#123; @throw [NSException exceptionWithName:NSGenericException reason:@"`-init` unavailable. Use `-initWithReachability:` instead" userInfo:nil]; return nil;&#125; 其他创建方法最终调用的都是 initWithReachability: ，实现如下，给 _networkReachability 和 networkReachabilityStatus 赋初值。对于其中的 _networkReachability，提供了 2 种创建方法，见下方 2 个创建 manager 的类方法。 123456789101112- (instancetype)initWithReachability:(SCNetworkReachabilityRef)reachability &#123; self = [super init]; if (!self) &#123; return nil; &#125; // 网络连接的对象 _networkReachability = CFRetain(reachability); // 初始状态为未知 self.networkReachabilityStatus = AFNetworkReachabilityStatusUnknown; return self;&#125; 123456789101112131415161718192021222324// 2 个创建 manager 的类方法+ (instancetype)managerForDomain:(NSString *)domain &#123; // 创建网络连接的对象：根据传入的域名创建网络连接引用 SCNetworkReachabilityRef reachability = SCNetworkReachabilityCreateWithName(kCFAllocatorDefault, [domain UTF8String]); AFNetworkReachabilityManager *manager = [[self alloc] initWithReachability:reachability]; CFRelease(reachability); return manager;&#125;+ (instancetype)managerForAddress:(const void *)address &#123; // 创建网络连接的对象：根据传入的地址创建网络连接的引用 SCNetworkReachabilityRef reachability = SCNetworkReachabilityCreateWithAddress(kCFAllocatorDefault, (const struct sockaddr *)address); AFNetworkReachabilityManager *manager = [[self alloc] initWithReachability:reachability]; CFRelease(reachability); return manager;&#125; 其中，根据地址创建网络连接的对象 reachability 时，地址的创建在 + (instancetype)manager; 这个类方法的实现里。 1234567891011121314151617+ (instancetype)manager&#123; // 创建零地址，0.0.0.0 的地址表示查询本机的网络连接状态#if (defined(__IPHONE_OS_VERSION_MIN_REQUIRED) &amp;&amp; __IPHONE_OS_VERSION_MIN_REQUIRED &gt;= 90000) || (defined(__MAC_OS_X_VERSION_MIN_REQUIRED) &amp;&amp; __MAC_OS_X_VERSION_MIN_REQUIRED &gt;= 101100) struct sockaddr_in6 address; bzero(&amp;address, sizeof(address)); address.sin6_len = sizeof(address); address.sin6_family = AF_INET6;#else struct sockaddr_in address; bzero(&amp;address, sizeof(address)); address.sin_len = sizeof(address); address.sin_family = AF_INET;#endif return [self managerForAddress:&amp;address];&#125; 接下来是控制网络状态监听开启/关闭的 2 个方法，以及 2 个辅助方法，详见注释。 123456789// 开启监听- (void)startMonitoring;// 关闭监听- (void)stopMonitoring; // 返回一个描述网络状态的本地化字符串，即将不同的网络状态转变成对应的文字描述后输出- (NSString *)localizedNetworkReachabilityStatusString;// 设置网络状态发生变化时的回调，用户自定义，实际会将这个 block 赋值给内部的一个 block 属性，当状态发生改变时执行这个 block。- (void)setReachabilityStatusChangeBlock:(nullable void (^)(AFNetworkReachabilityStatus status))block; 接下来，我们重点研究一下 - (void)startMonitoring; 这个方法，先贴上图和代码，然后我们按照图示顺序依次讨论各个环节。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546- (void)startMonitoring &#123; // 1.停止已经存在的监听 [self stopMonitoring]; if (!self.networkReachability) &#123; return; &#125; // 2.构建 block：AFNetworkReachabilityStatusBlock callback __weak __typeof(self)weakSelf = self; AFNetworkReachabilityStatusBlock callback = ^(AFNetworkReachabilityStatus status) &#123; __strong __typeof(weakSelf)strongSelf = weakSelf; strongSelf.networkReachabilityStatus = status; if (strongSelf.networkReachabilityStatusBlock) &#123; strongSelf.networkReachabilityStatusBlock(status); &#125; &#125;; // 3.构建结构体 SCNetworkReachabilityContext context SCNetworkReachabilityContext context = &#123; 0, (__bridge void *)callback, AFNetworkReachabilityRetainCallback, AFNetworkReachabilityReleaseCallback, NULL &#125;; // 4.设置回调 // AFNetworkReachabilityCallback =&gt; 监听到网络状态变化时需要执行的 block SCNetworkReachabilitySetCallback(self.networkReachability, AFNetworkReachabilityCallback, &amp;context); // 5.启动：将监听任务添加到 mainRunloop 的 CommonModes 里边 SCNetworkReachabilityScheduleWithRunLoop(self.networkReachability, CFRunLoopGetMain(), kCFRunLoopCommonModes); // 测试 dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_BACKGROUND, 0),^&#123; SCNetworkReachabilityFlags flags; // 这个函数用来获得测试连接的状态，第一个参数为之前建立的测试连接的引用，第二个参数用来保存获得的状态，如果能获得状态则返回TRUE，否则返回FALSE if (SCNetworkReachabilityGetFlags(self.networkReachability, &amp;flags)) &#123; AFPostReachabilityStatusChange(flags, callback); &#125; &#125;);&#125; 1.结束之前已经存在的网络状态监听 [self stopMonitoring];。 1234567- (void)stopMonitoring &#123; if (!self.networkReachability) &#123; return; &#125; // Unschedules the specified target from the specified run loop and mode. SCNetworkReachabilityUnscheduleFromRunLoop(self.networkReachability, CFRunLoopGetMain(), kCFRunLoopCommonModes);&#125; 2.创建一个 block (AFNetworkReachabilityStatusBlock)，用于在网络状态发生改变时，以 status 为参数，执行之前用户设置的 block。 12345678910__weak __typeof(self)weakSelf = self; AFNetworkReachabilityStatusBlock callback = ^(AFNetworkReachabilityStatus status) &#123; __strong __typeof(weakSelf)strongSelf = weakSelf; // 为 networkReachabilityStatus 赋新值 strongSelf.networkReachabilityStatus = status; // 执行用户定义的 block if (strongSelf.networkReachabilityStatusBlock) &#123; strongSelf.networkReachabilityStatusBlock(status); &#125; &#125;; 3.创建一个网络状态上下文对象 (结构体)，内含上一步创建的 block，用于下一步设置 callback。 1234567SCNetworkReachabilityContext context = &#123; 0, (__bridge void *)callback, // 第 2 步创建的 block AFNetworkReachabilityRetainCallback, AFNetworkReachabilityReleaseCallback, NULL &#125;; 4.为 networkReachability 设置回调，即当网络状态发生改变的时候，会执行对应的 block。 1SCNetworkReachabilitySetCallback(self.networkReachability, AFNetworkReachabilityCallback, &amp;context); 5.启动监听，即将监听任务添加到 mainRunloop 的 commonModes 里边。至此，启动了对网络状态的监听。12// Schedules the specified network target with the specified run loop and mode.SCNetworkReachabilityScheduleWithRunLoop(self.networkReachability, CFRunLoopGetMain(), kCFRunLoopCommonModes); 当状态发生改变时，首先会执行 AFNetworkReachabilityCallback() 123static void AFNetworkReachabilityCallback(SCNetworkReachabilityRef __unused target, SCNetworkReachabilityFlags flags, void *info) &#123; AFPostReachabilityStatusChange(flags, (__bridge AFNetworkReachabilityStatusBlock)info);&#125; 7.内部又调用了 AFPostReachabilityStatusChange(flags, (__bridge AFNetworkReachabilityStatusBlock)info);，其中 info 就是第 2 步时创建的那个 block。 1234567891011121314151617static void AFPostReachabilityStatusChange(SCNetworkReachabilityFlags flags, AFNetworkReachabilityStatusBlock block) &#123; AFNetworkReachabilityStatus status = AFNetworkReachabilityStatusForFlags(flags); dispatch_async(dispatch_get_main_queue(), ^&#123; // 1.执行 block, 即外边传进来的 info if (block) &#123; block(status); &#125; // 2.发送网络状态改变的通知 NSNotificationCenter *notificationCenter = [NSNotificationCenter defaultCenter]; NSDictionary *userInfo = @&#123; AFNetworkingReachabilityNotificationStatusItem: @(status) &#125;; [notificationCenter postNotificationName:AFNetworkingReachabilityDidChangeNotification object:nil userInfo:userInfo]; &#125;);&#125; 小结这是《AFNetWorking 源码学习笔记》的最后一篇，算是将源码大概看了一遍，不过由于本人所知甚少，而且时间有限，难免出现粗浅错漏之处，后期会及时纠正。 参考SCNetReachablityhttps://www.jianshu.com/p/f7f480a456c8]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>AFNetWorking</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[AFNetWorking 源码学习笔记 ☞ Serialization]]></title>
    <url>%2F2018%2F12%2F17%2FAFNetWorking-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-Serialization%2F</url>
    <content type="text"><![CDATA[前言本文是 AFNetWorking 源码学习笔记的第四篇，将介绍第三部分 – Serialization 目录下的文件：AFURLRequestSerialization(.h/.m) 和 AFURLResponseSerialization(.h/.m) ，仔细观察这些文件发现，AFURLRequestSerialization 和 AFURLResponseSerialization 不仅是文件名，还是 2 个协议的名称。 正文AFURLRequestSerialization(.h/.m)先看 AFURLRequestSerialization.h 文件，这个文件里边共有 2 个协议和 3 个类，他们之间的关系如下： 这里，我们以 上一篇 提到的构造 request 的 2 个方法为主线开始介绍。 123456789101112// 方法一NSMutableURLRequest *request = [self.requestSerializer requestWithMethod:method URLString:[[NSURL URLWithString:URLString relativeToURL:self.baseURL] absoluteString] parameters:parameters error:&amp;serializationError];// 方法二NSMutableURLRequest *request = [self.requestSerializer multipartFormRequestWithMethod:@"POST" URLString:[[NSURL URLWithString:URLString relativeToURL:self.baseURL] absoluteString] parameters:parameters constructingBodyWithBlock:block error:&amp;serializationError]; 先来看看第一类请求方法的实现中构建 MutableURLRequest 用到的方法： 123456789101112131415161718192021222324252627282930313233343536- (NSMutableURLRequest *)requestWithMethod:(NSString *)method URLString:(NSString *)URLString parameters:(id)parameters error:(NSError *__autoreleasing *)error&#123; // 0.检测 method、URLString 是否为空 NSParameterAssert(method); NSParameterAssert(URLString); NSURL *url = [NSURL URLWithString:URLString]; NSParameterAssert(url); // 1.开始创建（系统方法创建） NSMutableURLRequest *mutableRequest = [[NSMutableURLRequest alloc] initWithURL:url]; // 2.设置参数 // 2.1 请求方法 mutableRequest.HTTPMethod = method; // 方法默认是 “GET” // 2.2 为 request 设置其一些默认参数 for (NSString *keyPath in AFHTTPRequestSerializerObservedKeyPaths()) &#123; // AFHTTPRequestSerializerObservedKeyPaths()：AFHTTPRequestSerializer 的 6个属性对应 getter 方法名 string 组成的数组 // self.mutableObservedChangedKeyPaths 中是 值非nil 的属性名 // requstSerializer 初始化时，创建了 self.mutableObservedChangedKeyPaths 这个空 mutableSet，然后，如果设置了对应的参数，它里边就会加上对应的属性名 if ([self.mutableObservedChangedKeyPaths containsObject:keyPath]) &#123; [mutableRequest setValue:[self valueForKeyPath:keyPath] forKey:keyPath]; &#125; &#125; // 3.对参数进行序列化，并赋值给 request mutableRequest = [[self requestBySerializingRequest:mutableRequest withParameters:parameters error:error] mutableCopy]; return mutableRequest;&#125; 该方法来自 AFHTTPRequestSerializer，具体功能见上边的代码注释，简单看一下部分细节，2.2 for 循环的代码块，其中 AFHTTPRequestSerializerObservedKeyPaths() 是一个静态函数，实际返回了当前类 AFHTTPRequestSerializer 中的几个重要的属性名对应的字符串数组，也可以认为是其 get 方法名对应的字符串数组。1234567891011121314static NSArray * AFHTTPRequestSerializerObservedKeyPaths() &#123; static NSArray *_AFHTTPRequestSerializerObservedKeyPaths = nil; static dispatch_once_t onceToken; dispatch_once(&amp;onceToken, ^&#123; _AFHTTPRequestSerializerObservedKeyPaths = @[NSStringFromSelector(@selector(allowsCellularAccess)), NSStringFromSelector(@selector(cachePolicy)), NSStringFromSelector(@selector(HTTPShouldHandleCookies)), NSStringFromSelector(@selector(HTTPShouldUsePipelining)), NSStringFromSelector(@selector(networkServiceType)), NSStringFromSelector(@selector(timeoutInterval))]; &#125;); return _AFHTTPRequestSerializerObservedKeyPaths;&#125; 类的初始化方法里边主要设置了一些 header，然后就是给上边方法提到的几个属性添加了 KVO 监听，当用户给这些属性赋值 (可以为nil)时，就会给 self.mutableObservedChangedKeyPaths 这个 mutableSet 增删元素 (相关代码如下)： 123456789101112131415161718192021// 1. init 方法中给属性添加监听的代码for (NSString *keyPath in AFHTTPRequestSerializerObservedKeyPaths()) &#123; if ([self respondsToSelector:NSSelectorFromString(keyPath)]) &#123; [self addObserver:self forKeyPath:keyPath options:NSKeyValueObservingOptionNew context:AFHTTPRequestSerializerObserverContext]; &#125; &#125;// 2. 触发的监听方法- (void)observeValueForKeyPath:(NSString *)keyPath ofObject:(__unused id)object change:(NSDictionary *)change context:(void *)context&#123; if (context == AFHTTPRequestSerializerObserverContext) &#123; if ([change[NSKeyValueChangeNewKey] isEqual:[NSNull null]]) &#123; [self.mutableObservedChangedKeyPaths removeObject:keyPath]; &#125; else &#123; [self.mutableObservedChangedKeyPaths addObject:keyPath]; &#125; &#125;&#125; 回到 for 循环中的语句，里边 if 语句的目的是将用户设置的属性值赋给 mutableRequest。 然后，来到 3. 序列化参数的时候，用到了这样一个方法 requestBySerializingRequest: withParameters: error:，它属于 AFURLRequestSerialization 这个协议，AFURLRequestSerializer 遵守协议并实现了该方法，注释见下方法注释。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970#pragma mark - AFURLRequestSerialization// 为 request 设置参数- (NSURLRequest *)requestBySerializingRequest:(NSURLRequest *)request withParameters:(id)parameters error:(NSError *__autoreleasing *)error&#123; NSParameterAssert(request); // 0.将 request 转成 mutable，以便修改 NSMutableURLRequest *mutableRequest = [request mutableCopy]; // 1.设置 header 字段，self.HTTPRequestHeaders 是在 init 方法中设置的 useragent 等参数 [self.HTTPRequestHeaders enumerateKeysAndObjectsUsingBlock:^(id field, id value, BOOL * __unused stop) &#123; // 将 self.HTTPRequestHeaders 中的 header 参数设置到 request 里边 if (![request valueForHTTPHeaderField:field]) &#123; [mutableRequest setValue:value forHTTPHeaderField:field]; &#125; &#125;]; // 2.参数序列化 NSString *query = nil; if (parameters) &#123; // 2.1 self.queryStringSerialization 是一个 block，如果从外边 设置了序列化规则 if (self.queryStringSerialization) &#123; NSError *serializationError; query = self.queryStringSerialization(request, parameters, &amp;serializationError); if (serializationError) &#123; if (error) &#123; *error = serializationError; &#125; return nil; &#125; &#125; else &#123; // 2.2 使用AF提供的序列化规则，将 string、array、dictionary 的 parameters 统一转化成了 key1=value1&amp;key2=value2&amp;key3=value3 格式的字符串 switch (self.queryStringSerializationStyle) &#123; case AFHTTPRequestQueryStringDefaultStyle: query = AFQueryStringFromParameters(parameters); break; &#125; &#125; &#125; // 3.设置 request 的 URL 或 HTTPBody 👇 // 初始化时已经设置了 self.HTTPMethodsEncodingParametersInURI = [NSSet setWithObjects:@"GET", @"HEAD", @"DELETE", nil]; if ([self.HTTPMethodsEncodingParametersInURI containsObject:[[request HTTPMethod] uppercaseString]]) &#123; if (query &amp;&amp; query.length &gt; 0) &#123; mutableRequest.URL = [NSURL URLWithString:[[mutableRequest.URL absoluteString] stringByAppendingFormat:mutableRequest.URL.query ? @"&amp;%@" : @"?%@", query]]; &#125; &#125; else &#123; // POST 等其他方法，将 query 设置成 body // #2864: an empty string is a valid x-www-form-urlencoded payload if (!query) &#123; query = @""; &#125; // 如果设置了 body，需要有 Content-Type if (![mutableRequest valueForHTTPHeaderField:@"Content-Type"]) &#123; [mutableRequest setValue:@"application/x-www-form-urlencoded" forHTTPHeaderField:@"Content-Type"]; &#125; // self.stringEncoding 编码方式 [mutableRequest setHTTPBody:[query dataUsingEncoding:self.stringEncoding]]; &#125; return mutableRequest;&#125; 至此，第一种创建 mutableRequest 的主要方法就介绍完了，在讨论第二种创建 MutableURLRequest 的方法实现之前，我们先看看 AFNetWorking 中给出的一个上传数据的示例： 123456789101112// AFNetWorking 中提供的示例NSMutableURLRequest *request = [[AFHTTPRequestSerializer serializer] multipartFormRequestWithMethod:@"POST" URLString:@"http://example.com/upload" parameters:nil constructingBodyWithBlock:^(id&lt;AFMultipartFormData&gt; formData) &#123; [formData appendPartWithFileURL:[NSURL fileURLWithPath:@"file://path/to/image.jpg"] name:@"file" fileName:@"filename.jpg" mimeType:@"image/jpeg" error:nil]; &#125; error:nil]; 关注一下 constructingBodyWithBlock 中的代码就行，其中调用了参数 formData 遵守的一个协议方法 appendPartWithFileURL: ...。下面开始查看创建 MutableURLRequest 的方法实现： 1234567891011121314151617181920212223242526272829303132333435363738394041- (NSMutableURLRequest *)multipartFormRequestWithMethod:(NSString *)method URLString:(NSString *)URLString parameters:(NSDictionary *)parameters constructingBodyWithBlock:(void (^)(id &lt;AFMultipartFormData&gt; formData))block error:(NSError *__autoreleasing *)error&#123; // 0.过滤不符合要求的情况 NSParameterAssert(method); NSParameterAssert(![method isEqualToString:@"GET"] &amp;&amp; ![method isEqualToString:@"HEAD"]); // 1.创建 mutableRequest NSMutableURLRequest *mutableRequest = [self requestWithMethod:method URLString:URLString parameters:nil error:error]; // 2.构建 formData __block AFStreamingMultipartFormData *formData = [[AFStreamingMultipartFormData alloc] initWithURLRequest:mutableRequest stringEncoding:NSUTF8StringEncoding]; if (parameters) &#123; for (AFQueryStringPair *pair in AFQueryStringPairsFromDictionary(parameters)) &#123; NSData *data = nil; if ([pair.value isKindOfClass:[NSData class]]) &#123; data = pair.value; &#125; else if ([pair.value isEqual:[NSNull null]]) &#123; data = [NSData data]; &#125; else &#123; data = [[pair.value description] dataUsingEncoding:self.stringEncoding]; &#125; if (data) &#123; [formData appendPartWithFormData:data name:[pair.field description]]; &#125; &#125; &#125; // 3.执行外部传入的 block if (block) &#123; block(formData); &#125; // 4.返回最终处理完的 request return [formData requestByFinalizingMultipartFormData];&#125; 大概内容见上方代码注释，下面我们讨论一下细节部分。 1.创建 mutableRequest 的方法实现及注释如下： 123456789101112131415161718192021222324252627282930313233343536- (NSMutableURLRequest *)requestWithMethod:(NSString *)method URLString:(NSString *)URLString parameters:(id)parameters error:(NSError *__autoreleasing *)error&#123; // 0.检测 method、URLString 是否为空 NSParameterAssert(method); NSParameterAssert(URLString); NSURL *url = [NSURL URLWithString:URLString]; NSParameterAssert(url); // 1.开始创建（系统方法创建） NSMutableURLRequest *mutableRequest = [[NSMutableURLRequest alloc] initWithURL:url]; // 2.设置参数 // 2.1 请求方法 mutableRequest.HTTPMethod = method; // 方法默认是 “GET” // 2.2 为 request 设置其一些默认参数 for (NSString *keyPath in AFHTTPRequestSerializerObservedKeyPaths()) &#123; // AFHTTPRequestSerializerObservedKeyPaths()：AFHTTPRequestSerializer 的 6个属性对应 getter 方法名 string 组成的数组 // self.mutableObservedChangedKeyPaths 中是 值非nil 的属性名 // requstSerializer 初始化时，创建了 self.mutableObservedChangedKeyPaths 这个空 mutableSet，然后，如果设置了对应的参数，它里边就会加上对应的属性名 if ([self.mutableObservedChangedKeyPaths containsObject:keyPath]) &#123; [mutableRequest setValue:[self valueForKeyPath:keyPath] forKey:keyPath]; &#125; &#125; // 3.对参数进行序列化，与第一种方法调用的是相同的方法，不过多介绍 mutableRequest = [[self requestBySerializingRequest:mutableRequest withParameters:parameters error:error] mutableCopy]; return mutableRequest;&#125; 2.构建 AFStreamingMultipartFormData * 类型的 formData，并为其添加数据。下面是它的几个重要方法： AFStreamingMultipartFormData 是一个用于给 request 设置数据的类。通过初始化方法 initWithURLRequest: stringEncoding: ，将 request 赋值给 AFStreamingMultipartFormData 内部的 copy 属性 request，用于后边的处理及最终返回，。 formData 遵守协议 AFMultipartFormData，当然要实现其中的一些协议方法了，几个重要的方法 (协议方法和非协议方法) 实现见下方代码片段。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758// AFStreamingMultipartFormData : NSObject &lt;AFMultipartFormData&gt;- (instancetype)initWithURLRequest:(NSMutableURLRequest *)urlRequest stringEncoding:(NSStringEncoding)encoding&#123; self = [super init]; if (!self) &#123; return nil; &#125; self.request = urlRequest; self.stringEncoding = encoding; self.boundary = AFCreateMultipartFormBoundary(); self.bodyStream = [[AFMultipartBodyStream alloc] initWithStringEncoding:encoding]; return self;&#125;- (void)appendPartWithFormData:(NSData *)data name:(NSString *)name&#123; NSParameterAssert(name); NSMutableDictionary *mutableHeaders = [NSMutableDictionary dictionary]; [mutableHeaders setValue:[NSString stringWithFormat:@"form-data; name=\"%@\"", name] forKey:@"Content-Disposition"]; [self appendPartWithHeaders:mutableHeaders body:data];&#125;- (void)appendPartWithHeaders:(NSDictionary *)headers body:(NSData *)body&#123; NSParameterAssert(body); AFHTTPBodyPart *bodyPart = [[AFHTTPBodyPart alloc] init]; bodyPart.stringEncoding = self.stringEncoding; bodyPart.headers = headers; bodyPart.boundary = self.boundary; bodyPart.bodyContentLength = [body length]; bodyPart.body = body; [self.bodyStream appendHTTPBodyPart:bodyPart];&#125;- (NSMutableURLRequest *)requestByFinalizingMultipartFormData &#123; if ([self.bodyStream isEmpty]) &#123; return self.request; &#125; // Reset the initial and final boundaries to ensure correct Content-Length [self.bodyStream setInitialAndFinalBoundaries]; [self.request setHTTPBodyStream:self.bodyStream]; [self.request setValue:[NSString stringWithFormat:@"multipart/form-data; boundary=%@", self.boundary] forHTTPHeaderField:@"Content-Type"]; [self.request setValue:[NSString stringWithFormat:@"%llu", [self.bodyStream contentLength]] forHTTPHeaderField:@"Content-Length"]; return self.request;&#125; 我们发现，其中用到了 AFMultipartBodyStream 和 AFHTTPBodyPart 这 2 个类，其中 AFMultipartBodyStream 继承自 NSInputStream，将会作为参数传给 request。而 AFHTTPBodyPart 用于构造分段数据，并传递给 AFMultipartBodyStream。下边是 2 者的部分方法实现： 12345678910111213141516171819202122232425262728293031323334// AFMultipartBodyStream : NSInputStream &lt;NSStreamDelegate&gt;@property (readwrite, nonatomic, strong) NSMutableArray *HTTPBodyParts;// 添加 part- (void)appendHTTPBodyPart:(AFHTTPBodyPart *)bodyPart &#123; [self.HTTPBodyParts addObject:bodyPart];&#125;// 对所有的 part 组成的数组进行处理：第一个 part 的 hasInitialBoundary = YES，最后一个 part 的 hasFinalBoundary = YES，其他 part 的这两个属性s都是 NO。// 这个在计算分段大小的时候会用到- (void)setInitialAndFinalBoundaries &#123; if ([self.HTTPBodyParts count] &gt; 0) &#123; for (AFHTTPBodyPart *bodyPart in self.HTTPBodyParts) &#123; bodyPart.hasInitialBoundary = NO; bodyPart.hasFinalBoundary = NO; &#125; [[self.HTTPBodyParts firstObject] setHasInitialBoundary:YES]; [[self.HTTPBodyParts lastObject] setHasFinalBoundary:YES]; &#125;&#125;// 计算所有分段加起来的总数据- (unsigned long long)contentLength &#123; unsigned long long length = 0; for (AFHTTPBodyPart *bodyPart in self.HTTPBodyParts) &#123; length += [bodyPart contentLength]; &#125; return length;&#125; 123456789101112131415161718192021// AFHTTPBodyPart : NSObject// 计算每一个分段的大小- (unsigned long long)contentLength &#123; unsigned long long length = 0; // hasInitialBoundary 是前边 AFMultipartBodyStream 里的 setInitialAndFinalBoundaries 方法里边设置过的哦 NSData *encapsulationBoundaryData = [([self hasInitialBoundary] ? AFMultipartFormInitialBoundary(self.boundary) : AFMultipartFormEncapsulationBoundary(self.boundary)) dataUsingEncoding:self.stringEncoding]; length += [encapsulationBoundaryData length]; NSData *headersData = [[self stringForHeaders] dataUsingEncoding:self.stringEncoding]; length += [headersData length]; length += _bodyContentLength; // hasFinalBoundary 也是前边设置过的 NSData *closingBoundaryData = ([self hasFinalBoundary] ? [AFMultipartFormFinalBoundary(self.boundary) dataUsingEncoding:self.stringEncoding] : [NSData data]); length += [closingBoundaryData length]; return length;&#125; 3.执行 block ，其实也是在给 formData 添加数据，block 的实现中必须调用 AFMultipartFormData 中的协议方法。 4.返回结果时，调用了 formData 自己的方法 requestByFinalizingMultipartFormData（非协议方法），即为 request 的相关字段赋值，并将其返回。 最后，我们以这些类之间的关系图最为本部分的小结： AFURLResponseSerialization如前文所述，AFURLResponseSerialization 即是协议名称，又是文件名。之前在讨论 NSURLSessionTaskDelegate 的代理方法 - (void)URLSession:(__unused NSURLSession *)session task:(NSURLSessionTask *)task didCompleteWithError:(NSError *)error; 时有这样一行代码： 1234567// AFURLSessionManagerTaskDelegate（Class）// *** 使用 responseSerializer 处理返回结果responseObject = [manager.responseSerializer responseObjectForResponse:task.response data:data error:&amp;serializationError];// AFURLSessionManager（Class）self.responseSerializer = [AFJSONResponseSerializer serializer]; 我们知道，这里的 manager 指的是 AFURLSessionManager，在其 init() 方法中初始化了一个重要属性 responseSerializer。那么 AFURLResponseSerialization 这个文件里边到底有哪些类，他们之间的关系又是怎样的？ 从上图可以发现，这里有一个重要的基类 AFHTTPResponseSerializer，他遵守 AFURLResponseSerialization 这个协议 (只有一个协议方法：- (id)responseObjectForResponse:(NSURLResponse *)response data:(NSData *)data error:(NSError *__autoreleasing *)error;)，其它几个类都是他的子类，他们都各自实现了该协议方法 ，用来处理接口返回的数据。 下面我们从基类 AFHTTPResponseSerializer 开始讨论，下面是它的头文件。 1234567891011121314151617181920@interface AFHTTPResponseSerializer : NSObject &lt;AFURLResponseSerialization&gt;// 编码方式，不过源码中已经指明 It is never used。@property (nonatomic, assign) NSStringEncoding stringEncoding;// 可接受的 HTTP 状态码@property (nonatomic, copy, nullable) NSIndexSet *acceptableStatusCodes;// 可接受的 MIME types@property (nonatomic, copy, nullable) NSSet &lt;NSString *&gt; *acceptableContentTypes;// 创建及初始化方法，创建默认配置的 serializer- (instancetype)init;+ (instancetype)serializer;// 校验接口返回的指定 response 和 data，默认检查了 acceptable status code 和 content type- (BOOL)validateResponse:(nullable NSHTTPURLResponse *)response data:(nullable NSData *)data error:(NSError * _Nullable __autoreleasing *)error;@end 上边 3 个属性中的第 1 个 stringEncoding 已经被标记为过期，即不再对接收到的数据进行解码 (decode)。acceptableStatusCodes 和 acceptableContentTypes 是两个集合（set），依次表示客户端可以接受的响应报文的 HTTP 状态码和 Content-Type，用于后边对响应报文 response 和 data 进行校验。 再来看创建及初始化方法的实现，实际重点在 init 方法中， 123456789101112131415+ (instancetype)serializer &#123; return [[self alloc] init];&#125;- (instancetype)init &#123; self = [super init]; if (!self) &#123; return nil; &#125; self.acceptableStatusCodes = [NSIndexSet indexSetWithIndexesInRange:NSMakeRange(200, 100)]; self.acceptableContentTypes = nil; return self;&#125; 即给 acceptableStatusCodes 和 acceptableContentTypes 赋初值，前者默认为从 200 到 299 之间的整数，及成功的状态码，后者默认为 nil，也就是说接收任何类型 (Content-Type) 的响应数据。 现在来看开头提的解析数据的方法：- (id)responseObjectForResponse: data: error: 12345678910#pragma mark - AFURLResponseSerialization- (id)responseObjectForResponse:(NSURLResponse *)response data:(NSData *)data error:(NSError *__autoreleasing *)error&#123; [self validateResponse:(NSHTTPURLResponse *)response data:data error:error]; return data;&#125; 这其实是 AFURLResponseSerialization 规定的协议方法，首先校验了返回的 response 和 data，如果出错，就将错误信息写入 error。最后将 data 返回。用于校验的方法实现如下： 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657- (BOOL)validateResponse:(NSHTTPURLResponse *)response data:(NSData *)data error:(NSError * __autoreleasing *)error&#123; BOOL responseIsValid = YES; NSError *validationError = nil; // 1.response 是 HTTP 响应报文的情况 if (response &amp;&amp; [response isKindOfClass:[NSHTTPURLResponse class]]) &#123; // 2.ContentType 不符的情况 if (self.acceptableContentTypes &amp;&amp; ![self.acceptableContentTypes containsObject:[response MIMEType]] &amp;&amp; !([response MIMEType] == nil &amp;&amp; [data length] == 0)) &#123; if ([data length] &gt; 0 &amp;&amp; [response URL]) &#123; NSMutableDictionary *mutableUserInfo = [@&#123; NSLocalizedDescriptionKey: [NSString stringWithFormat:NSLocalizedStringFromTable(@"Request failed: unacceptable content-type: %@", @"AFNetworking", nil), [response MIMEType]], NSURLErrorFailingURLErrorKey:[response URL], AFNetworkingOperationFailingURLResponseErrorKey: response, &#125; mutableCopy]; if (data) &#123; mutableUserInfo[AFNetworkingOperationFailingURLResponseDataErrorKey] = data; &#125; validationError = AFErrorWithUnderlyingError([NSError errorWithDomain:AFURLResponseSerializationErrorDomain code:NSURLErrorCannotDecodeContentData userInfo:mutableUserInfo], validationError); &#125; responseIsValid = NO; &#125; // 2.StatusCode 不符的情况 if (self.acceptableStatusCodes &amp;&amp; ![self.acceptableStatusCodes containsIndex:(NSUInteger)response.statusCode] &amp;&amp; [response URL]) &#123; NSMutableDictionary *mutableUserInfo = [@&#123; NSLocalizedDescriptionKey: [NSString stringWithFormat:NSLocalizedStringFromTable(@"Request failed: %@ (%ld)", @"AFNetworking", nil), [NSHTTPURLResponse localizedStringForStatusCode:response.statusCode], (long)response.statusCode], NSURLErrorFailingURLErrorKey:[response URL], AFNetworkingOperationFailingURLResponseErrorKey: response, &#125; mutableCopy]; if (data) &#123; mutableUserInfo[AFNetworkingOperationFailingURLResponseDataErrorKey] = data; &#125; validationError = AFErrorWithUnderlyingError([NSError errorWithDomain:AFURLResponseSerializationErrorDomain code:NSURLErrorBadServerResponse userInfo:mutableUserInfo], validationError); responseIsValid = NO; &#125; &#125; // 3.保存错误信息-有错误信息并且response不符合要求的情况 if (error &amp;&amp; !responseIsValid) &#123; *error = validationError; &#125; // 4.返回 return responseIsValid;&#125; 代码看起来有点长，实际做的事情比较少，首先判断响应的报文是否是 HTTP 的响应报文，如果不是，直接返回 responseIsValid （此时为 YES），如果是 HTTP 报文时才会进行接下来的判断。接着分别判断 StatusCode 和 ContentType 是否符合要求，如果不符合要求，则构建相应的 validationError 信息，并将 responseIsValid 置为 NO。 当 error 存在，并且 responseIsValid 为 NO 时，将 validationError 赋值给 error。最后返回校验结果。 至于 AFHTTPResponseSerializer 的子类，我们就只看一下 AFJSONResponseSerializer，其他子类与之类似，就不多做介绍了。 AFJSONResponseSerializer 是专门用于解析 JSON 格式的响应数据的。他重写了父类的初始化方法，给 acceptableContentTypes 添加了 application/json、text/json、text/javascript 这 3 中 json 相关的格式。 1self.acceptableContentTypes = [NSSet setWithObjects:@"application/json", @"text/json", @"text/javascript", nil]; 同时通过两个工厂方法为属性 readingOptions 赋初值： 12345678910+ (instancetype)serializer &#123; return [self serializerWithReadingOptions:(NSJSONReadingOptions)0];&#125;+ (instancetype)serializerWithReadingOptions:(NSJSONReadingOptions)readingOptions &#123; AFJSONResponseSerializer *serializer = [[self alloc] init]; serializer.readingOptions = readingOptions; return serializer;&#125; 最后，重写了解析数据的方法 (如下)，先是和父类一样校验了 ContentType 和 StatusCode，然后反序列化拿到的响应数据，过滤掉反序列化后的数据中的 value 为 Null 的数据，最后将处理完的数据返回。 12345678910111213141516171819202122232425262728293031323334353637383940#pragma mark - AFURLResponseSerialization- (id)responseObjectForResponse:(NSURLResponse *)response data:(NSData *)data error:(NSError *__autoreleasing *)error&#123; // 1.调用父类方法，校验 ContentType 和 statusCode if (![self validateResponse:(NSHTTPURLResponse *)response data:data error:error]) &#123; if (!error || AFErrorOrUnderlyingErrorHasCodeInDomain(*error, NSURLErrorCannotDecodeContentData, AFURLResponseSerializationErrorDomain)) &#123; return nil; &#125; &#125; // 异常处理 // Workaround for behavior of Rails to return a single space for `head :ok` (a workaround for a bug in Safari), which is not interpreted as valid input by NSJSONSerialization. // See https://github.com/rails/rails/issues/1742 BOOL isSpace = [data isEqualToData:[NSData dataWithBytes:" " length:1]]; if (data.length == 0 || isSpace) &#123; return nil; &#125; NSError *serializationError = nil; // 2.反序列化数据 id responseObject = [NSJSONSerialization JSONObjectWithData:data options:self.readingOptions error:&amp;serializationError]; if (!responseObject) &#123; if (error) &#123; *error = AFErrorWithUnderlyingError(serializationError, *error); &#125; return nil; &#125; // 3.移除 value 为 Null 的 键值对 if (self.removesKeysWithNullValues) &#123; return AFJSONObjectByRemovingKeysWithNullValues(responseObject, self.readingOptions); &#125; return responseObject;&#125; 以上就是构建及序列化请求参数的 AFHTTPRequestSerializer 和 处理响应数据的 AFHTTPResponseSerializer 的主要实现代码，当然还有很多细节，在此就不再多做介绍了。 参考https://blog.csdn.net/tsunamier/article/details/53611811https://blog.csdn.net/tsunamier/article/details/53673751]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>AFNetWorking</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[AFNetWorking 源码学习笔记 ☞ Security]]></title>
    <url>%2F2018%2F12%2F17%2FAFNetWorking-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-Security%2F</url>
    <content type="text"><![CDATA[前言本文是 AFNetWorking 源码学习笔记的第三篇，本篇将介绍第二部分 – Security 目录下的类，其实只有一个就是 AFSecurityPolicy。 正文依照惯例，先从声明文件开始，打开 AFSecurityPolicy.h 文件，我们发现内容非常少，只有 4 个属性和 5 个方法。 首先来看看这几个属性，各自的作用见代码注释。 1234567891011// 根据 SSL 证书验证 server trust 的标准，默认为不验证。@property (readonly, nonatomic, assign) AFSSLPinningMode SSLPinningMode;// 根据 SSL pinning mode 验证 server trust 的证书@property (nonatomic, strong, nullable) NSSet &lt;NSData *&gt; *pinnedCertificates;// 是否允许无效或已经过期的 SSL 证书。@property (nonatomic, assign) BOOL allowInvalidCertificates;// 是否验证证书中的域名，默认为 YES。@property (nonatomic, assign) BOOL validatesDomainName; 然后再来看看它提供的 5 个代理方法， 1+ (NSSet &lt;NSData *&gt; *)certificatesInBundle:(NSBundle *)bundle; 此类方法的作用是获取 Bundle 中的所有证书文件（xxx.cer）存放在 set 里边。 123+ (instancetype)defaultPolicy;+ (instancetype)policyWithPinningMode:(AFSSLPinningMode)pinningMode；+ (instancetype)policyWithPinningMode:(AFSSLPinningMode)pinningMode withPinnedCertificates:(NSSet *)pinnedCertificates; 这 3 个方法都在创建 AFSecurityPolicy 对象，第一个默认将属性 SSLPinningMode 设置为 AFSSLPinningModeNone，即不做验证；第二个可以指定 SSLPinningMode，最后一个还可以指定 pinnedCertificates。 下边这个实例方法负责基于当前安全策略评估传入的 serverTrust 是否可以接受，也就是我们在上一篇中接受认证挑战时掉那个的方法。 12- (BOOL)evaluateServerTrust:(SecTrustRef)serverTrust forDomain:(nullable NSString *)domain; 接下来，我们看看 .m 文件，这里就只介绍其中的两个主要方法实现。第一个是 - (void)setPinnedCertificates:(NSSet *)pinnedCertificates，这里不仅给 _pinnedCertificates 赋值，而且取出了所有证书中的公钥（public key），存放到集合 self.pinnedPublicKeys 里边。 1234567891011121314151617181920- (void)setPinnedCertificates:(NSSet *)pinnedCertificates &#123; // 1.给 _pinnedCertificates 赋值 _pinnedCertificates = pinnedCertificates; // 2.给 self.pinnedPublicKeys 赋值，值是从 Certificates 中提取的 publicKeys if (self.pinnedCertificates) &#123; NSMutableSet *mutablePinnedPublicKeys = [NSMutableSet setWithCapacity:[self.pinnedCertificates count]]; for (NSData *certificate in self.pinnedCertificates) &#123; id publicKey = AFPublicKeyForCertificate(certificate); if (!publicKey) &#123; continue; &#125; [mutablePinnedPublicKeys addObject:publicKey]; &#125; self.pinnedPublicKeys = [NSSet setWithSet:mutablePinnedPublicKeys]; &#125; else &#123; self.pinnedPublicKeys = nil; &#125;&#125; 提取 Certificate 中的 public key 时，利用了 AFPublicKeyForCertificate() 这个静态函数，下面来看看它的具体实现。 12345678910111213141516171819202122232425262728293031323334353637383940static id AFPublicKeyForCertificate(NSData *certificate) &#123; // 1.初始化会用到的局部变量 id allowedPublicKey = nil; SecCertificateRef allowedCertificate; SecPolicyRef policy = nil; SecTrustRef allowedTrust = nil; SecTrustResultType result; // 2.提取公钥 // ① 根据传入的二进制证书数据生成一个 SecCertificateRef 类型的证书，并验证结果 allowedCertificate = SecCertificateCreateWithData(NULL, (__bridge CFDataRef)certificate); __Require_Quiet(allowedCertificate != NULL, _out); // ② 生成一个默认 X.509 policy 的实例 policy = SecPolicyCreateBasicX509(); // ③ 基于证书 (allowedCertificate) 和安全策略 (policy) 创建一个信任管理对象 (a trust management object) __Require_noErr_Quiet(SecTrustCreateWithCertificates(allowedCertificate, policy, &amp;allowedTrust), _out); // ④ 利用步骤 ③ 创建的 allowedTrust 校验证书，并将结果写入 result，不过后边好像没有用到它 __Require_noErr_Quiet(SecTrustEvaluate(allowedTrust, &amp;result), _out); // ⑤ 验证通过后，从 allowedTrust 中获取公钥 allowedPublicKey = (__bridge_transfer id)SecTrustCopyPublicKey(allowedTrust);_out: // 3.释放内存 if (allowedTrust) &#123; CFRelease(allowedTrust); &#125; if (policy) &#123; CFRelease(policy); &#125; if (allowedCertificate) &#123; CFRelease(allowedCertificate); &#125; return allowedPublicKey;&#125; 从上边的代码中我们发现，重点在第 2 步获取公钥的代码，具体内容建代码注释，这里就不啰嗦了。其中用到了系统提供的 2 个宏：__Require_Quiet 和 __Require_noErr_Quiet，下边来看看他们的定义： 12345678910#ifndef __Require_Quiet #define __Require_Quiet(assertion, exceptionLabel) \ do \ &#123; \ if ( __builtin_expect(!(assertion), 0) ) \ &#123; \ goto exceptionLabel; \ &#125; \ &#125; while ( 0 )#endif 12345678910#ifndef __Require_noErr_Quiet #define __Require_noErr_Quiet(errorCode, exceptionLabel) \ do \ &#123; \ if ( __builtin_expect(0 != (errorCode), 0) ) \ &#123; \ goto exceptionLabel; \ &#125; \ &#125; while ( 0 )#endif 从定义可以看出，对于 Require_Quiet，当入参 assertion 为空的时候，会跳转到 exceptionLabel 所在位置去继续执行；而 Require_noErr_Quiet，是当入参 errorCode != 0 即出错的时候会跳转至 exceptionLabel 所在行继续执行。 核心方法下边我们来看看 上一篇 提到过的方法 evaluateServerTrust: forDomain:，该方法是 AFNetWorking 自己提前做的一次 HTTPS 认证，用于验证服务器是否合法，如果验证不通过就不再继续调用系统方法验证了。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374757677787980818283848586878889909192- (BOOL)evaluateServerTrust:(SecTrustRef)serverTrust forDomain:(NSString *)domain&#123; // 1.过滤 // 域名存在 &amp;&amp; 允许自签名证书 &amp;&amp; 需要验证域名 &amp;&amp; (要求不做验证 || 没有证书) if (domain &amp;&amp; self.allowInvalidCertificates &amp;&amp; self.validatesDomainName &amp;&amp; (self.SSLPinningMode == AFSSLPinningModeNone || [self.pinnedCertificates count] == 0)) &#123; NSLog(@"In order to validate a domain name for self signed certificates, you MUST use pinning."); return NO; &#125; // 2.准备安全策略，并将其设置到 serverTrust 里边，以备后边验证使用 NSMutableArray *policies = [NSMutableArray array]; if (self.validatesDomainName) &#123; [policies addObject:(__bridge_transfer id)SecPolicyCreateSSL(true, (__bridge CFStringRef)domain)]; &#125; else &#123; [policies addObject:(__bridge_transfer id)SecPolicyCreateBasicX509()]; &#125; // Sets the policies to use in an evaluation. SecTrustSetPolicies(serverTrust, (__bridge CFArrayRef)policies); // 3.再次过滤 if (self.SSLPinningMode == AFSSLPinningModeNone) &#123; // 不验证的情况下 // 如果允许自签名证书或者 serverTrust 验证成功时，返回 YES return self.allowInvalidCertificates || AFServerTrustIsValid(serverTrust); &#125; else if (!AFServerTrustIsValid(serverTrust) &amp;&amp; !self.allowInvalidCertificates) &#123; // 如果 serverTrust 验证失败 &amp;&amp; 不允许自签名证书，则返回 NO return NO; &#125; // 4.验证 switch (self.SSLPinningMode) &#123; case AFSSLPinningModeNone: /** * 1.不验证 * * 但 紧上边的那个 if-else 语句已经处理了 AFSSLPinningModeNone 的情况，这里还做这个判断有意义吗？难道是是为了看起来完整。 */ default: return NO; case AFSSLPinningModeCertificate: &#123; /** * 2.验证证书 * * 从serverTrust中去获取证书链，然后和我们一开始初始化设置的证书集合self.pinnedCertificates去匹配，如果有一对能匹配成功的，就返回YES，否则NO。 */ NSMutableArray *pinnedCertificates = [NSMutableArray array]; for (NSData *certificateData in self.pinnedCertificates) &#123; [pinnedCertificates addObject:(__bridge_transfer id)SecCertificateCreateWithData(NULL, (__bridge CFDataRef)certificateData)]; &#125; SecTrustSetAnchorCertificates(serverTrust, (__bridge CFArrayRef)pinnedCertificates); if (!AFServerTrustIsValid(serverTrust)) &#123; return NO; &#125; // obtain the chain after being validated, which *should* contain the pinned certificate in the last position (if it's the Root CA) NSArray *serverCertificates = AFCertificateTrustChainForServerTrust(serverTrust); for (NSData *trustChainCertificate in [serverCertificates reverseObjectEnumerator]) &#123; if ([self.pinnedCertificates containsObject:trustChainCertificate]) &#123; return YES; &#125; &#125; return NO; &#125; case AFSSLPinningModePublicKey: &#123; /** * 3.验证公钥 * * 从serverTrust，获取证书链每一个证书的公钥，放到数组中。和我们的self.pinnedPublicKeys，去配对，如果有一个相同的，就返回YES，否则NO。 */ NSUInteger trustedPublicKeyCount = 0; NSArray *publicKeys = AFPublicKeyTrustChainForServerTrust(serverTrust); for (id trustChainPublicKey in publicKeys) &#123; for (id pinnedPublicKey in self.pinnedPublicKeys) &#123; if (AFSecKeyIsEqualToKey((__bridge SecKeyRef)trustChainPublicKey, (__bridge SecKeyRef)pinnedPublicKey)) &#123; trustedPublicKeyCount += 1; &#125; &#125; &#125; return trustedPublicKeyCount &gt; 0; &#125; &#125; return NO;&#125; 代码稍微有点长，不过主要做了这些事： 过滤一些特殊情况：域名存在 &amp;&amp; 允许自签名证书 &amp;&amp; 需要验证域名 &amp;&amp; (要求不做验证 || 没有证书)； 准备安全策略，并将其设置到 serverTrust 里边，以备后边验证使用； 再次过滤：不验证、serverTrust 验证失败 &amp;&amp; 不允许自签名证书 的情况； 开始验证，根据 self.SSLPinningMode 分成了几种情况分别验证： 1.不验证 2.验证证书 3.验证公钥 这一步的验证中又用到了几个静态函数，下边来简单看一下。 验证serverTrust 是否有效，调用了系统方法 SecTrustEvaluate() 进行校验。 1234567891011static BOOL AFServerTrustIsValid(SecTrustRef serverTrust) &#123; BOOL isValid = NO; SecTrustResultType result; __Require_noErr_Quiet(SecTrustEvaluate(serverTrust, &amp;result), _out); // 只有当 "用户未指定验证的策略，即使用系统提供的默认策略" 或者 "要求永远信任" 的时候，才会返回 YES，其他时候都是 NO。 isValid = (result == kSecTrustResultUnspecified || result == kSecTrustResultProceed);_out: return isValid;&#125; 取出 serverTrust 里边的所有证书，将其二进制文件组成的数组返回。 1234567891011static NSArray * AFCertificateTrustChainForServerTrust(SecTrustRef serverTrust) &#123; CFIndex certificateCount = SecTrustGetCertificateCount(serverTrust); NSMutableArray *trustChain = [NSMutableArray arrayWithCapacity:(NSUInteger)certificateCount]; for (CFIndex i = 0; i &lt; certificateCount; i++) &#123; SecCertificateRef certificate = SecTrustGetCertificateAtIndex(serverTrust, i); [trustChain addObject:(__bridge_transfer NSData *)SecCertificateCopyData(certificate)]; &#125; return [NSArray arrayWithArray:trustChain];&#125; 取出 serverTrust 里边的所有证书，然后遍历证书取出其中的 public key，将公钥组成的数组返回。 123456789101112131415161718192021222324252627282930313233static NSArray * AFPublicKeyTrustChainForServerTrust(SecTrustRef serverTrust) &#123; SecPolicyRef policy = SecPolicyCreateBasicX509(); CFIndex certificateCount = SecTrustGetCertificateCount(serverTrust); NSMutableArray *trustChain = [NSMutableArray arrayWithCapacity:(NSUInteger)certificateCount]; for (CFIndex i = 0; i &lt; certificateCount; i++) &#123; SecCertificateRef certificate = SecTrustGetCertificateAtIndex(serverTrust, i); SecCertificateRef someCertificates[] = &#123;certificate&#125;; CFArrayRef certificates = CFArrayCreate(NULL, (const void **)someCertificates, 1, NULL); SecTrustRef trust; __Require_noErr_Quiet(SecTrustCreateWithCertificates(certificates, policy, &amp;trust), _out); SecTrustResultType result; __Require_noErr_Quiet(SecTrustEvaluate(trust, &amp;result), _out); [trustChain addObject:(__bridge_transfer id)SecTrustCopyPublicKey(trust)]; _out: if (trust) &#123; CFRelease(trust); &#125; if (certificates) &#123; CFRelease(certificates); &#125; continue; &#125; CFRelease(policy); return [NSArray arrayWithArray:trustChain];&#125; 小结以上就是安全验证过程中会使用到的类 AFSecurityPolicy 中的主要内容，其他部分本文暂不做过多讨论，以后有时间再更新吧。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>AFNetWorking</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[AFNetWorking 源码学习笔记 ☞ NSURLSession]]></title>
    <url>%2F2018%2F12%2F17%2FAFNetWorking-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-NSURLSession%2F</url>
    <content type="text"><![CDATA[前言本文是 AFNetWorking 源码学习笔记的第二篇，从本篇开始每次介绍四个部分中的一个，按顺序先介绍第一部分 – NSURLSession 目录下的两个关键类 AFHTTPSessionManager 和 AFURLSessionManager。 注：标题容易让人误解，其实指的是 AFNetWorking 源码中的 NSURLSession 目录，而不是真的 NSURLSession 这个类。 正文我们从整个框架对外的最直接接口 AFHTTPSessionManager.h 文件开始查看，此文件主要分三部分： 4个基本属性：①baseURL：顾名思义，基础的 URL ，对于同一个 App，通常情况下，请求 url 的前半部分都是相同的，将这一部分赋值给 baseURL，发送请求时就可以使用相对路径了。②requestSerializer：请求的序列化类，负责拼接参数，及对参数的编码。③responseSerializer：负责对请求结果的处理，④securityPolicy：安全策略类，负责对服务器证书的内部验证，即在 AFNetWorking 内部先进行一次验证。 创建及初始化方法虽然提供了3种看似不同的创建及初始化方法(见下方代码)，实际上前两者只是对后者不同程度的封装，最终都是调用了最后一个参数最多的初始化方法。 AFHTTPSessionManager.h 1234567// 并非单例，从方法名就可以看出来+ (instancetype)manager;- (instancetype)initWithBaseURL:(nullable NSURL *)url;- (instancetype)initWithBaseURL:(nullable NSURL *)url sessionConfiguration:(nullable NSURLSessionConfiguration *)configuration NS_DESIGNATED_INITIALIZER; AFHTTPSessionManager.m 12345678910111213141516171819202122// *** 初始化 最终都调用的是这个方法- (instancetype)initWithBaseURL:(NSURL *)url sessionConfiguration:(NSURLSessionConfiguration *)configuration&#123; // 0.调用父类初始化方法 self = [super initWithSessionConfiguration:configuration]; if (!self) &#123; return nil; &#125; // 1.调整 url, 确保 NSURL 的 +URLWithString:relativeToURL: 方法能够正常调用 if ([[url path] length] &gt; 0 &amp;&amp; ![[url absoluteString] hasSuffix:@"/"]) &#123; url = [url URLByAppendingPathComponent:@""]; &#125; // 2.初始化本类的属性 self.baseURL = url; self.requestSerializer = [AFHTTPRequestSerializer serializer]; self.responseSerializer = [AFJSONResponseSerializer serializer]; return self;&#125; 第一步就是调用父类（AFURLSessionManager）的 initWithSessionConfiguration: 方法初始化，然后才是初始化自己的属性，点开父类方法看看。123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566// *** init 子类初始化时会通过 super 调用😎- (instancetype)initWithSessionConfiguration:(NSURLSessionConfiguration *)configuration &#123; self = [super init]; if (!self) &#123; return nil; &#125; // 初始化 configuration，如果为空，则使用默认，即 defaultSessionConfiguration，默认配置使用的是持久化的硬盘缓存，存储证书到用户 keychain，存储 cookie 到 shareCookie。 if (!configuration) &#123; configuration = [NSURLSessionConfiguration defaultSessionConfiguration]; &#125; self.sessionConfiguration = configuration; // 创建 delegate 所在的操作队列，因为 maxConcurrentOperationCount = 1，所以此处是串行队列，如果 &gt; 1，则为并发队列。 self.operationQueue = [[NSOperationQueue alloc] init]; self.operationQueue.maxConcurrentOperationCount = 1; /** * 初始化 session * 用于后边创建各种 task，这里设置了 delegate、operationQueue 和 sessionConfiguration * 注意1: 这个 delegate 是 readonly，只能通过初始化方法设置； * 注意2: self.operationQueue 默认是串行队列； * 注意3: self.sessionConfiguration 默认取 defaultSessionConfiguration。 */ self.session = [NSURLSession sessionWithConfiguration:self.sessionConfiguration delegate:self delegateQueue:self.operationQueue]; // 用于对接口返回结果的处理 - 下一篇详细说明 self.responseSerializer = [AFJSONResponseSerializer serializer]; // 设置默认的安全策略类 self.securityPolicy = [AFSecurityPolicy defaultPolicy]; // 如果是 Apple Watch，则不需要初始化 reachabilityManager#if !TARGET_OS_WATCH self.reachabilityManager = [AFNetworkReachabilityManager sharedManager];#endif // 存放 task.idenfier 与其 delegate 组成的键值对，用于将系统提供的代理方法转发给其 delegate self.mutableTaskDelegatesKeyedByTaskIdentifier = [[NSMutableDictionary alloc] init]; // 初始化锁 🔐 self.lock = [[NSLock alloc] init]; self.lock.name = AFURLSessionManagerLockName; /** * 理论上，在初始化的时候，应该是还没有创建 task 的。 * * 一个代码贡献者给出的解释是 I believe this for restoring a session from the background. 即 为从后台恢复一个 session 的情况准备的，将其代理的所有回调全都置为 nil。 * 详见：https://github.com/AFNetworking/AFNetworking/issues/3499 */ [self.session getTasksWithCompletionHandler:^(NSArray *dataTasks, NSArray *uploadTasks, NSArray *downloadTasks) &#123; for (NSURLSessionDataTask *task in dataTasks) &#123; [self addDelegateForDataTask:task uploadProgress:nil downloadProgress:nil completionHandler:nil]; &#125; for (NSURLSessionUploadTask *uploadTask in uploadTasks) &#123; [self addDelegateForUploadTask:uploadTask progress:nil completionHandler:nil]; &#125; for (NSURLSessionDownloadTask *downloadTask in downloadTasks) &#123; [self addDelegateForDownloadTask:downloadTask progress:nil destination:nil completionHandler:nil]; &#125; &#125;]; return self;&#125; 核心 - 发送请求的方法细数 AFHTTPSessionManager 中提供的一系列发送请求的方法，可以发现，其实主要可以分为两大类：一类是普通的 GET/POST/HEAD/PUT/PATCH/DELETE 方法，另一类是包含复合结构 request 的 POST 请求方法。 第一类方法下面我们首先从第一类方法开始讨论，主要有以下几个方法，此处做了简化： 123456789101112131415- (nullable NSURLSessionDataTask *)GET: parameters: success: failure: DEPRECATED_ATTRIBUTE- (nullable NSURLSessionDataTask *)GET: parameters: progress: success: failure:- (nullable NSURLSessionDataTask *)HEAD: parameters: success: failure:- (nullable NSURLSessionDataTask *)POST: parameters: success: failure: DEPRECATED_ATTRIBUTE- (nullable NSURLSessionDataTask *)POST: parameters: progress: success: failure:- (nullable NSURLSessionDataTask *)PUT: parameters: success: failure:- (nullable NSURLSessionDataTask *)PATCH: parameters: success: failure:- (nullable NSURLSessionDataTask *)DELETE: parameters: success: failure: 这些方法的实现类似，都是调用了同一个方法创建 task，只不过传入的 method 不同而已，然后 resume 这个 task。如下边的这个 GET 方法所示，其他方法，只是将 GET 换成了对应 POST、HEAD 等等。 12345678910111213141516171819- (NSURLSessionDataTask *)GET:(NSString *)URLString parameters:(id)parameters progress:(void (^)(NSProgress * _Nonnull))downloadProgress success:(void (^)(NSURLSessionDataTask * _Nonnull, id _Nullable))success failure:(void (^)(NSURLSessionDataTask * _Nullable, NSError * _Nonnull))failure&#123; // 1.创建任务 NSURLSessionDataTask *dataTask = [self dataTaskWithHTTPMethod:@"GET" URLString:URLString parameters:parameters uploadProgress:nil downloadProgress:downloadProgress success:success failure:failure]; // 2.启动任务 [dataTask resume]; return dataTask;&#125; 这个创建方法 dataTaskWithHTTPMethod: … 最终做了 2 件事： 123456789101112131415161718192021222324252627282930313233343536373839404142434445// *** 构建 NSURLSessionDataTask- (NSURLSessionDataTask *)dataTaskWithHTTPMethod:(NSString *)method URLString:(NSString *)URLString parameters:(id)parameters uploadProgress:(nullable void (^)(NSProgress *uploadProgress)) uploadProgress downloadProgress:(nullable void (^)(NSProgress *downloadProgress)) downloadProgress success:(void (^)(NSURLSessionDataTask *, id))success failure:(void (^)(NSURLSessionDataTask *, NSError *))failure&#123; // 1. 构建 NSMutableURLRequest，实际调用 requestSerializer 中创建 request 的方法 // 因为 NSURLRequest 的属性都是 readonly，所以此处构建了 NSMutableURLRequest。 NSError *serializationError = nil; NSMutableURLRequest *request = [self.requestSerializer requestWithMethod:method URLString:[[NSURL URLWithString:URLString relativeToURL:self.baseURL] absoluteString] parameters:parameters error:&amp;serializationError]; // 构建失败的处理 if (serializationError) &#123; if (failure) &#123; dispatch_async(self.completionQueue ?: dispatch_get_main_queue(), ^&#123; failure(nil, serializationError); &#125;); &#125; return nil; &#125; // 2. 构建 NSURLSessionDataTask：实际调用父类 AFURLSessionManager 的方法 __block NSURLSessionDataTask *dataTask = nil; dataTask = [self dataTaskWithRequest:request uploadProgress:uploadProgress downloadProgress:downloadProgress completionHandler:^(NSURLResponse * __unused response, id responseObject, NSError *error) &#123; if (error) &#123; if (failure) &#123; failure(dataTask, error); &#125; &#125; else &#123; if (success) &#123; success(dataTask, responseObject); &#125; &#125; &#125;]; return dataTask;&#125; 1.调用 AFHTTPRequestSerializer 的 requestWithMethod: URLString: parameters: error: 方法创建 mutableURLRequest； 2.利用 父类即 AFURLSessionManager 的 dataTaskWithRequest: uploadProgress: downloadProgress: completionHandler: 创建 task（需要上一步得到的 mutableURLRequest 做参数）。 AFHTTPRequestSerializer 中的方法主要做了一些拼接参数及编码的工作，留待下一篇介绍。这里来看看 AFURLSessionManager 中的方法： 12345678910111213141516171819- (NSURLSessionDataTask *)dataTaskWithRequest:(NSURLRequest *)request uploadProgress:(nullable void (^)(NSProgress *uploadProgress)) uploadProgressBlock downloadProgress:(nullable void (^)(NSProgress *downloadProgress)) downloadProgressBlock completionHandler:(nullable void (^)(NSURLResponse *response, id _Nullable responseObject, NSError * _Nullable error))completionHandler &#123; // 1.创建Task(同时修复iOS8以下系统出现的Bug) __block NSURLSessionDataTask *dataTask = nil; url_session_manager_create_task_safely(^&#123; dataTask = [self.session dataTaskWithRequest:request]; &#125;); // 2.为 task 添加代理[self addDelegateForDataTask:dataTask uploadProgress:uploadProgressBlock downloadProgress:downloadProgressBlock completionHandler:completionHandler]; return dataTask;&#125; 这里也做了 2 件事，见上边注释，不过，创建 dataTask 的时候使用了一个 block，点开定义发现，当 iOS 系统版本 ＜ 8.0 时，创建了一个串行队列来执行 block 中的任务。 这是为了防止 iOS8 以前的版本在并发队列上创建任务时，可能会调用错误的 completionHandlers。当任务返回一个重复的 taskIdentifier 时，先前的 completionHandler 被清除并替换为新的。 如果第一个请求的数据在第二个请求的数据之前返回，那么将针对第二个 completionHandler 调用第一个响应。 1234567891011121314151617181920static void url_session_manager_create_task_safely(dispatch_block_t block) &#123; if (NSFoundationVersionNumber &lt; NSFoundationVersionNumber_With_Fixed_5871104061079552_bug) &#123; // Fix of bug // Open Radar:http://openradar.appspot.com/radar?id=5871104061079552 (status: Fixed in iOS8) // Issue about:https://github.com/AFNetworking/AFNetworking/issues/2093 dispatch_sync(url_session_manager_creation_queue(), block); &#125; else &#123; block(); &#125;&#125;static dispatch_queue_t url_session_manager_processing_queue() &#123; static dispatch_queue_t af_url_session_manager_processing_queue; static dispatch_once_t onceToken; dispatch_once(&amp;onceToken, ^&#123; af_url_session_manager_processing_queue = dispatch_queue_create("com.alamofire.networking.session.manager.processing", DISPATCH_QUEUE_CONCURRENT); &#125;); return af_url_session_manager_processing_queue;&#125; 第二类方法第二类方法最终调用的只有一个方法，如下所示： 1234567891011121314151617181920212223242526272829303132333435363738- (NSURLSessionDataTask *)POST:(NSString *)URLString parameters:(id)parameters constructingBodyWithBlock:(void (^)(id &lt;AFMultipartFormData&gt; formData))block progress:(nullable void (^)(NSProgress * _Nonnull))uploadProgress success:(void (^)(NSURLSessionDataTask *task, id responseObject))success failure:(void (^)(NSURLSessionDataTask *task, NSError *error))failure&#123; // 1.创建 request NSError *serializationError = nil; NSMutableURLRequest *request = [self.requestSerializer multipartFormRequestWithMethod:@"POST" URLString:[[NSURL URLWithString:URLString relativeToURL:self.baseURL] absoluteString] parameters:parameters constructingBodyWithBlock:block error:&amp;serializationError]; if (serializationError) &#123; if (failure) &#123; dispatch_async(self.completionQueue ?: dispatch_get_main_queue(), ^&#123; failure(nil, serializationError); &#125;); &#125; return nil; &#125; // 2.创建 task __block NSURLSessionDataTask *task = [self uploadTaskWithStreamedRequest:request progress:uploadProgress completionHandler:^(NSURLResponse * __unused response, id responseObject, NSError *error) &#123; if (error) &#123; if (failure) &#123; failure(task, error); &#125; &#125; else &#123; if (success) &#123; success(task, responseObject); &#125; &#125; &#125;]; // 3.启动任务 [task resume]; return task;&#125; 基本步骤与第一类方法类似，只不过创建 request 和 task 的方法不一样，对于 request 的创建方法留待下一篇介绍，这里我们看看创建 dataTask 的方法有什么不同。 12345678910111213- (NSURLSessionUploadTask *)uploadTaskWithStreamedRequest:(NSURLRequest *)request progress:(void (^)(NSProgress *uploadProgress)) uploadProgressBlock completionHandler:(void (^)(NSURLResponse *response, id responseObject, NSError *error))completionHandler&#123; __block NSURLSessionUploadTask *uploadTask = nil; url_session_manager_create_task_safely(^&#123; uploadTask = [self.session uploadTaskWithStreamedRequest:request]; &#125;); [self addDelegateForUploadTask:uploadTask progress:uploadProgressBlock completionHandler:completionHandler]; return uploadTask;&#125; 原来是其中创建 task 的方法 uploadTaskWithStreamedRequest: 不同，生成的是 NSURLSessionUploadTask 类型，之后设置代理的方法与第一类方法类似。 注意：其实理论上应该还有第 3 类请求方法，即下载的请求，不过 AFHTTPSessionManager 里边并未封装，而只是在它的父类里边提供了创建 downloadTask 的方法，比如下边的。也是创建 task 使用的系统方法不同，其他类似。 1234567891011121314- (NSURLSessionDownloadTask *)downloadTaskWithRequest:(NSURLRequest *)request progress:(void (^)(NSProgress *downloadProgress)) downloadProgressBlock destination:(NSURL * (^)(NSURL *targetPath, NSURLResponse *response))destination completionHandler:(void (^)(NSURLResponse *response, NSURL *filePath, NSError *error))completionHandler&#123; __block NSURLSessionDownloadTask *downloadTask = nil; url_session_manager_create_task_safely(^&#123; downloadTask = [self.session downloadTaskWithRequest:request]; &#125;); [self addDelegateForDownloadTask:downloadTask progress:downloadProgressBlock destination:destination completionHandler:completionHandler]; return downloadTask;&#125; 现在分别介绍完了 2 类发送请求的方法，下边我们看看为 task 添加代理的方法。 12345678910111213141516- (void)addDelegateForDataTask:(NSURLSessionDataTask *)dataTask uploadProgress:(nullable void (^)(NSProgress *uploadProgress)) uploadProgressBlock downloadProgress:(nullable void (^)(NSProgress *downloadProgress)) downloadProgressBlock completionHandler:(void (^)(NSURLResponse *response, id responseObject, NSError *error))completionHandler&#123; AFURLSessionManagerTaskDelegate *delegate = [[AFURLSessionManagerTaskDelegate alloc] initWithTask:dataTask]; delegate.manager = self; delegate.completionHandler = completionHandler; dataTask.taskDescription = self.taskDescriptionForSessionTasks; // 保存 task 和 delegate 的映射关系，并添加对任务开始和暂停的监听 [self setDelegate:delegate forTask:dataTask]; delegate.uploadProgressBlock = uploadProgressBlock; delegate.downloadProgressBlock = downloadProgressBlock;&#125; 这里创建了一个代理对象（AFURLSessionManagerTaskDelegate），在这个 task 中对请求过程进行处理；设置完代理的属性之后，才调用 setDelegate: forTask: 方法来为 task 及代理添加关联。 12345678910111213- (void)setDelegate:(AFURLSessionManagerTaskDelegate *)delegate forTask:(NSURLSessionTask *)task&#123; NSParameterAssert(task); NSParameterAssert(delegate); [self.lock lock]; // 以 taskIdentifier 为 key，将 delegate 存储在一个字典(self.mutableTaskDelegatesKeyedByTaskIdentifier)里 self.mutableTaskDelegatesKeyedByTaskIdentifier[@(task.taskIdentifier)] = delegate; // 添加对任务开始和暂停的监听 [self addNotificationObserverForTask:task]; [self.lock unlock];&#125; 主要就是保存 task 和 代理之间的映射关系，保存在之前已经创建好的字典 mutableTaskDelegatesKeyedByTaskIdentifier 里边，并为 task 添加通知，监听了 task resume 和 suspend 两个状态。然后我们发现当状态发生改变的时候，实际上只是在主队列又发送了 2 中通知，只是改了个名字而已。全局搜索新的通知名后发现，只有在 UIKit 那个目录下的类里边才会用到。 然后看看代理类 AFURLSessionManagerTaskDelegate，从他的初始化方法开始。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748- (instancetype)initWithTask:(NSURLSessionTask *)task &#123; self = [super init]; if (!self) &#123; return nil; &#125; // 1.存放下载的数据 _mutableData = [NSMutableData data]; // 2.创建并初始化上传及下载进度 _uploadProgress 和 _downloadProgress _uploadProgress = [[NSProgress alloc] initWithParent:nil userInfo:nil]; _downloadProgress = [[NSProgress alloc] initWithParent:nil userInfo:nil]; __weak __typeof__(task) weakTask = task; for (NSProgress *progress in @[ _uploadProgress, _downloadProgress ]) &#123; progress.totalUnitCount = NSURLSessionTransferSizeUnknown; progress.cancellable = YES; // 可以取消 progress.cancellationHandler = ^&#123; [weakTask cancel]; &#125;; progress.pausable = YES; // 可以暂停 progress.pausingHandler = ^&#123; [weakTask suspend]; &#125;; // 恢复#if AF_CAN_USE_AT_AVAILABLE if (@available(iOS 9, macOS 10.11, *))#else if ([progress respondsToSelector:@selector(setResumingHandler:)])#endif &#123; progress.resumingHandler = ^&#123; [weakTask resume]; &#125;; &#125; // 给 progress 添加监听 [progress addObserver:self forKeyPath:NSStringFromSelector(@selector(fractionCompleted)) options:NSKeyValueObservingOptionNew context:NULL]; &#125; return self;&#125; 从上边的代码可以看到，就做了 2 件事： 创建存放下载的数据的可变数组 _mutableData； 创建用于存储上传及下载进度的对象 _uploadProgress 和 _downloadProgress，并为其添加 KVO 监听。查看了 KVO 对应的 observeValueForKeyPath: 方法实现后，发现只是将最新的下载或上传进度回传给了对应的 block。当然，会在 dealloc 方法中移除监听，否则会 Crash。 12345678910111213- (void)observeValueForKeyPath:(NSString *)keyPath ofObject:(id)object change:(NSDictionary&lt;NSString *,id&gt; *)change context:(void *)context &#123; if ([object isEqual:self.downloadProgress]) &#123; // 下载进度 if (self.downloadProgressBlock) &#123; self.downloadProgressBlock(object); &#125; &#125; else if ([object isEqual:self.uploadProgress]) &#123; // 上传进度 if (self.uploadProgressBlock) &#123; self.uploadProgressBlock(object); &#125; &#125;&#125; 接下来就是一些代理方法了，他们都是从 AFURLSessionManager 里边的代理方法转发过来的，后边再作介绍吧。 回到 AFURLSessionManager.m 中，前边都是在请求发送之前做的准备工作，现在我们来看看发起请求之后的情况，当然是看代理方法了。 代理方法这里的 15 个代理方法分属 4 个不同的协议：NSURLSessionDelegateNSURLSessionTaskDelegateNSURLSessionDataDelegateNSURLSessionDownloadDelegate 我们依次看看其中实现的协议方法： NSURLSessionDelegate1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253// 当 session 失效时调用- (void)URLSession:(NSURLSession *)sessiondidBecomeInvalidWithError:(NSError *)error&#123; if (self.sessionDidBecomeInvalid) &#123; self.sessionDidBecomeInvalid(session, error); &#125; [[NSNotificationCenter defaultCenter] postNotificationName:AFURLSessionDidInvalidateNotification object:session];&#125;// 如何接受授权挑战，如果此方法未实现，会调用后边 NSURLSessionTaskDelegate 中的第一个代理方法 URLSession:task:willPerformHTTPRedirection:newRequest:completionHandler:- (void)URLSession:(NSURLSession *)sessiondidReceiveChallenge:(NSURLAuthenticationChallenge *)challenge completionHandler:(void (^)(NSURLSessionAuthChallengeDisposition disposition, NSURLCredential *credential))completionHandler&#123; NSURLSessionAuthChallengeDisposition disposition = NSURLSessionAuthChallengePerformDefaultHandling; __block NSURLCredential *credential = nil; // 1.判断有没有自定义 block: sessionDidReceiveAuthenticationChallenge if (self.sessionDidReceiveAuthenticationChallenge) &#123; disposition = self.sessionDidReceiveAuthenticationChallenge(session, challenge, &amp;credential); &#125; else &#123; // 2.如果没有自定义 block，判断 如果服务端要求的认证方法是不是 NSURLAuthenticationMethodServerTrust if ([challenge.protectionSpace.authenticationMethod isEqualToString:NSURLAuthenticationMethodServerTrust]) &#123; // 3.基于客户端的安全策略来决定是否信任该服务器 // *** 点开此方法 if ([self.securityPolicy evaluateServerTrust:challenge.protectionSpace.serverTrust forDomain:challenge.protectionSpace.host]) &#123; credential = [NSURLCredential credentialForTrust:challenge.protectionSpace.serverTrust]; if (credential) &#123; disposition = NSURLSessionAuthChallengeUseCredential; // 使用证书的 Challenge &#125; else &#123; disposition = NSURLSessionAuthChallengePerformDefaultHandling; // 默认 Challenge &#125; &#125; else &#123; disposition = NSURLSessionAuthChallengeCancelAuthenticationChallenge; // 取消 Challenge &#125; &#125; else &#123; disposition = NSURLSessionAuthChallengePerformDefaultHandling; // 默认 Challenge &#125; &#125; // *** 去执行 iOS 系统的认证方案 if (completionHandler) &#123; completionHandler(disposition, credential); &#125;&#125; 对于第一个方法 URLSession: didBecomeInvalidWithError:，当 session 失效时调用此方法。官方文档给出的具体说明是，如果我们调用了 finishTasksAndInvalidate 方法，则会等到 session 里边最后一个 task 执行完，才会调用此代理方法；如果调用了 invalidateAndCancel 这个方法，则会立即执行该代理方法。 第 2 个代理方法是当客户端收到 “授权挑战”（姑且这么翻译吧，虽然很别扭） 时实行的方法，流程如下图： 首先，判断用户(使用者)有没有自定义 block: sessionDidReceiveAuthenticationChallenge，如果有，则执行自定义的 block 处理挑战，返回一个枚举值给 disposition，并给 credential 赋值 (因为参数是 &amp;credential) 处理，他们都将作为最后一步执行 completionhandler() 时的参数。如果没有自定义处理挑战的 block，则接着进行下一步的判断； 然后，判断服务端要求的认证方法是不是 NSURLAuthenticationMethodServerTrust。如果是，则只需要验证服务端证书是否安全（即 https 的单向认证，这是 AFNetworking 默认处理的认证方式，其他的认证方式，只能通过定义 block 来实现）。这种情况下，还需要做下一步处理； 接下来，有一个 if 判断，条件是 securityPolicy 调用 evaluateServerTrust: forDomain: 方法的执行结果，该方法实际是 AFNetWorking 自己对先做的一次 HTTPS 认证，返回结果表明该服务器是否可以信任，具体方法实现见下一篇。 如果 if 条件成立，即 AFNetworking 认为服务器可信任，执行 NSURLCredential 的系统方法 credentialForTrust: 生成一个凭证或称证书 credential，如果成功生成，就给 disposition 赋相应的值。 如果 if 条件不成立，则取消认证。 最后，执行 completionHandler(disposition, credential)，其中，credential 是根据服务端返回的证书及相关信息生成的用于客户端验证的对象；disposition 就是一个枚举值（常量），用于说明应该以何种方式处理凭证 credential。 NSURLSessionTaskDelegate这里以典型的请求结束的代理方法为例做一个简单介绍，代码如下： 123456789101112131415161718- (void)URLSession:(NSURLSession *)session task:(NSURLSessionTask *)taskdidCompleteWithError:(NSError *)error&#123; // 取出 task 对应的 delegate AFURLSessionManagerTaskDelegate *delegate = [self delegateForTask:task]; // 如果是在后台完成的任务，则此处 delegate 为 nil。 if (delegate) &#123; [delegate URLSession:session task:task didCompleteWithError:error]; [self removeDelegateForTask:task]; &#125; // 执行 block if (self.taskDidComplete) &#123; self.taskDidComplete(session, task, error); &#125;&#125; 这是在请求结束时执行的代理方法（可能成功或者失败，如果失败，error有值），调用自己的 delegateForTask: 方法，取出 task 对应的 delegate，如果取到了，则执行 delegate 的 URLSession: task: didCompleteWithError: 方法。 注意，实际我们最开始传到 GET、POST 等方法中的 block 都是赋值给了对应 task 的 delegate，所以真正执行那些 block 也是在转发到的 delegate(AFURLSessionManagerTaskDelegate) 的对应代理方法中，而不是上边代码最后的 block。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475- (void)URLSession:(__unused NSURLSession *)session task:(NSURLSessionTask *)taskdidCompleteWithError:(NSError *)error&#123; __strong AFURLSessionManager *manager = self.manager; __block id responseObject = nil; __block NSMutableDictionary *userInfo = [NSMutableDictionary dictionary]; userInfo[AFNetworkingTaskDidCompleteResponseSerializerKey] = manager.responseSerializer; //Performance Improvement from #2672 NSData *data = nil; if (self.mutableData) &#123; data = [self.mutableData copy]; //We no longer need the reference, so nil it out to gain back some memory. self.mutableData = nil; &#125; if (self.downloadFileURL) &#123; userInfo[AFNetworkingTaskDidCompleteAssetPathKey] = self.downloadFileURL; &#125; else if (data) &#123; userInfo[AFNetworkingTaskDidCompleteResponseDataKey] = data; &#125; if (error) &#123; // 出错的时候 userInfo[AFNetworkingTaskDidCompleteErrorKey] = error; // 出错时，执行完成的回调 及 发送任务结束的通知 dispatch_group_async(manager.completionGroup ?: url_session_manager_completion_group(), manager.completionQueue ?: dispatch_get_main_queue(), ^&#123; if (self.completionHandler) &#123; self.completionHandler(task.response, responseObject, error); &#125; dispatch_async(dispatch_get_main_queue(), ^&#123; [[NSNotificationCenter defaultCenter] postNotificationName:AFNetworkingTaskDidCompleteNotification object:task userInfo:userInfo]; &#125;); &#125;); &#125; else &#123; // 正常结束的时候 dispatch_async(url_session_manager_processing_queue(), ^&#123; NSError *serializationError = nil; // *** 使用 responseSerializer 处理返回结果 responseObject = [manager.responseSerializer responseObjectForResponse:task.response data:data error:&amp;serializationError]; if (self.downloadFileURL) &#123; responseObject = self.downloadFileURL; &#125; if (responseObject) &#123; userInfo[AFNetworkingTaskDidCompleteSerializedResponseKey] = responseObject; &#125; if (serializationError) &#123; userInfo[AFNetworkingTaskDidCompleteErrorKey] = serializationError; &#125; // 正常结束的时候，执行完成的回调 及 发送任务结束的通知 dispatch_group_async(manager.completionGroup ?: url_session_manager_completion_group(), manager.completionQueue ?: dispatch_get_main_queue(), ^&#123; if (self.completionHandler) &#123; self.completionHandler(task.response, responseObject, serializationError); &#125; dispatch_async(dispatch_get_main_queue(), ^&#123; [[NSNotificationCenter defaultCenter] postNotificationName:AFNetworkingTaskDidCompleteNotification object:task userInfo:userInfo]; &#125;); &#125;); &#125;); &#125;&#125; 仔细看了一下，这个方法里边就主要就为了做了 2 件事，第一件是执行 self.completionHandler()，包括请求结果的序列化将会在下一篇介绍；第二件是发送任务完成的通知，包括构建 userInfo 这个可变字典，详见代码。 至于其他协议及代理方法，大部分都是遵循将代理方法转发给 AFURLSessionManagerTaskDelegate 里的对应方法的逻辑，限于篇幅暂时就不做讨论了。 彩蛋忽然想起来，其实 AFNetWorking 的 readme.md 里边给的 4 个示例都是直接使用 AFURLSessionManager 而不是使用它的子类 AFHTTPSessionManager，难道我又说多了，哈哈。 参考NS_UNAVAILABLE 与 NS_DESIGNATED_INITIALIZER进度: NSProgress]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>AFNetWorking</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[AFNetWorking 源码学习笔记 ☞ 主体流程]]></title>
    <url>%2F2018%2F12%2F17%2FAFNetWorking-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-%E4%B8%BB%E4%BD%93%E6%B5%81%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[简介AFNetworking 是使用 Objective-C 开发 iOS App 时首选的第三方网络框架，当然早期还有 ASI 的框架，不过因为长期无人维护，已很少与人使用了。本系列主要是对 AFNetWorking (3.2.1) 源码学习的一个记录，共分 5 篇进行讨论，目录如下： AFNetWorking 源码学习笔记 ☞ 主体流程AFNetWorking 源码学习笔记 ☞ NSURLSessionAFNetWorking 源码学习笔记 ☞ SecurityAFNetWorking 源码学习笔记 ☞ SerializationAFNetWorking 源码学习笔记 ☞ Reachability本文是第一篇，简要介绍一下框架结构和主体流程。 框架结构为了找到一个合适的切入点，首先查看了 github 上的文档，然后根据示例写了一个粗略的 demo，使用 cocoapods 导入了 AFNetWorking 之后，查看其文件结构，如下图所示： 可以看到，除 UIKit 之外，主要分了 4 部分，各部分的主要作用如下： NSURLSession，包括 2 个主要的类，AFHTTPSessionManager 和 AFURLSessionManager，前者提供一些对外接口，它继承自 AFURLSessionManager 这个核心类，主要的工作都在这里，包括创建及启动 task，代理方法的处理等等。 Security，只有一个类 AFSecurityPolicy，代码也不多，负责在系统底层认证 HTTPS 之前，AFNetWorking 自己做一次认证。 Serialization，包含 2 个类，AFURLRequestSerialization 和 AFURLResponseSerialization 分别负责请求参数和接口返回结果的处理。 Reachability，提供了一个监听网络状态的类 AFNetworkReachabilityManager，不太常用，我们一般都是用苹果提供的 Reachability 这个类，然后自己封装一下，不过需要手动将它的 .h/.m 文件导入到工程里。 主体流程本文的 demo 创建了一个简单的网络请求，我们以此作为突破口，开始一步一步研究这个框架的基本流程。下边是自己添加的发送请求的代码。 1234567891011121314151617181920212223242526272829303132333435#import "ViewController.h"#import &lt;AFNetworking/AFNetworking.h&gt;@interface ViewController ()@end@implementation ViewController- (void)viewDidLoad &#123; [super viewDidLoad]; [self startLoadData];&#125;- (void)startLoadData &#123; // 1.拼接参数 NSString *urlString = [[unsplash_ENDPOINT_HOST stringByAppendingString:unsplash_ENDPOINT_POPULAR] stringByAppendingString:unsplash_CONSUMER_KEY_PARAM]; NSUInteger nextPage = 1; NSString *imageSizeParam = @"&amp;image_size=600"; NSString *urlAdditions = [NSString stringWithFormat:@"&amp;page=%lu&amp;per_page=%d%@", (unsigned long)nextPage, 10, imageSizeParam]; NSString *URLString = [urlString stringByAppendingString:urlAdditions]; // 2.发送请求 // *** 点开此处 GET 方法 [[AFHTTPSessionManager manager] GET:URLString parameters:urlAdditions progress:^(NSProgress * _Nonnull downloadProgress) &#123; NSLog(@"+++++&gt; 进行中...：%@", downloadProgress); &#125; success:^(NSURLSessionDataTask * _Nonnull task, id _Nullable responseObject) &#123; NSLog(@"=====&gt; 成功：%@", responseObject); &#125; failure:^(NSURLSessionDataTask * _Nullable task, NSError * _Nonnull error) &#123; if (error) &#123; NSLog(@"-----&gt; 失败原因：%@", error.domain); &#125; &#125;];&#125; 在 GET 方法上 “command + 右键” 跳转到位于 AFHTTPSessionManager.m 文件中的方法实现（见下方代码），做了两件事：①创建 dataTask；②启动任务（使用 resume 可能有感觉有点怪，不过苹果文档给出的解释是，新创建的 task 是处于挂起状态的，需要使用 resume 启动任务）。 1234567891011121314151617181920- (NSURLSessionDataTask *)GET:(NSString *)URLString parameters:(id)parameters progress:(void (^)(NSProgress * _Nonnull))downloadProgress success:(void (^)(NSURLSessionDataTask * _Nonnull, id _Nullable))success failure:(void (^)(NSURLSessionDataTask * _Nullable, NSError * _Nonnull))failure&#123; // 1.创建任务 // *** 点开此方法 NSURLSessionDataTask *dataTask = [self dataTaskWithHTTPMethod:@"GET" URLString:URLString parameters:parameters uploadProgress:nil downloadProgress:downloadProgress success:success failure:failure]; // 2.启动任务 [dataTask resume]; return dataTask;&#125; 我们再点开 dataTaskWithHTTPMethod: 方法，看看他里边究竟做了什么。我们发现其实也差不多做了两件事：①使用 requestSerializer 这个类创建 NSMutableURLRequest 对象；②又是创建 task ，这里将 上一步得到的 request 及下载、上传的 block 作为参数传入下一层创建 task 的方法，该方法位于 AFURLSessionManager 类中，它是 AFHTTPSessionManager 的父类，主要工作都在这个类里边处理。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546- (NSURLSessionDataTask *)dataTaskWithHTTPMethod:(NSString *)method URLString:(NSString *)URLString parameters:(id)parameters uploadProgress:(nullable void (^)(NSProgress *uploadProgress)) uploadProgress downloadProgress:(nullable void (^)(NSProgress *downloadProgress)) downloadProgress success:(void (^)(NSURLSessionDataTask *, id))success failure:(void (^)(NSURLSessionDataTask *, NSError *))failure&#123; // 1.1 构建 NSMutableURLRequest，实际调用 requestSerializer 中创建 request 的方法 // 因为 NSURLRequest 的属性都是 readonly，所以此处构建了 NSMutableURLRequest。 NSError *serializationError = nil; // *** 点开此方法 NSMutableURLRequest *request = [self.requestSerializer requestWithMethod:method URLString:[[NSURL URLWithString:URLString relativeToURL:self.baseURL] absoluteString] parameters:parameters error:&amp;serializationError]; // 构建失败的处理 if (serializationError) &#123; if (failure) &#123; dispatch_async(self.completionQueue ?: dispatch_get_main_queue(), ^&#123; failure(nil, serializationError); &#125;); &#125; return nil; &#125; // 1.2 构建 NSURLSessionDataTask：实际调用父类 AFURLSessionManager 的方法 __block NSURLSessionDataTask *dataTask = nil; // *** 点开此方法 dataTask = [self dataTaskWithRequest:request uploadProgress:uploadProgress downloadProgress:downloadProgress completionHandler:^(NSURLResponse * __unused response, id responseObject, NSError *error) &#123; if (error) &#123; if (failure) &#123; failure(dataTask, error); &#125; &#125; else &#123; if (success) &#123; success(dataTask, responseObject); &#125; &#125; &#125;]; return dataTask;&#125; 发现这里创建 request 和 创建 task 的方法还可继续深入，先点开 requestWithMethod 方法看看，从下边的代码中可以发现，这里主要就是调用系统方法创建 NSMutableURLRequest，然后设置参数并序列化，最后将该 request 返回。 这里说件出糗的事，记得刚学编程的时候曾被人问过 URLRequest 是否可以发起网络请求，还信心满满的回答可以，心想 request 翻译过来可不就是请求吗 😓。简单点说，request 在这里的作用其实就是拼装参数。 123456789101112131415161718192021222324252627282930313233- (NSMutableURLRequest *)requestWithMethod:(NSString *)method URLString:(NSString *)URLString parameters:(id)parameters error:(NSError *__autoreleasing *)error&#123; // 0.检测 method、URLString 是否为空 NSParameterAssert(method); NSParameterAssert(URLString); NSURL *url = [NSURL URLWithString:URLString]; NSParameterAssert(url); // 1.开始创建 NSMutableURLRequest，因为 NSURLRequest 的属性都是 readonly，为了能够修改，只能创建可变的了。 NSMutableURLRequest *mutableRequest = [[NSMutableURLRequest alloc] initWithURL:url]; // 2.设置参数 mutableRequest.HTTPMethod = method; // 为 request 设置其一些默认参数 // AFHTTPRequestSerializerObservedKeyPaths()：AFHTTPRequestSerializer 的 6个属性对应 get特然 的 方法名string 组成的数组 for (NSString *keyPath in AFHTTPRequestSerializerObservedKeyPaths()) &#123; // self.mutableObservedChangedKeyPaths 中是 值非nil 的属性名 // requstSerializer 初始化时，创建了 self.mutableObservedChangedKeyPaths 这个空 mutableSet，然后，如果设置了对应的参数，它里边就会加上对应的属性名 if ([self.mutableObservedChangedKeyPaths containsObject:keyPath]) &#123; [mutableRequest setValue:[self valueForKeyPath:keyPath] forKey:keyPath]; &#125; &#125; // 3.对参数进行序列化，并赋值给 request，另外，设置必要的 header。 mutableRequest = [[self requestBySerializingRequest:mutableRequest withParameters:parameters error:error] mutableCopy]; return mutableRequest;&#125; 再来看看这一层创建 task 的方法: 123456789101112131415161718// 供子类调用- (NSURLSessionDataTask *)dataTaskWithRequest:(NSURLRequest *)request uploadProgress:(nullable void (^)(NSProgress *uploadProgress)) uploadProgressBlock downloadProgress:(nullable void (^)(NSProgress *downloadProgress)) downloadProgressBlock completionHandler:(nullable void (^)(NSURLResponse *response, id _Nullable responseObject, NSError * _Nullable error))completionHandler &#123; // 1.创建Task，同时修复iOS8以下系统出现的一个Bug __block NSURLSessionDataTask *dataTask = nil; url_session_manager_create_task_safely(^&#123; dataTask = [self.session dataTaskWithRequest:request]; &#125;); // 2.为 task 添加代理 // *** 点开此方法 [self addDelegateForDataTask:dataTask uploadProgress:uploadProgressBlock downloadProgress:downloadProgressBlock completionHandler:completionHandler]; return dataTask;&#125; 也做了 2 件事： ①先是 利用 session 创建 task（这是系统方法）。这里用了 self.session，于是我们回到 AFURLSessionManager 的初始化方法里，查看 session 的创建过程，非常简单，只有一句：1self.session = [NSURLSession sessionWithConfiguration:self.sessionConfiguration delegate:self delegateQueue:self.operationQueue]; 这里我只关注 delegate，这说明网络请求过程中的代理方法，将会在 AFURLSessionManager 这个类里边实现。 ②然后 为 task 添加代理。添加代理的方法是将 delegate（AFURLSessionManagerTaskDelegate）和 task 的标识组成的键值对存储在当前对象中的一个字典里边，以备使用，具体实现如下： 12345678910111213141516- (void)addDelegateForDataTask:(NSURLSessionDataTask *)dataTask uploadProgress:(nullable void (^)(NSProgress *uploadProgress)) uploadProgressBlock downloadProgress:(nullable void (^)(NSProgress *downloadProgress)) downloadProgressBlock completionHandler:(void (^)(NSURLResponse *response, id responseObject, NSError *error))completionHandler&#123; AFURLSessionManagerTaskDelegate *delegate = [[AFURLSessionManagerTaskDelegate alloc] initWithTask:dataTask]; delegate.manager = self; delegate.completionHandler = completionHandler; dataTask.taskDescription = self.taskDescriptionForSessionTasks; // 保存 task 和 delegate 的对应关系到一个字典里，并添加对任务开始和暂停的监听 [self setDelegate:delegate forTask:dataTask]; delegate.uploadProgressBlock = uploadProgressBlock; delegate.downloadProgressBlock = downloadProgressBlock;&#125; 请求开始前的准备工作基本已经做完了，现在让我们看看请求发起后都做了什么，当然是看 AFURLSessionManager 里边的实现的代理方法了，因为代理方法较多，这里挑其中最重要的 2 个作简要介绍。 1.如何接受挑战 123456789101112131415161718192021222324252627282930- (void)URLSession:(NSURLSession *)sessiondidReceiveChallenge:(NSURLAuthenticationChallenge *)challenge completionHandler:(void (^)(NSURLSessionAuthChallengeDisposition disposition, NSURLCredential *credential))completionHandler&#123; NSURLSessionAuthChallengeDisposition disposition = NSURLSessionAuthChallengePerformDefaultHandling; __block NSURLCredential *credential = nil; if (self.sessionDidReceiveAuthenticationChallenge) &#123; disposition = self.sessionDidReceiveAuthenticationChallenge(session, challenge, &amp;credential); &#125; else &#123; if ([challenge.protectionSpace.authenticationMethod isEqualToString:NSURLAuthenticationMethodServerTrust]) &#123; if ([self.securityPolicy evaluateServerTrust:challenge.protectionSpace.serverTrust forDomain:challenge.protectionSpace.host]) &#123; credential = [NSURLCredential credentialForTrust:challenge.protectionSpace.serverTrust]; if (credential) &#123; disposition = NSURLSessionAuthChallengeUseCredential; &#125; else &#123; disposition = NSURLSessionAuthChallengePerformDefaultHandling; &#125; &#125; else &#123; disposition = NSURLSessionAuthChallengeCancelAuthenticationChallenge; &#125; &#125; else &#123; disposition = NSURLSessionAuthChallengePerformDefaultHandling; &#125; &#125; if (completionHandler) &#123; completionHandler(disposition, credential); &#125;&#125; 2.请求结束成功或失败的回调 1234567891011121314151617- (void)URLSession:(NSURLSession *)session task:(NSURLSessionTask *)taskdidCompleteWithError:(NSError *)error&#123; AFURLSessionManagerTaskDelegate *delegate = [self delegateForTask:task]; // delegate may be nil when completing a task in the background if (delegate) &#123; [delegate URLSession:session task:task didCompleteWithError:error]; [self removeDelegateForTask:task]; &#125; if (self.taskDidComplete) &#123; self.taskDidComplete(session, task, error); &#125;&#125; 转发给了代理 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768#pragma mark - NSURLSessionTaskDelegate- (void)URLSession:(__unused NSURLSession *)session task:(NSURLSessionTask *)taskdidCompleteWithError:(NSError *)error&#123; __strong AFURLSessionManager *manager = self.manager; __block id responseObject = nil; __block NSMutableDictionary *userInfo = [NSMutableDictionary dictionary]; userInfo[AFNetworkingTaskDidCompleteResponseSerializerKey] = manager.responseSerializer; //Performance Improvement from #2672 NSData *data = nil; if (self.mutableData) &#123; data = [self.mutableData copy]; //We no longer need the reference, so nil it out to gain back some memory. self.mutableData = nil; &#125; if (self.downloadFileURL) &#123; userInfo[AFNetworkingTaskDidCompleteAssetPathKey] = self.downloadFileURL; &#125; else if (data) &#123; userInfo[AFNetworkingTaskDidCompleteResponseDataKey] = data; &#125; if (error) &#123; userInfo[AFNetworkingTaskDidCompleteErrorKey] = error; dispatch_group_async(manager.completionGroup ?: url_session_manager_completion_group(), manager.completionQueue ?: dispatch_get_main_queue(), ^&#123; if (self.completionHandler) &#123; self.completionHandler(task.response, responseObject, error); &#125; dispatch_async(dispatch_get_main_queue(), ^&#123; [[NSNotificationCenter defaultCenter] postNotificationName:AFNetworkingTaskDidCompleteNotification object:task userInfo:userInfo]; &#125;); &#125;); &#125; else &#123; dispatch_async(url_session_manager_processing_queue(), ^&#123; NSError *serializationError = nil; responseObject = [manager.responseSerializer responseObjectForResponse:task.response data:data error:&amp;serializationError]; if (self.downloadFileURL) &#123; responseObject = self.downloadFileURL; &#125; if (responseObject) &#123; userInfo[AFNetworkingTaskDidCompleteSerializedResponseKey] = responseObject; &#125; if (serializationError) &#123; userInfo[AFNetworkingTaskDidCompleteErrorKey] = serializationError; &#125; dispatch_group_async(manager.completionGroup ?: url_session_manager_completion_group(), manager.completionQueue ?: dispatch_get_main_queue(), ^&#123; if (self.completionHandler) &#123; self.completionHandler(task.response, responseObject, serializationError); &#125; dispatch_async(dispatch_get_main_queue(), ^&#123; [[NSNotificationCenter defaultCenter] postNotificationName:AFNetworkingTaskDidCompleteNotification object:task userInfo:userInfo]; &#125;); &#125;); &#125;); &#125;&#125; 小结至此，我们大概了解了一下 AFNetWorking 工作的主体流程，简单画了个图总结一下，当然略去了很多细节，不过都将会在后边的章节继续讨论。 Demo及源码注释HHAFNetworkingStudy]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>AFNetWorking</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ SDImageCache]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-SDImageCache%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 5 篇，也是最后一篇，主要讨论处理缓存的类 SDImageCache 及相关类 SDMemoryCache 、SDImageCacheConfig 等。 正文2 个缓存相关的枚举先介绍 SDImageCache.h 中定义的 2 个缓存相关的枚举：SDImageCacheType 和 SDImageCacheOptions。 1234567891011121314151617typedef NS_ENUM(NSInteger, SDImageCacheType) &#123; // 不缓存，从网络下载数据 SDImageCacheTypeNone, // 磁盘缓存 SDImageCacheTypeDisk, // 内存缓存 SDImageCacheTypeMemory&#125;;typedef NS_OPTIONS(NSUInteger, SDImageCacheOptions) &#123; // 即使内存中有缓存，也要强制查询磁盘缓存 SDImageCacheQueryDataWhenInMemory = 1 &lt;&lt; 0, // 强制同步查询磁盘缓存 SDImageCacheQueryDiskSync = 1 &lt;&lt; 1, // 压缩大图 SDImageCacheScaleDownLargeImages = 1 &lt;&lt; 2&#125;; 几个重要属性：1234567891011121314151617/// *** 缓存配置信息 ***@property (nonatomic, nonnull, readonly) SDImageCacheConfig *config;/// 内存缓存的最大消耗@property (assign, nonatomic) NSUInteger maxMemoryCost;/// 内存缓存的最大缓存数量@property (assign, nonatomic) NSUInteger maxMemoryCountLimit;/// *** 内存缓存 ***@property (strong, nonatomic, nonnull) SDMemoryCache *memCache;/// 磁盘缓存路径@property (strong, nonatomic, nonnull) NSString *diskCachePath;///@property (strong, nonatomic, nullable) NSMutableArray&lt;NSString *&gt; *customPaths;/// 读写操作的串行队列@property (strong, nonatomic, nullable) dispatch_queue_t ioQueue;/// 用于操作文件的 fileManager@property (strong, nonatomic, nonnull) NSFileManager *fileManager; 其中 2 个属性需要重点关注一下： ① config 所属类 SDImageCacheConfig 定义了很短属性，只提供了一个init方法，在里边给所有属性付了初值，详见下方法代码： 1234567891011121314151617181920static const NSInteger kDefaultCacheMaxCacheAge = 60 * 60 * 24 * 7; // 1 week@implementation SDImageCacheConfig- (instancetype)init &#123; if (self = [super init]) &#123; _shouldDecompressImages = YES; _shouldDisableiCloud = YES; _shouldCacheImagesInMemory = YES; _shouldUseWeakMemoryCache = YES; _diskCacheReadingOptions = 0; _diskCacheWritingOptions = NSDataWritingAtomic; _maxCacheAge = kDefaultCacheMaxCacheAge; _maxCacheSize = 0; _diskCacheExpireType = SDImageCacheConfigExpireTypeModificationDate; &#125; return self;&#125;@end ② memCache 它属于一个继承自 NSCache 的缓存类 SDMemoryCache， 他有一个关键属性： 12// weakCache 是 NSMapTable 类型@property (nonatomic, strong, nonnull) NSMapTable&lt;KeyType, ObjectType&gt; *weakCache; 下面观察一下 SDMemoryCache 的初始化及相关代码： 1234567891011121314151617181920212223242526- (instancetype)initWithConfig:(SDImageCacheConfig *)config &#123; self = [super init]; if (self) &#123; self.weakCache = [[NSMapTable alloc] initWithKeyOptions:NSPointerFunctionsStrongMemory valueOptions:NSPointerFunctionsWeakMemory capacity:0]; // 其他省略 ... [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(didReceiveMemoryWarning:) name:UIApplicationDidReceiveMemoryWarningNotification object:nil]; &#125; return self;&#125;- (void)didReceiveMemoryWarning:(NSNotification *)notification &#123; // 注意：此处是调用的 suoper 方法，所以并没有移除 weak cache，如果是调用 self 重写的 removeAllObjects 方法，就会移除 weak cache。 [super removeAllObjects];&#125;- (void)dealloc &#123; [[NSNotificationCenter defaultCenter] removeObserver:self name:UIApplicationDidReceiveMemoryWarningNotification object:nil];&#125; 当遇到内存警告的时候，缓存会被清除，但 weak cache 并不会被移除，如果手动清除的话，weak cache 当然会被移除。 这里 value 设置成 weak 可以避免可能的循环引用，虽然是 weak，不过，image 实例可以被其他对象持有，像 imageView，这种情况下，value 就不是 nil。 方法好了，我们还是切回来继续讨论 SDImageCache 提供的方法吧！ 首先，是 SDImageCache 的创建方法，它给我们提供了一个单例方法 + (nonnull instancetype)sharedImageCache，下边是他的方法实现： 12345678+ (nonnull instancetype)sharedImageCache &#123; static dispatch_once_t once; static id instance; dispatch_once(&amp;once, ^&#123; instance = [self new]; &#125;); return instance;&#125; 然后，看看初始化方法，我们发现所有初始化方法最后均调用了同一个核心方法 - (nonnull instancetype)initWithNamespace: diskCacheDirectory:，具体作用见下方代码注释。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950- (instancetype)init &#123; return [self initWithNamespace:@"default"];&#125;- (nonnull instancetype)initWithNamespace:(nonnull NSString *)ns &#123; NSString *path = [self makeDiskCachePath:ns]; return [self initWithNamespace:ns diskCacheDirectory:path];&#125;- (nonnull instancetype)initWithNamespace:(nonnull NSString *)ns diskCacheDirectory:(nonnull NSString *)directory &#123; if ((self = [super init])) &#123; NSString *fullNamespace = [@"com.hackemist.SDWebImageCache." stringByAppendingString:ns]; // 创建一个 IO 串行队列 (依次执行操作) _ioQueue = dispatch_queue_create("com.hackemist.SDWebImageCache", DISPATCH_QUEUE_SERIAL); // 初始化内存缓存 _config = [[SDImageCacheConfig alloc] init]; _memCache = [[SDMemoryCache alloc] initWithConfig:_config]; _memCache.name = fullNamespace; // 初始化磁盘缓存路径 if (directory != nil) &#123; _diskCachePath = [directory stringByAppendingPathComponent:fullNamespace]; &#125; else &#123; NSString *path = [self makeDiskCachePath:ns]; _diskCachePath = path; &#125; dispatch_sync(_ioQueue, ^&#123; self.fileManager = [NSFileManager new]; &#125;);#if SD_UIKIT // App 即将关闭的时候，清除过期缓存 [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(deleteOldFiles) name:UIApplicationWillTerminateNotification object:nil]; // App 即将进入后台的时候，清除过期缓存 [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(backgroundDeleteOldFiles) name:UIApplicationDidEnterBackgroundNotification object:nil];#endif &#125; return self;&#125; 记得 上一篇 介绍 SDWebImageManager 的时候，是这样使用 imageCache 的： 123456operation.cacheOperation = [self.imageCache queryCacheOperationForKey:key options:cacheOptions done:^(UIImage *cachedImage, NSData *cachedData, SDImageCacheType cacheType)&#123; // 查询完成后的操作在这里，可能查到了，也可能没查到 ...&#125; 现在，我们就来揭开这个方法的什么面纱。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778- (nullable NSOperation *)queryCacheOperationForKey:(nullable NSString *)key options:(SDImageCacheOptions)options done:(nullable SDCacheQueryCompletedBlock)doneBlock&#123; // 1.校验参数 if (!key) &#123; if (doneBlock) &#123; doneBlock(nil, nil, SDImageCacheTypeNone); &#125; return nil; &#125; // 2.查询内存缓存 (NSCache) UIImage *image = [self imageFromMemoryCacheForKey:key]; BOOL shouldQueryMemoryOnly = (image &amp;&amp; !(options &amp; SDImageCacheQueryDataWhenInMemory)); if (shouldQueryMemoryOnly) &#123; if (doneBlock) &#123; doneBlock(image, nil, SDImageCacheTypeMemory); &#125; return nil; &#125; NSOperation *operation = [NSOperation new]; // 3.将获取缓存及解压的 ‘耗时’ 操作封装成一个 block void(^queryDiskBlock)(void) = ^&#123; // 如果已经取消，不作任何处理，直接返回。 if (operation.isCancelled) &#123; return; &#125; @autoreleasepool &#123; NSData *diskData = [self diskImageDataBySearchingAllPathsForKey:key]; UIImage *diskImage; SDImageCacheType cacheType = SDImageCacheTypeDisk; if (image) &#123; // A &gt; 从 memery 取的 diskImage = image; cacheType = SDImageCacheTypeMemory; &#125; else if (diskData) &#123; // B &gt; 如果内存没有，但是从 disc 取到了，需要解压 diskImage = [self diskImageForKey:key data:diskData options:options]; if (diskImage &amp;&amp; self.config.shouldCacheImagesInMemory) &#123; // 缓存到内存 NSUInteger cost = SDCacheCostForImage(diskImage); // 计算大小 [self.memCache setObject:diskImage forKey:key cost:cost]; &#125; &#125; if (doneBlock) &#123; if (options &amp; SDImageCacheQueryDiskSync) &#123; // 同步执行完成回调 doneBlock(diskImage, diskData, cacheType); &#125; else &#123; // 异步执行完成回调 dispatch_async(dispatch_get_main_queue(), ^&#123; doneBlock(diskImage, diskData, cacheType); &#125;); &#125; &#125; &#125; &#125;; // 4.执行查询磁盘缓存的 block if (options &amp; SDImageCacheQueryDiskSync) &#123; queryDiskBlock(); &#125; else &#123; dispatch_async(self.ioQueue, queryDiskBlock); &#125; return operation;&#125; 如上边的代码所示，主要分了这么 4 步： ① 校验参数 — 如果 key 不存在，直接 doneBlock，返回 nil。 ② 查询内存缓存 NSCache — 如果内存中有，并且没有强制要求必须查询磁盘，则 执行 doneBlock，将 image 返回。 ③ 将获取缓存及解压的 ‘耗时’ 操作封装成一个 block — 这是为了最后执行异步操作的方便。 ④ 执行查询磁盘缓存的 block — 如果设置了 SDImageCacheQueryDiskSync，则同步执行；否则，默认是异步执行。 第 ③ 步中查询磁盘缓存的 queryDiskBlock 里边有两个比较重要的方法：- (nullable NSData *)diskImageDataBySearchingAllPathsForKey: 和 - (nullable UIImage *)diskImageForKey: data: options:，下边分别了解一下这两个方法： diskImageDataBySearchingAllPathsForKey: 这个方法用于查询磁盘缓存，实现及代码注释如下，拼接缓存路径的方法就不展开了，其中文件名的生成是对传入的 key 执行了一次 MD5。 12345678910111213141516171819202122232425262728293031- (nullable NSData *)diskImageDataBySearchingAllPathsForKey:(nullable NSString *)key &#123; // 1.尝试 通过默认路径查询磁盘缓存 NSString *defaultPath = [self defaultCachePathForKey:key]; NSData *data = [NSData dataWithContentsOfFile:defaultPath options:self.config.diskCacheReadingOptions error:nil]; if (data) &#123; return data; &#125; // 2.异常情况的处理：更换了路径再取一次，新路径是将默认路径的后缀去掉 (如果有的话) data = [NSData dataWithContentsOfFile:defaultPath.stringByDeletingPathExtension options:self.config.diskCacheReadingOptions error:nil]; if (data) &#123; return data; &#125; // 3.遍历所有用户自定义的路径，执行类似 1、2 的操作，查询磁盘缓存 NSArray&lt;NSString *&gt; *customPaths = [self.customPaths copy]; for (NSString *path in customPaths) &#123; NSString *filePath = [self cachePathForKey:key inPath:path]; NSData *imageData = [NSData dataWithContentsOfFile:filePath options:self.config.diskCacheReadingOptions error:nil]; if (imageData) &#123; return imageData; &#125; imageData = [NSData dataWithContentsOfFile:filePath.stringByDeletingPathExtension options:self.config.diskCacheReadingOptions error:nil]; if (imageData) &#123; return imageData; &#125; &#125; // 没查到的话，返回 nil return nil;&#125; diskImageForKey: data: options: 此方法的作用是对从 Disc 直接取的 data，进行 解码、解压操作，实现代码如下。 12345678910111213141516171819- (nullable UIImage *)diskImageForKey:(nullable NSString *)key data:(nullable NSData *)data options:(SDImageCacheOptions)options &#123; if (data) &#123; // 1.解码 UIImage *image = [[SDWebImageCodersManager sharedInstance] decodedImageWithData:data]; image = [self scaledImageForKey:key image:image]; // 2.解压 if (self.config.shouldDecompressImages) &#123; BOOL shouldScaleDown = options &amp; SDImageCacheScaleDownLargeImages; image = [[SDWebImageCodersManager sharedInstance] decompressedImageWithImage:image data:&amp;data options:@&#123;SDWebImageCoderScaleDownLargeImagesKey: @(shouldScaleDown)&#125;]; &#125; return image; &#125; else &#123; return nil; &#125;&#125; 此方法分别使用了 SDWebImageCodersManager 的 2 个重要方法： 解码方法 - (UIImage *)decodedImageWithData:data，其中 coder 可以理解为一个解码器，SDWebImage 提供了多种 coder，如 SDWebImageIOCoder、SDWebImageGIFCoder 分别用于解码某一种类型的图片，如果新增一种图片，可以将对应的 coder（需遵守协议：SDWebImageCoder） 添加到 coders 里即可。 123456789101112131415// SDWebImageCodersManager.m- (UIImage *)decodedImageWithData:(NSData *)data &#123; LOCK(self.codersLock); NSArray&lt;id&lt;SDWebImageCoder&gt;&gt; *coders = self.coders; UNLOCK(self.codersLock); for (id&lt;SDWebImageCoder&gt; coder in coders.reverseObjectEnumerator) &#123; if ([coder canDecodeFromData:data]) &#123; return [coder decodedImageWithData:data]; &#125; &#125; return nil;&#125; 解压方法 - (UIImage *)decompressedImageWithImage:image data:data options:optionsDict，这个方法和上边的解码方法都属于 SDWebImageCoder 这个协议，与解码方法类似，也是针对不同类型的 image 有不同的 coder。 1234567891011121314151617181920// SDWebImageCodersManager.m- (UIImage *)decompressedImageWithImage:(UIImage *)image data:(NSData *__autoreleasing _Nullable *)data options:(nullable NSDictionary&lt;NSString*, NSObject*&gt;*)optionsDict &#123; if (!image) &#123; return nil; &#125; LOCK(self.codersLock); NSArray&lt;id&lt;SDWebImageCoder&gt;&gt; *coders = self.coders; UNLOCK(self.codersLock); for (id&lt;SDWebImageCoder&gt; coder in coders.reverseObjectEnumerator) &#123; if ([coder canDecodeFromData:*data]) &#123; UIImage *decompressedImage = [coder decompressedImageWithImage:image data:data options:optionsDict]; decompressedImage.sd_imageFormat = image.sd_imageFormat; return decompressedImage; &#125; &#125; return nil;&#125; 那么，这些 coder 是什么时候加进去的，又是怎么添加的呢？其实，这些逻辑都在 SDWebImageCodersManager 的实现代码里。 1234567891011121314// SDWebImageCodersManager.m- (instancetype)init &#123; if (self = [super init]) &#123; // 初始化 coders NSMutableArray&lt;id&lt;SDWebImageCoder&gt;&gt; *mutableCoders = [@[[SDWebImageImageIOCoder sharedCoder]] mutableCopy];#ifdef SD_WEBP [mutableCoders addObject:[SDWebImageWebPCoder sharedCoder]];#endif _coders = [mutableCoders copy]; _codersLock = dispatch_semaphore_create(1); &#125; return self;&#125; 从初始化方法可以看出来，此时只给 coders 添加了一种 coder，即 SDWebImageImageIOCoder，它是用来对普通的 JPG、PNG 等图片解码的。 为了支持对其他类型图片（如 GIF）的解码，manager 给我们提供了下边这个添加 coder 的方法，代码逻辑很简单，就不多做解释了。 123456789101112131415// SDWebImageCodersManager.m- (void)addCoder:(nonnull id&lt;SDWebImageCoder&gt;)coder &#123; if (![coder conformsToProtocol:@protocol(SDWebImageCoder)]) &#123; return; &#125; LOCK(self.codersLock); NSMutableArray&lt;id&lt;SDWebImageCoder&gt;&gt; *mutableCoders = [self.coders mutableCopy]; if (!mutableCoders) &#123; mutableCoders = [NSMutableArray array]; &#125; [mutableCoders addObject:coder]; self.coders = [mutableCoders copy]; UNLOCK(self.codersLock);&#125; 小结关于缓存相关的类暂时就先介绍到这里，当然还有很多细节没来得及讨论，不过可以查看 demo 中的注释。 到此，关于 SDWebImage 的源码学习就告一段落了，目前的理解可能有点肤浅，以后随着理解的深入，会不定时的更新。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ SDWebImageDownloader]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-SDWebImageDownloader%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 4 篇，本篇将主要介绍 SDWebImageDownloader 这个负责下载的类，当然还有一些相关类及协议，如： SDWebImageDownloadToken、SDWebImageDownloaderOperation 和 SDWebImageDownloaderOperationInterface 等。 正文开启正文描述之前，依旧先看 2 个重要的枚举：SDWebImageDownloaderOptions 和 SDWebImageDownloaderExecutionOrder，具体含义见下方代码注释。 1234567891011121314151617181920212223242526272829// 控制下载过程的选项typedef NS_OPTIONS(NSUInteger, SDWebImageDownloaderOptions) &#123; // 降低下载任务在队列中的优先级 SDWebImageDownloaderLowPriority = 1 &lt;&lt; 0, // 图片将在下载过程中逐步展示，而不是等下载完成后才一次性展示 SDWebImageDownloaderProgressiveDownload = 1 &lt;&lt; 1, // 使用 NSURLCache，默认是不使用的 SDWebImageDownloaderUseNSURLCache = 1 &lt;&lt; 2, // 如果图片是从 NSURLCache 读取的，那么执行 completionHandler 的时候，回传 nil SDWebImageDownloaderIgnoreCachedResponse = 1 &lt;&lt; 3, // 后台继续执行任务 SDWebImageDownloaderContinueInBackground = 1 &lt;&lt; 4, // 允许处理存储在 NSHTTPCookieStore 中的 Cookie SDWebImageDownloaderHandleCookies = 1 &lt;&lt; 5, // 允许不受信任的 SSL 证书，生产环境慎用 SDWebImageDownloaderAllowInvalidSSLCertificates = 1 &lt;&lt; 6, // 提高下载任务在队列中的优先级 SDWebImageDownloaderHighPriority = 1 &lt;&lt; 7, // 缩放大图 SDWebImageDownloaderScaleDownLargeImages = 1 &lt;&lt; 8,&#125;;// 任务的执行顺序typedef NS_ENUM(NSInteger, SDWebImageDownloaderExecutionOrder) &#123; // 先进先出，也是队列的默认执行顺序 SDWebImageDownloaderFIFOExecutionOrder, // 后进先出，栈的执行顺序 SDWebImageDownloaderLIFOExecutionOrder&#125;; 接下来就到了 SDWebImageDownloader 这个类，我不准备一个属性一个方法地按顺序讨论，而是先说创建方法，然后通过一个主要方法将主体串起来。先看创建方法吧！ 对外其实只公开了一个创建单例的方法 sharedDownloader，仔细查看代码会发现，最终调用的是 - (nonnull instancetype)initWithSessionConfiguration: 这个初始化方法，主要做一些初始化工作，并创建一个新 session。如果使用单例方法创建 downloader，则只会有一个 session，而如果通过其他方法创建，则可能创建 session 之前已经有一个了，这时候就需要先将之前的 cancel 之后再创建新的。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263+ (nonnull instancetype)sharedDownloader &#123; static dispatch_once_t once; static id instance; dispatch_once(&amp;once, ^&#123; instance = [self new]; &#125;); return instance;&#125;- (nonnull instancetype)init &#123; return [self initWithSessionConfiguration:[NSURLSessionConfiguration defaultSessionConfiguration]];&#125;- (nonnull instancetype)initWithSessionConfiguration:(nullable NSURLSessionConfiguration *)sessionConfiguration &#123; if ((self = [super init])) &#123; // 执行下载任务的 operation _operationClass = [SDWebImageDownloaderOperation class]; // 要求解压图片 _shouldDecompressImages = YES; // 执行顺序，先进先出 _executionOrder = SDWebImageDownloaderFIFOExecutionOrder; // 设置下载操作的队列，由于最大并发数是 6，所以此 queue 是 并发队列，如果是 1，则为串行队列。 _downloadQueue = [NSOperationQueue new]; _downloadQueue.maxConcurrentOperationCount = 6; _downloadQueue.name = @"com.hackemist.SDWebImageDownloader"; _URLOperations = [NSMutableDictionary new]; // 请求头的字段，可接受的文件类型#ifdef SD_WEBP _HTTPHeaders = [@&#123;@"Accept": @"image/webp,image/*;q=0.8"&#125; mutableCopy];#else _HTTPHeaders = [@&#123;@"Accept": @"image/*;q=0.8"&#125; mutableCopy];#endif // 锁，这里使用了信号量 _operationsLock = dispatch_semaphore_create(1); _headersLock = dispatch_semaphore_create(1); // 超时时间 _downloadTimeout = 15.0; [self createNewSessionWithConfiguration:sessionConfiguration]; &#125; return self;&#125;// 创建新的 session- (void)createNewSessionWithConfiguration:(NSURLSessionConfiguration *)sessionConfiguration &#123; // 为避免影响，先取消可能存在的下载任务 [self cancelAllDownloads]; // cancel 之前的 session，然后创建一个新的 if (self.session) &#123; [self.session invalidateAndCancel]; &#125; sessionConfiguration.timeoutIntervalForRequest = self.downloadTimeout; self.session = [NSURLSession sessionWithConfiguration:sessionConfiguration delegate:self delegateQueue:nil];&#125; 然后，我们看一下主要方法 - (nullable SDWebImageDownloadToken *)downloadImageWithURL:url options:options progress:progressBlock completed:completedBlock。直接调用了添加进度与完成回调的方法，并将返回值作为结果返回。 添加进度与完成回调的方法我们稍后再议，先看一下调用时传入的 createCallback。就做了两件事：先创建一个 request，用于准备一些基础参数，然后，依据 request 创建 operation，详见代码注释 ☟。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465- (nullable SDWebImageDownloadToken *)downloadImageWithURL:(nullable NSURL *)url options:(SDWebImageDownloaderOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock &#123; __weak SDWebImageDownloader *wself = self; return [self addProgressCallback:progressBlock completedBlock:completedBlock forURL:url createCallback:^SDWebImageDownloaderOperation *&#123; __strong __typeof (wself) sself = wself; NSTimeInterval timeoutInterval = sself.downloadTimeout; if (timeoutInterval == 0.0) &#123; timeoutInterval = 15.0; &#125;// *** 1.创建 request // 为避免重复缓存 (NSURLCache + SDImageCache) ，如果没有明确要求使用 NSURLCache，我们默认忽略本地缓存 NSURLRequestCachePolicy cachePolicy = options &amp; SDWebImageDownloaderUseNSURLCache ? NSURLRequestUseProtocolCachePolicy : NSURLRequestReloadIgnoringLocalCacheData; NSMutableURLRequest *request = [[NSMutableURLRequest alloc] initWithURL:url cachePolicy:cachePolicy timeoutInterval:timeoutInterval]; // The default is YES - in other words, cookies are sent from and stored to the cookie manager by default. request.HTTPShouldHandleCookies = (options &amp; SDWebImageDownloaderHandleCookies); request.HTTPShouldUsePipelining = YES; // 设置 header，headersFilter 是过滤头部参数的block if (sself.headersFilter) &#123; request.allHTTPHeaderFields = sself.headersFilter(url, [sself allHTTPHeaderFields]); &#125; else &#123; request.allHTTPHeaderFields = [sself allHTTPHeaderFields]; &#125;// *** 2.创建下载的 operation (这个 operationClass ，给他赋什么值，他就是什么，如果不设置，就是默认值：[SDWebImageDownloaderOperation class]) SDWebImageDownloaderOperation *operation = [[sself.operationClass alloc] initWithRequest:request inSession:sself.session options:options]; operation.shouldDecompressImages = sself.shouldDecompressImages; // NSURLCredential 身份认证 if (sself.urlCredential) &#123; operation.credential = sself.urlCredential; &#125; else if (sself.username &amp;&amp; sself.password) &#123; // NSURLCredentialPersistenceForSession: Credential should be stored only for this session. operation.credential = [NSURLCredential credentialWithUser:sself.username password:sself.password persistence:NSURLCredentialPersistenceForSession]; &#125; // 设置优先级 if (options &amp; SDWebImageDownloaderHighPriority) &#123; operation.queuePriority = NSOperationQueuePriorityHigh; &#125; else if (options &amp; SDWebImageDownloaderLowPriority) &#123; operation.queuePriority = NSOperationQueuePriorityLow; &#125; // 更改执行顺序：先进后出(可在此设置) or 先进先出(默认) if (sself.executionOrder == SDWebImageDownloaderLIFOExecutionOrder) &#123; // 通过反向设置依赖，指定了队列中任务的执行顺序先加进去的依赖于后加进去的，那就成了后进先出了😎 [sself.lastAddedOperation addDependency:operation]; sself.lastAddedOperation = operation; &#125; return operation; &#125;];&#125; 现在，我们来看看添加进度与完成回调的方法： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253- (nullable SDWebImageDownloadToken *)addProgressCallback:(SDWebImageDownloaderProgressBlock)progressBlock completedBlock:(SDWebImageDownloaderCompletedBlock)completedBlock forURL:(nullable NSURL *)url createCallback:(SDWebImageDownloaderOperation *(^)(void))createCallback &#123; // The URL will be used as the key to the callbacks dictionary so it cannot be nil. If it is nil immediately call the completed block with no image or data. if (url == nil) &#123; if (completedBlock != nil) &#123; completedBlock(nil, nil, nil, NO); &#125; return nil; &#125; LOCK(self.operationsLock); SDWebImageDownloaderOperation *operation = [self.URLOperations objectForKey:url]; // 如果是第 1 次进来，通过 url 是取不出 URLOperation 的，但是第 2 次就有可能找到，也就是想要重复发第 2 次请求的话，就可以取到。 // 第 2 次可以取到(并且已经完成的情况下)，则不会走括号里边，也就不会执行关键步骤：[self.downloadQueue addOperation:operation]; ，所以就不会发起请求了，因为将 operation 添加到队列的时候，系统会自动触发请求。 if (!operation || operation.isFinished) &#123; // 创建 operation operation = createCallback(); __weak typeof(self) wself = self; operation.completionBlock = ^&#123; __strong typeof(wself) sself = wself; if (!sself) &#123; return; &#125; LOCK(sself.operationsLock); [sself.URLOperations removeObjectForKey:url]; UNLOCK(sself.operationsLock); &#125;; [self.URLOperations setObject:operation forKey:url]; // 添加到队列，即开始执行！ // Add operation to operation queue only after all configuration done according to Apple's doc. // `addOperation:` does not synchronously execute the `operation.completionBlock` so this will not cause deadlock. [self.downloadQueue addOperation:operation]; &#125; UNLOCK(self.operationsLock); // 存放进度和完成回调的 数组 array id downloadOperationCancelToken = [operation addHandlersForProgress:progressBlock completed:completedBlock]; // 与下载任务关联的一个对象，用于取消操作的时候 SDWebImageDownloadToken *token = [SDWebImageDownloadToken new]; token.downloadOperation = operation; token.url = url; token.downloadOperationCancelToken = downloadOperationCancelToken; return token;&#125; 主要做了这么几件事： 开始依然是参数校验 然后从 self.URLOperation 里边取 operation 第一次进来当然取不到 operation，于是就会进入 if (!operation || operation.isFinished) { ... } 的代码块。先执行我们传入的 createCallback() 创建 operation，然后将 operation 加入到 self.URLOperations 里边，同时设置好 operation 的 completionBlock，到时将 operation 移除，最后将 operation 加入到操作队列里，就会自动开始执行了。 创建一个 token，他是 SDWebImageDownloadToken 的实例，将它与 operation、url、progressBlock 及 completedBlock 关联起来，用于后边之后的取消操作。其中 progressBlock 及 completedBlock 是通过 downloadOperationCancelToken 与 token 关联起来的，这里用到了 operation 中的一个方法： 12345678910111213141516171819// SDWebImageDownloaderOperation- (nullable id)addHandlersForProgress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock &#123; SDCallbacksDictionary *callbacks = [NSMutableDictionary new]; if (progressBlock) callbacks[kProgressCallbackKey] = [progressBlock copy]; if (completedBlock) callbacks[kCompletedCallbackKey] = [completedBlock copy]; LOCK(self.callbacksLock); [self.callbackBlocks addObject:callbacks]; UNLOCK(self.callbacksLock); return callbacks;&#125; 由此可知，这个 id downloadOperationCancelToken 是一个存放 progressBlock 和 completedBlock 的 dictionary。 这里 if 语句起到了一个非常重要的作用，即 避免重复下载相同数据，具体原因就不解释了，上边的代码注释里已经写了。 到这里是不是觉得少了点什么，是的，SDWebImageDownloaderOperation 和 SDWebImageDownloadToken 的具体实现还不知道呢，接下来我们就分别查看这 2 个类，先从简单的开始吧！ SDWebImageDownloadToken这个类只有 3 个属性，前边都用到了，属性声明如下： 123456// 下载任务对应的 url@property (nonatomic, strong, nullable) NSURL *url;// 实际是包含 progressBlock 和 completionBlock 的字典，是通过 `addHandlersForProgress:completed:` 返回的，用于取消操作@property (nonatomic, strong, nullable) id downloadOperationCancelToken;// 操作的 operation，继承自 NSOperation，不过又遵守了 `SDWebImageDownloaderOperationInterface` 这个协议，扩展了一些方法。@property (nonatomic, weak, nullable) NSOperation&lt;SDWebImageDownloaderOperationInterface&gt; *downloadOperation; 它只实现了一个协议方法 cancel（Protocol: SDWebImageOperation），其中 self.downloadOperationCancelToken就是存放 progressBlock 和 completionBlock 的字典，然后将这个 token 创递给了 operation 的 cancel: 方法（也是一个协议方法），这个方法的具体实现下边就会说到。 12345678- (void)cancel &#123; if (self.downloadOperation) &#123; SDWebImageDownloadToken *cancelToken = self.downloadOperationCancelToken; if (cancelToken) &#123; [self.downloadOperation cancel:cancelToken]; &#125; &#125;&#125; SDWebImageDownloaderOperationInterface在开始介绍 operation 之前，先看看他遵守的协议 SDWebImageDownloaderOperationInterface，声明了以下协议方法。如果想要使用自定义的 operation，则它必须继承自 NSOperation 并且遵守这个协议。这些方法的实现可以参考 SDWebImageDownloaderOperation。 12345678910111213141516// SDWebImageDownloaderOperationInterface// 初始化方法- (nonnull instancetype)initWithRequest:(nullable NSURLRequest *)request inSession:(nullable NSURLSession *)session options:(SDWebImageDownloaderOptions)options;// 保存进度和完成的回调- (nullable id)addHandlersForProgress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock;// 是否需要解压图片- (BOOL)shouldDecompressImages;- (void)setShouldDecompressImages:(BOOL)value;// 凭证或称证书信息- (nullable NSURLCredential *)credential;- (void)setCredential:(nullable NSURLCredential *)value;// 取消- (BOOL)cancel:(nullable id)token; SDWebImageDownloaderOperation现在开始讨论 SDWebImageDownloaderOperation 这个类，下边是初始化方法： 123456789101112131415161718192021- (nonnull instancetype)init &#123; return [self initWithRequest:nil inSession:nil options:0];&#125;// 也是协议方法- (nonnull instancetype)initWithRequest:(nullable NSURLRequest *)request inSession:(nullable NSURLSession *)session options:(SDWebImageDownloaderOptions)options &#123; if ((self = [super init])) &#123; _request = [request copy]; _shouldDecompressImages = YES; _options = options; _callbackBlocks = [NSMutableArray new]; _executing = NO; _finished = NO; _expectedSize = 0; _unownedSession = session; _callbacksLock = dispatch_semaphore_create(1); _coderQueue = dispatch_queue_create("com.hackemist.SDWebImageDownloaderOperationCoderQueue", DISPATCH_QUEUE_SERIAL); &#125; return self;&#125; 下面介绍 2 个比较重要的方法： 核心方法 start 这是重写父类 NSOperation 的 start 方法，添加了自定义的操作。这个方法不需要手动调用，在将 operation 添加到 operationQueue 中的时候，系统会自动调用其 start 方法。重写后的操作包括以下几点： ①检测操作是否已取消，如果取消了，重置数据后直接返回。12345if (self.isCancelled) &#123; self.finished = YES; [self reset]; return;&#125; ②如果需要 App 进入后台时，继续执行下载操作，需要开启后台任务。并设置 ExpirationHandler，取消下载任务，并结束后台操作。1234567891011121314151617// 如果需要App进入后台时，继续执行此操作，需要开启后台任务。Class UIApplicationClass = NSClassFromString(@"UIApplication");BOOL hasApplication = UIApplicationClass &amp;&amp; [UIApplicationClass respondsToSelector:@selector(sharedApplication)];if (hasApplication &amp;&amp; [self shouldContinueWhenAppEntersBackground]) &#123; __weak __typeof__ (self) wself = self; UIApplication * app = [UIApplicationClass performSelector:@selector(sharedApplication)]; self.backgroundTaskId = [app beginBackgroundTaskWithExpirationHandler:^&#123; __strong __typeof (wself) sself = wself; if (sself) &#123; [sself cancel]; [app endBackgroundTask:sself.backgroundTaskId]; sself.backgroundTaskId = UIBackgroundTaskInvalid; &#125; &#125;];&#125; ③获取或创建 session，然后创建 dataTask， ④若 dataTask 创建成功，启动下载任务 [self.dataTask resume];，然后执行一次 progressBlock，并发送开始下载的通知。 ④若 dataTask 创建失败，直接调用完成回调，构建 error 信息并返回，然后重置数据。 ⑤关闭可能存在的后台下载任务。 具体下载过程中的操作，都在 session 相关的那些协议方法里边，详见代码注释，这里就不啰嗦了。 取消操作 最后看一下取消操作，即协议方法 - (BOOL)cancel:(nullable id)token; 的实现，将取消过程中用到的所有方法都展开就是下边这样： 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263- (BOOL)cancel:(nullable id)token &#123; BOOL shouldCancel = NO; LOCK(self.callbacksLock); // 移除 token，即移除一个存储着 completionBlock 和 progressBlock 的字典 [self.callbackBlocks removeObjectIdenticalTo:token]; // 如果已经没有回调，就去执行整体的 cancel 操作 if (self.callbackBlocks.count == 0) &#123; shouldCancel = YES; &#125; UNLOCK(self.callbacksLock); if (shouldCancel) &#123; // *** 点开 ☟ [self cancel]; &#125; return shouldCancel;&#125;- (void)cancel &#123; @synchronized (self) &#123; // *** 点开 ☟ [self cancelInternal]; &#125;&#125;- (void)cancelInternal &#123; if (self.isFinished) return; [super cancel]; if (self.dataTask) &#123; // 取消下载任务，并发出停止的通知 [self.dataTask cancel]; __weak typeof(self) weakSelf = self; dispatch_async(dispatch_get_main_queue(), ^&#123; [[NSNotificationCenter defaultCenter] postNotificationName:SDWebImageDownloadStopNotification object:weakSelf]; &#125;); // As we cancelled the task, its callback won't be called and thus won't // maintain the isFinished and isExecuting flags. if (self.isExecuting) self.executing = NO; if (!self.isFinished) self.finished = YES; &#125; // *** 点开 ☟ [self reset];&#125;// 重置变量- (void)reset &#123; LOCK(self.callbacksLock); [self.callbackBlocks removeAllObjects]; UNLOCK(self.callbacksLock); self.dataTask = nil; if (self.ownedSession) &#123; [self.ownedSession invalidateAndCancel]; self.ownedSession = nil; &#125;&#125; 小结SDWebImageDownloader 的内容就先介绍到这类，其他细节见 HHSDWebImageStudy 中的源码注释。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ SDWebImageManager]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-SDWebImageManager%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 3 篇，在前一篇中，我们了解了 SDWebImage 执行的基本流程，本篇就来介绍第一个核心类 SDWebImageMananger。 正文SDWebImageMananger.h 文件基本可以分为 3 各部分： ①定义了一个枚举 SDWebImageOptions，列举了可能会用到的一些场景。123456789101112131415161718192021222324252627282930313233343536typedef NS_OPTIONS(NSUInteger, SDWebImageOptions) &#123; // 重试已经失败的 url SDWebImageRetryFailed = 1 &lt;&lt; 0, // 低优先级，比如，在有 UI 交互的情况下，会延迟下载操作 SDWebImageLowPriority = 1 &lt;&lt; 1, // 下载完成后，仅做内存缓存，不做磁盘缓存 SDWebImageCacheMemoryOnly = 1 &lt;&lt; 2, // 下载过程中逐步加载图片，而不是完全下载完之后才展示 SDWebImageProgressiveDownload = 1 &lt;&lt; 3, // 刷新缓存 SDWebImageRefreshCached = 1 &lt;&lt; 4, // 当 App 进入后台时，继续下载任务，如果后台任务超时，操作将被自动取消 SDWebImageContinueInBackground = 1 &lt;&lt; 5, // 允许处理 Cookie SDWebImageHandleCookies = 1 &lt;&lt; 6, // 允许不受信任的 SSL 证书，生产环境慎用 SDWebImageAllowInvalidSSLCertificates = 1 &lt;&lt; 7, // 高优先级，即会把相应的图片放到最前边加载，而不是按照加入队列时的顺序执行 SDWebImageHighPriority = 1 &lt;&lt; 8, // 延迟 placeholder 的加载，即在下载完成时才加载 SDWebImageDelayPlaceholder = 1 &lt;&lt; 9, // 对动图也执行 transform 操作 SDWebImageTransformAnimatedImage = 1 &lt;&lt; 10, // 图片下载完成后，不直接自动给 imageView 赋值，给用户调整图片的机会 SDWebImageAvoidAutoSetImage = 1 &lt;&lt; 11, // 依据设备内存缩放图片，如果设置了 `SDWebImageProgressiveDownload` ，此设置无效 SDWebImageScaleDownLargeImages = 1 &lt;&lt; 12, // 在有内存缓存的情况下，依然需要查询磁盘缓存，建议与 SDWebImageQueryDiskSync 配合使用 SDWebImageQueryDataWhenInMemory = 1 &lt;&lt; 13, // 同步查询磁盘缓存 SDWebImageQueryDiskSync = 1 &lt;&lt; 14, // 仅加载缓存图片 SDWebImageFromCacheOnly = 1 &lt;&lt; 15, // 对内存和磁盘中的 image 也执行 transition 的操作 SDWebImageForceTransition = 1 &lt;&lt; 16&#125;; ②定义了一个协议 SDWebImageManagerDelegate，这里提供了以下 3 个协议方法：12345678// 缓存中没有指定图片时，是否需要下载- (BOOL)imageManager:(nonnull SDWebImageManager *)imageManager shouldDownloadImageForURL:(nullable NSURL *)imageURL;// 是否需要将制定 URL 标记为失败的 URL- (BOOL)imageManager:(nonnull SDWebImageManager *)imageManager shouldBlockFailedURL:(nonnull NSURL *)imageURL withError:(nonnull NSError *)error;// 允许在刚刚下载到 image 并且未做缓存之前，对图片执行 transform，返回处理后的 image- (nullable UIImage *)imageManager:(nonnull SDWebImageManager *)imageManager transformDownloadedImage:(nullable UIImage *)image withURL:(nullable NSURL *)imageURL; ③SDWebImageManager 的头文件，有几个重要属性，他们的作用见下边的注释。 12345678910// 代理对象@property (weak, nonatomic, nullable) id &lt;SDWebImageManagerDelegate&gt; delegate;// 处理缓存的对象@property (strong, nonatomic, readonly, nullable) SDImageCache *imageCache;// 处理下载工作的对象@property (strong, nonatomic, readonly, nullable) SDWebImageDownloader *imageDownloader;// 一个用户定义的 block，用于生成 cacheKey@property (nonatomic, copy, nullable) SDWebImageCacheKeyFilterBlock cacheKeyFilter;// 一个用户定义的 block，用于序列化下载到的数据@property (nonatomic, copy, nullable) SDWebImageCacheSerializerBlock cacheSerializer; 下面是 2 个常用的创建方法：+ (nonnull instancetype)sharedManager; 和 - (nonnull instancetype)initWithCache:(nonnull SDImageCache *)cache downloader:(nonnull SDWebImageDownloader *)downloader;，其实最终都是调用了后者 。 123456789101112131415161718192021222324252627282930313233// 单例+ (nonnull instancetype)sharedManager &#123; static dispatch_once_t once; static id instance; dispatch_once(&amp;once, ^&#123; instance = [self new]; &#125;); return instance;&#125;// 初始化，创建处理缓存和下载任务的对象 cache 和 downloader- (nonnull instancetype)init &#123; SDImageCache *cache = [SDImageCache sharedImageCache]; SDWebImageDownloader *downloader = [SDWebImageDownloader sharedDownloader]; return [self initWithCache:cache downloader:downloader];&#125;// 核心的初始化方法，为各属性赋初值- (nonnull instancetype)initWithCache:(nonnull SDImageCache *)cache downloader:(nonnull SDWebImageDownloader *)downloader &#123; if ((self = [super init])) &#123; // 处理缓存和下载任务的对象 _imageCache = cache; _imageDownloader = downloader; // 用于存储请求失败的 URL 的集合及操作时用的锁 (信号量) _failedURLs = [NSMutableSet new]; _failedURLsLock = dispatch_semaphore_create(1); // 存储运行中 operation 的集合，通过判断他的 count 是否为 0，判断操作是否在进行中：BOOL isRunning = (self.runningOperations.count &gt; 0); _runningOperations = [NSMutableSet new]; // 操作时用的锁 (信号量) _runningOperationsLock = dispatch_semaphore_create(1); &#125; return self;&#125; 另外几个方法，就不单独介绍了，用到的时候再继续讨论。此处，我们只看一个核心方法 - (nullable id &lt;SDWebImageOperation&gt;)loadImageWithURL:url options:options progress:progressBlock completed:completedBlock;，下面我们来一步步讨论这个方法的具体实现。 1.校验参数依次做如下处理：如果传入的 completedBlock 为空，就直接报错；如果传入的参数是 NSString * 类型的，需要将其转换成 NSURL；最后，如果 url 还不是 NSURL 类型，那就只能将其置为 nil，以免造成后边 Crash。123456789NSAssert(completedBlock != nil, @"If you mean to prefetch the image, use -[SDWebImagePrefetcher prefetchURLs] instead");if ([url isKindOfClass:NSString.class]) &#123; url = [NSURL URLWithString:(NSString *)url];&#125;if (![url isKindOfClass:NSURL.class]) &#123; url = nil;&#125; 2.生成总的 operation他是 SDWebImageCombinedOperation 实例对象，也是当前方法要返回的结果，并将当前类赋值给 operation 的一个 weak 属性(避免循环引用)。 12SDWebImageCombinedOperation *operation = [SDWebImageCombinedOperation new];operation.manager = self; // 肯定是 weak 属性 SDWebImageCombinedOperation 的声明与实现文件均在当前类 SDWebImageManager 的实现文件里边，简单看一下他的 .h/.m 文件吧。 1234567891011121314151617181920212223242526272829303132333435363738394041// SDWebImageCombinedOperation.h@interface SDWebImageCombinedOperation : NSObject &lt;SDWebImageOperation&gt;// 标识是否已取消@property (assign, nonatomic, getter = isCanceled) BOOL cancelled;// downloadToken 这是一个继承自 NSObject 的类，他有一个继承自 NSOperation 的属性，也就是真正执行下载操作时的 operation，cancel 时会用到@property (strong, nonatomic, nullable) SDWebImageDownloadToken *downloadToken;// 查询缓存时的 operation，用于标识当前 operation 是否已经被取消。其实查询缓存时，首先查看 operation.isCanceled，如果没被取消了，就会再去查询了。cancel 时会将其 isCanceled 属性置为 YES。@property (strong, nonatomic, nullable) NSOperation *cacheOperation;// manager@property (weak, nonatomic, nullable) SDWebImageManager *manager;@end// SDWebImageCombinedOperation.m#pragma mark - 代理方法实现@implementation SDWebImageCombinedOperation- (void)cancel &#123; @synchronized(self) &#123; self.cancelled = YES; // 取消查询缓存的 Operation，此时 isCanceled 会被置为 YES。 if (self.cacheOperation) &#123; [self.cacheOperation cancel]; self.cacheOperation = nil; &#125; // 取消下载操作 if (self.downloadToken) &#123; [self.manager.imageDownloader cancel:self.downloadToken]; &#125; // 将当前 operation 从 manager 中运行着的 operation 数组中移除。 [self.manager safelyRemoveOperationFromRunning:self]; &#125;&#125;@end// SDWebImageOperation 协议的定义@protocol SDWebImageOperation &lt;NSObject&gt;- (void)cancel;@end 可以看到，SDWebImageCombinedOperation 这个类的主要作用就是 cancel 操作，包括 cancel 查询缓存 和 cancel 下载数据。 3.再次检测一下 url如果是曾经失败的 url，而且不允许重试，或者 url 为空时，执行 completionBlock，并返回当前 operation。 123456789101112131415161718// self.failedURLs 是一个保存曾经失败过的 URL 的数组，用于检测当前 URL 是不是曾经请求失败过的URL.另外，搜索一个个元素的时候，NSSet 比 NSArray 查询更快。 BOOL isFailedUrl = NO; if (url) &#123; LOCK(self.failedURLsLock); isFailedUrl = [self.failedURLs containsObject:url]; UNLOCK(self.failedURLsLock); &#125; // 若出现以下两种情况就不再往下走了，直接执行 CompletionBlock：① URL 是空的；② 此 URL 是曾经请求失败的 URL，并且规定不允许重新请求曾经失败的 URL。 if (url.absoluteString.length == 0 || (!(options &amp; SDWebImageRetryFailed) &amp;&amp; isFailedUrl)) &#123; [self callCompletionBlockForOperation:operation completion:completedBlock error:[NSError errorWithDomain:NSURLErrorDomain code:NSURLErrorFileDoesNotExist userInfo:nil] url:url]; return operation; &#125; 4.保存 operation 至 self.runningOperations后者是一个数组，这里使用了信号量来确保线程安装。 123LOCK(self.runningOperationsLock);[self.runningOperations addObject:operation];UNLOCK(self.runningOperationsLock); 5.查询缓存。123456789101112131415NSString *key = [self cacheKeyForURL:url];SDImageCacheOptions cacheOptions = 0;if (options &amp; SDWebImageQueryDataWhenInMemory) cacheOptions |= SDImageCacheQueryDataWhenInMemory;if (options &amp; SDWebImageQueryDiskSync) cacheOptions |= SDImageCacheQueryDiskSync;if (options &amp; SDWebImageScaleDownLargeImages) cacheOptions |= SDImageCacheScaleDownLargeImages;__weak SDWebImageCombinedOperation *weakOperation = operation;operation.cacheOperation = [self.imageCache queryCacheOperationForKey:key options:cacheOptions done:^(UIImage *cachedImage, NSData *cachedData, SDImageCacheType cacheType)&#123; // 查询完成后的操作在这里，可能查到了，也可能没查到...&#125; 这里先准备了 2 个参数，查询的依据 key 和一些条件 cacheOptions。key 的获取是通过一个私有方法 (如下)，如果自定义了 key 的生成规则 self.cacheKeyFilter，就用自定义的，如果没有，就直接取 url.absoluteString。cacheOptions 是一个用 NS_OPTIONS 定义的枚举类型 (前边已介绍过)，可组合多种情况，在这里综合了 2 个查询的要求和 1 个缩放图片的要求。 1234567891011- (nullable NSString *)cacheKeyForURL:(nullable NSURL *)url &#123; if (!url) &#123; return @""; &#125; if (self.cacheKeyFilter) &#123; return self.cacheKeyFilter(url); &#125; else &#123; return url.absoluteString; &#125;&#125; 6.查询的具体过程详情将会在 SDImageCache 中介绍，下面讨论一下查询缓存结束后的操作。 7.移除当前 operation从 self.runningOperations 这个数组中移除当前 operation。 1234if (!strongOperation || strongOperation.isCancelled) &#123; [self safelyRemoveOperationFromRunning:strongOperation]; return;&#125; 8.判断是否需要下载当同时满足 3 个要求时，就需要下载新数据了：①没要求只能从缓存获取数据，即当缓存找不到时，可以去下载；②找不到缓存 或 要求必须更新缓存；③当 self.delegate 没有遵守协议， 或者 协议方法返回 YES。 123BOOL shouldDownload = (!(options &amp; SDWebImageFromCacheOnly)) &amp;&amp; (!cachedImage || options &amp; SDWebImageRefreshCached) &amp;&amp; (![self.delegate respondsToSelector:@selector(imageManager:shouldDownloadImageForURL:)] || [self.delegate imageManager:self shouldDownloadImageForURL:url]); 9.若需要下载 首先依然要做一个判断，即 如果有缓存数据并且要求刷新缓存数据时，需要先调用一次 CompletionBlock，将缓存数据返回去，然后再开始下载新数据，代码如下： 123if (cachedImage &amp;&amp; options &amp; SDWebImageRefreshCached) &#123; [self callCompletionBlockForOperation:strongOperation completion:completedBlock image:cachedImage data:cachedData error:nil cacheType:cacheType finished:YES url:url];&#125; 然后，准备下载数据时所需的一些基本选项，可以参考篇头介绍的枚举 SDWebImageOptions。 开始下载，调用了 SDWebImageDownloader 的下载方法，留待 SDWebImageDownloader 介绍，这里只讨论下载完成之后的操作。 12345678__weak typeof(strongOperation) weakSubOperation = strongOperation; strongOperation.downloadToken = [self.imageDownloader downloadImageWithURL:url options:downloaderOptions progress:progressBlock completed:^(UIImage *downloadedImage, NSData *downloadedData, NSError *error, BOOL finished)&#123; // 下载完成后的操作...&#125; 下载完成后，可以分这么几种情况：a.当前 operation 已经被取消，这种情况下不作任何操作，包括回调。 b.下载出错，先将失败的 error 信息返回，然后决定是否需要将当前 URL 存入失败的 URL 数组。 c.下载成功，此时要做的工作还有许多： ①如果设置了失败重发，则将当前 URL 从失败的 URL 数组中移除。12345if ((options &amp; SDWebImageRetryFailed)) &#123; LOCK(self.failedURLsLock); [self.failedURLs removeObject:url]; UNLOCK(self.failedURLsLock);&#125; ②对于自定义的 manager，需要执行另外一套缩放标准。123456if (self != [SDWebImageManager sharedManager] &amp;&amp; self.cacheKeyFilter &amp;&amp; downloadedImage)&#123; downloadedImage = [self scaledImageForKey:key image:downloadedImage];&#125; ③若需要更新缓存，但是未下载到图片，且缓存中本来有值的情况下，什么也不做，因为下载之前早已经缓存数据返回了。123if (options &amp; SDWebImageRefreshCached &amp;&amp; cachedImage &amp;&amp; !downloadedImage) &#123;// 需要更新缓存，但是未下载到图片，且缓存中本来有值的情况下，什么也不做，因为下载之前早已经缓存数据返回了&#125; ④如果下载到了图片，并且要求 transform 图片的情况下，异步执行 transform 和缓存图片的工作，然后回到主线程执行 completionBlock。12345678910111213141516171819202122232425262728293031323334dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_HIGH, 0), ^&#123; UIImage *transformedImage = [self.delegate imageManager:self transformDownloadedImage:downloadedImage withURL:url]; if (transformedImage &amp;&amp; finished) &#123; BOOL imageWasTransformed = ![transformedImage isEqual:downloadedImage]; NSData *cacheData; // pass nil if the image was transformed, so we can recalculate the data from the image if (self.cacheSerializer) &#123; cacheData = self.cacheSerializer(transformedImage, (imageWasTransformed ? nil : downloadedData), url); &#125; else &#123; cacheData = (imageWasTransformed ? nil : downloadedData); &#125; // *** 存盘：注意是存的 imageData [self.imageCache storeImage:transformedImage imageData:cacheData forKey:key toDisk:cacheOnDisk completion:nil]; &#125; [self callCompletionBlockForOperation:strongSubOperation completion:completedBlock image:transformedImage data:downloadedData error:nil cacheType:SDImageCacheTypeNone finished:finished url:url]; &#125;); ⑤如果下载到了图片，并且下载完成的话，则存盘并执行 completionBlock。存盘调用了 SDImageCache 的方法，随后介绍。 最后将当前 operation 移除。 12345678910111213141516171819202122232425262728293031323334353637383940dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_HIGH, 0), ^&#123; UIImage *transformedImage = [self.delegate imageManager:self transformDownloadedImage:downloadedImage withURL:url]; if (transformedImage &amp;&amp; finished) &#123; BOOL imageWasTransformed = ![transformedImage isEqual:downloadedImage]; NSData *cacheData; // pass nil if the image was transformed, so we can recalculate the data from the image if (self.cacheSerializer) &#123; cacheData = self.cacheSerializer(transformedImage, (imageWasTransformed ? nil : downloadedData), url); &#125; else &#123; cacheData = (imageWasTransformed ? nil : downloadedData); &#125; // *** 存盘：注意是存的 imageData [self.imageCache storeImage:transformedImage imageData:cacheData forKey:key toDisk:cacheOnDisk completion:nil]; &#125; [self callCompletionBlockForOperation:strongSubOperation completion:completedBlock image:transformedImage data:downloadedData error:nil cacheType:SDImageCacheTypeNone finished:finished url:url]; &#125;);// ...if (finished) &#123; [self safelyRemoveOperationFromRunning:strongSubOperation];&#125; 10.若不需要下载，并且有缓存此时，执行 completionBlock 将缓存数据返回，然后移除当前 operation。 12345678910[self callCompletionBlockForOperation:strongOperation completion:completedBlock image:cachedImage data:cachedData error:nil cacheType:cacheType finished:YES url:url];[self safelyRemoveOperationFromRunning:strongOperation]; 11.其它，即没有缓存，且不需要下载和上边的操作类似，只不过传回的 image 和 data 均为 nil。12345678910[self callCompletionBlockForOperation:strongOperation completion:completedBlock image:nil data:nil error:nil cacheType:SDImageCacheTypeNone finished:YES url:url];[self safelyRemoveOperationFromRunning:strongOperation]; 最后将 operation 返回。 小结以上就是 SDWebImageManager 这个类的主要功能，其中关于缓存和下载的内容，详见后边几篇的讨论。]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage源码学习笔记 ☞ 结构及基本流程]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-%E2%98%9E-%E7%BB%93%E6%9E%84%E5%8F%8A%E5%9F%BA%E6%9C%AC%E6%B5%81%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[前言这是本系列的第 2 篇，本篇的目的如题所示，就是了解 SDWebImage 这个常用框架的文件结构，然后通过案例梳理流程，作为后边章节的主线。 目录结构为了查看 SDWebImage 的完整目录结构，首先需要导入 SDWebImage，由 上一篇 我们了解到，新版 SDWebImage 总共分了 4 个子 pod，默认只导入了 Core，为了将它们全部导入 demo 中，Podfile 文件需要这么来写： 1234567891011source 'https://github.com/CocoaPods/Specs.git'platform :ios, '9.0'target 'HHSDWebImageStudy' dopod 'SDWebImage', '4.4.2' // 为了讨论方便，这里选定了一个比较新的版本pod 'SDWebImage/WebP'pod 'SDWebImage/GIF'pod 'SDWebImage/MapKit'end 执行 pod install 之后得到的目录结构如下图所示，点此查看完整目录结构 。 示例：用 UIImageView 展示一张静态网络图片下面是使用时的代码，加载图片时调用的是 UIImageView+WebCache 中的方法 sd_setImageWithURL:。 123456// 1.创建 UIImageViewUIImageView *imgV = [[UIImageView alloc] initWithFrame:CGRectMake(125, 70, 160, 160)];[self.view addSubview:imgV];// 2.加载网络图片[imgV sd_setImageWithURL:[NSURL URLWithString:@"https://img.zcool.cn/community/01c81558a2723ca801219c77a1e34e.jpg"]]; 查看 sd_setImageWithURL: 的实现，内部调用了一个参数很全的方法 sd_setImageWithURL: placeholderImage: options: progress: completed:，只不过其他参数已经给了默认值。123- (void)sd_setImageWithURL:(nullable NSURL *)url &#123; [self sd_setImageWithURL:url placeholderImage:nil options:0 progress:nil completed:nil];&#125; 其实，还有很多类似方法，都是对这个方法不同程度的封装，即给一些参数提供了默认值，或者增加了一些额外操作，比如下边这几个方法： 123456789101112131415161718192021222324252627282930313233343536- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:0 progress:nil completed:nil];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:options progress:nil completed:nil];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url completed:(nullable SDExternalCompletionBlock)completedBlock &#123; [self sd_setImageWithURL:url placeholderImage:nil options:0 progress:nil completed:completedBlock];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder completed:(nullable SDExternalCompletionBlock)completedBlock &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:0 progress:nil completed:completedBlock];&#125;- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options completed:(nullable SDExternalCompletionBlock)completedBlock &#123; [self sd_setImageWithURL:url placeholderImage:placeholder options:options progress:nil completed:completedBlock];&#125;- (void)sd_setImageWithPreviousCachedImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock &#123; // 1.取出本地缓存 NSString *key = [[SDWebImageManager sharedManager] cacheKeyForURL:url]; UIImage *lastPreviousCachedImage = [[SDImageCache sharedImageCache] imageFromCacheForKey:key]; // 2.调用本类中的 参数很多的方法，并将缓存数据 (没有时用 placeholder) 传给 placeholder [self sd_setImageWithURL:url placeholderImage:lastPreviousCachedImage ?: placeholder options:options progress:progressBlock completed:completedBlock];&#125; 再来看看这个参数巨多的方法 sd_setImageWithURL: placeholderImage: options: progress: completed: 究竟是如何实现的： 1234567891011121314- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock &#123; // 跳转 UIView+WebCache 中的 sd_internalSetImageWithURL: 方法执行，若 &lt;= 3.x.x 时，sd_internalSetImageWithURL: 在本类内部 [self sd_internalSetImageWithURL:url placeholderImage:placeholder options:options operationKey:nil setImageBlock:nil progress:progressBlock completed:completedBlock];&#125; 还是继续调用别的方法，不过这次是调用父类分类 (UIView+WebCache) 中的方法，之所以要调用直接或间接父类的方法，是为了让其他控件 (如 UIButton 等) 可以复用加载网络图片的方法，他们间的关系如下： 现在去父类的分类 UIView+WebCache 看看吧，为了缩减篇幅，以下代码做了适当精简。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081- (void)sd_internalSetImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options operationKey:(nullable NSString *)operationKey setImageBlock:(nullable SDSetImageBlock)setImageBlock progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock &#123; return [self sd_internalSetImageWithURL:url placeholderImage:placeholder options:options operationKey:operationKey setImageBlock:setImageBlock progress:progressBlock completed:completedBlock context:nil];&#125;// *** 核心方法- (void)sd_internalSetImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options operationKey:(nullable NSString *)operationKey setImageBlock:(nullable SDSetImageBlock)setImageBlock progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDExternalCompletionBlock)completedBlock context:(nullable NSDictionary&lt;NSString *, id&gt; *)context &#123; // 1.取消当前 validOperationKey 对应的 loadOperation NSString *validOperationKey = operationKey ?: NSStringFromClass([self class]); [self sd_cancelImageLoadOperationWithKey:validOperationKey]; // 创建 manager（两种：1.用户自定义 2.此库自带的单例） SDWebImageManager *manager; if ([context valueForKey:SDWebImageExternalCustomManagerKey]) &#123; manager = (SDWebImageManager *)[context valueForKey:SDWebImageExternalCustomManagerKey]; &#125; else &#123; manager = [SDWebImageManager sharedManager]; &#125; // 2.下载 id &lt;SDWebImageOperation&gt; operation = [manager loadImageWithURL:url options:options progress:combinedProgressBlock completed:^(UIImage *image, NSData *data, NSError *error, SDImageCacheType cacheType, BOOL finished, NSURL *imageURL) &#123; __strong __typeof (wself) sself = wself; // ... SDWebImageNoParamsBlock callCompletedBlockClojure = ^&#123; if (!sself) &#123; return; &#125; if (!shouldNotSetImage) &#123; [sself sd_setNeedsLayout]; &#125; if (completedBlock &amp;&amp; shouldCallCompletedBlock) &#123; completedBlock(image, error, cacheType, url); &#125; &#125;; // ... dispatch_main_async_safe(^&#123;#if SD_UIKIT || SD_MAC [sself sd_setImage:targetImage imageData:targetData basedOnClassOrViaCustomSetImageBlock:setImageBlock transition:transition cacheType:cacheType imageURL:imageURL];#else [sself sd_setImage:targetImage imageData:targetData basedOnClassOrViaCustomSetImageBlock:setImageBlock];#endif callCompletedBlockClojure(); &#125;); &#125;];// 3.为 UIImageView 绑定新的 operation，即上边这个 operation [self sd_setImageLoadOperation:operation forKey:validOperationKey]; &#125; else &#123; // 如果图片 URL 不存在，执行 completedBlock，返回错误提示 dispatch_main_async_safe(^&#123; if (completedBlock) &#123; NSError *error = [NSError errorWithDomain:SDWebImageErrorDomain code:-1 userInfo:@&#123;NSLocalizedDescriptionKey : @"Trying to load a nil url"&#125;]; completedBlock(nil, error, SDImageCacheTypeNone, url); &#125; &#125;); &#125;&#125; 可以看到，父类最终调用了 - (void)sd_internalSetImageWithURL: ... 这个核心方法，虽然实现代码较多，其实主要就做了这么 3 件事： ① 取消 validOperationKey 对应的已经存在的 loadOperation。因为传进来的参数 validOperationKey 是 nil，所以 validOperationKey 实际取的是当前类名对应的字符串 NSStringFromClass([self class])。 ② 执行 SDWebImageManager 的方法 - (id &lt;SDWebImageOperation&gt;)loadImageWithURL: ... 下载图片，并返回一个 operation 对象，其实是这个类 SDWebImageCombinedOperation 的实例，与上一步 cancel 的对象是同一类型，下面就会用到。 ③ 保存 validOperationKey 与刚刚生成的 operation 之间的映射关系，以备取消时使用（如 ①）。 对于 ② 用到的方法，其实现代码太长，此处代码也做了精简： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556// SDWebImageManager- (id &lt;SDWebImageOperation&gt;)loadImageWithURL:(nullable NSURL *)url options:(SDWebImageOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDInternalCompletionBlock)completedBlock&#123; // ... // 1.创建 operation SDWebImageCombinedOperation *operation = [SDWebImageCombinedOperation new]; operation.manager = self; // 肯定是 weak 属性 __weak typeof(strongOperation) weakSubOperation = strongOperation; // 2.使用 imageCache 的方法查询缓存 operation.cacheOperation = [self.imageCache queryCacheOperationForKey:key options:cacheOptions done:^(UIImage *cachedImage, NSData *cachedData, SDImageCacheType cacheType) &#123;// 以下都是查询结束后 (查到/没查到) 的操作 if (shouldDownload) &#123;// 3. 需要下载 __weak typeof(strongOperation) weakSubOperation = strongOperation; strongOperation.downloadToken = [self.imageDownloader downloadImageWithURL:url options:downloaderOptions progress:progressBlock completed:^(UIImage *downloadedImage, NSData *downloadedData, NSError *error, BOOL finished) &#123; // 缩放图片(如果可以的话) // 缓存图片 // 执行完成的回调 &#125; &#125; else if (cachedImage) &#123;// 4. 如果取到了缓存 // 执行完成的回调 [self callCompletionBlockForOperation:strongOperation completion:completedBlock image:cachedImage data:cachedData error:nil cacheType:cacheType finished:YES url:url]; // 从正在运行的 operation 数组中移除当前 operation [self safelyRemoveOperationFromRunning:strongOperation]; &#125; else &#123;// 5. 没取到缓存 &amp;&amp; 不允许下载 // 执行完成的回调 // Image not in cache and download disallowed by delegate [self callCompletionBlockForOperation:strongOperation completion:completedBlock image:nil data:nil error:nil cacheType:SDImageCacheTypeNone finished:YES url:url]; // 从正在运行的 operation 数组中移除当前 operation [self safelyRemoveOperationFromRunning:strongOperation]; &#125; &#125; return operation;&#125; 该方法主要是在创建 SDWebImageCombinedOperation 对象，大概的操作见上边的代码注释。只对其中 2 个自己认为更重要点的方法做一个简要说明，详细的讨论可以查看后边的相关篇章。 SDImageCache 中查询缓存的方法 - (nullable NSOperation *)queryCacheOperationForKey: ...。这个方法的作用是查询二级缓存，也就依次从内存、磁盘两处缓存查询我们需要的图片数据，前者查不到时，才在后者中查找。 1234// SDImageCache- (nullable NSOperation *)queryCacheOperationForKey:(nullable NSString *)key options:(SDImageCacheOptions)options done:(nullable SDCacheQueryCompletedBlock)doneBlock; SDWebImageDownloader 中下载图片的方法 - (nullable SDWebImageDownloadToken *)downloadImageWithURL: ...。此方法的实现通过 operation 和 operationQueue 配合使用来执行下载操作的，即 先分别创建 operation 和 operationQueue，然后将 operation 添加到 operationQueue 中，就自动启动任务了。 12345// SDWebImageDownloader- (nullable SDWebImageDownloadToken *)downloadImageWithURL:(nullable NSURL *)url options:(SDWebImageDownloaderOptions)options progress:(nullable SDWebImageDownloaderProgressBlock)progressBlock completed:(nullable SDWebImageDownloaderCompletedBlock)completedBlock; 无论是查询缓存还是请求网络数据，最终都会给 imageView 赋值，也就是我们通常希望达到的效果。 小结至此，我们大概理了一下 使用 SDWebImage 加载网络图片的主要流程，最后借用 SDWebImage 作者提供的一张时序图做个简单总结吧。 源码注释及 demoHHSDWebImageStudy]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SDWebImage 源码学习笔记·前传 ☞ 升级 4.x.x 后适配 WebP/GIF]]></title>
    <url>%2F2018%2F12%2F16%2FSDWebImage-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%C2%B7%E5%89%8D%E4%BC%A0-%E2%98%9E-%E5%8D%87%E7%BA%A7-4-x-x-%E5%90%8E%E9%80%82%E9%85%8D-WebP-GIF%2F</url>
    <content type="text"><![CDATA[前言前段时间对项目中使用的 SDWebImage 进行了一次升级 (3.7.6 → 4.4.2)，升级过程中遇到了一些问题，于是又把源码读了一遍，为了记录读码过程，就有了这个系列： SDWebImage 源码学习笔记·前传 ☞ 升级 4.x.x 后适配 WebP/GIF SDWebImage源码学习笔记 ☞ 结构及基本流程 SDWebImage源码学习笔记 ☞ SDWebImageManager SDWebImage源码学习笔记 ☞ SDWebImageDownloader SDWebImage源码学习笔记 ☞ SDImageCache 本篇是第 1 篇，主要记述了升级过程中的遇到的问题及处理方案。 升级现在转回到本篇的主要内容，首先当然是升级了，不过最新版的 SDWebImage 里边分了几个子 pod ，默认下载不全。为了满足我们的日常使用要求，还需要加上 WebP 和 GIF 这两个子 pod： 12345platform :ios, '7.0'pod 'SDWebImage', '~&gt; 4.0'pod 'SDWebImage/WebP'pod 'SDWebImage/GIF' 不过，在执行 pod install 的时候，发现 pod &#39;SDWebImage/WebP&#39; 卡在了 Installing libwebp (0.6.0) 的地方。 据说 WebP 的解析库是 Google 的开源库，所以需要使用 VPN。笔者使用了蓝灯 Lantern，然后发现 浏览器可以正常访问 Google 网站，可是命令行 pod install 依然会卡住，可以通过下边两条命令检测命令行是否已添加 VPN：1curl www.google.com 执行上述命令后，如果返回了网页信息，就说明 VPN 已经添加成功。 于是猛查资料，才有了下文： 下载 Lantern 安装后，打开 Lantern，页面如下，查询自己 VPN 软件 (Lantern) 的http代理： 可以看出 Lantern 的 http 代理运行在 52425(选中部分) 端口，不同机器上的端口号也会不同。为什么说是 http 而不是 https 呢？将地址栏中的域名复制出来贴到任意文本编辑器，就会发现变成了 http://localhost:52425/axxx 3.在命令行输入以下指令，查看端口使用情况：1sudo lsof -i -P 可以看出来基本使用的只有 2 个代理端口，52425 和 52427，结合第一步可以得出，52425 是 http 的代理端口，52427 是 https 的代理端口 😎 4.配置命令行代理端口，有两种方式：A、给本地 git 设置 http/https 代理123456789// 添加 http/https 代理的指令 https://127.0.0.1:XXXX (XXXX代表端口号)git config --global http.proxy https://127.0.0.1:52425git config --global https.proxy https://127.0.0.1:52427// 取消 http/https 代理的指令git config --global --unset http.proxy git config --global --unset https.proxy// 这种方法可以 pod install 成功，不过 curl www.google.com 不成功，即打不出 Google 的网页信息。 B、设置系统全局变量 http/https 代理1234567891011121314切换到etc目录下 cd /etc 以管理员身份编辑 sudo vim bashrc 输入密码进入后, G $ 定位到文件末尾 按下键盘字母 i 开始输入，在最下面位置粘贴下面两句export http_proxy=127.0.0.1:52425export https_proxy=127.0.0.1:52427 然后退出保存 按Esc :wq接下来让修改文件生效 source bashrc最后一步 验证成功: curl www.google.com 如果收到 google 的 html 数据代表已通。 完成后 pod 内部的结构如下： 使用为叙述方便，此处将图片的 URL 定义成了宏： 1234#define URL_Normal [NSURL URLWithString:@"此处是 普通静态图 地址"]#define URL_WebP_Normal [NSURL URLWithString:@"此处是 静态 WebP 地址"]#define URL_WebP_Dynamic [NSURL URLWithString:@"此处是 动态 WebP 地址"]#define URL_GIF [NSURL URLWithString:@"此处是 GIF 地址"] 第一种方案 png/jpg，直接使用 SDWebImage 提供的普通方法即可。 WebP，只要导入了 WebP 的子 pod，直接使用普通方法即可正常加载静态、动态的 WebP。 GIF，此处使用了 FLAnimatedImageView 代替 UIImageView 来展示 GIF。因为 SDWebImage4.0 以后，如果继续 直接 使用 UIImageView 将只展示 GIF 的第一帧，可以使用推荐的 FLAnimatedImageView 替换 UIImageView。 12345678910111213141516171819202122232425 // 普通静态图 UIImageView *imgV = [[UIImageView alloc] initWithFrame:CGRectMake(125, 70, 160, 160)]; imgV.backgroundColor = [UIColor lightGrayColor]; [self.view addSubview:imgV]; [imgV sd_setImageWithURL:URL_Normal]; // WebP UIImageView *imgVB = [[UIImageView alloc] initWithFrame:CGRectMake(125, 70+160+10, 160, 160)]; imgVB.backgroundColor = [UIColor lightGrayColor]; imgVB.contentMode = UIViewContentModeScaleAspectFill; [self.view addSubview:imgVB]; // WebP 静态图// [imgVB sd_setImageWithURL:URL_WebP_Normal]; // WebP 动态图 [imgVB sd_setImageWithURL:URL_WebP_Dynamic]; // GIF FLAnimatedImageView *imgView = [[FLAnimatedImageView alloc] initWithFrame:CGRectMake(125, 70+160+10+160+10, 160, 200)]; imgView.backgroundColor = [UIColor lightGrayColor]; imgView.contentMode = UIViewContentModeScaleAspectFill; [self.view addSubview:imgView]; [imgView sd_setShowActivityIndicatorView:YES]; [imgView sd_setImageWithURL:URL_GIF placeholderImage:[UIImage imageNamed:@"placeholder"] options:1]; 第二种方案统一都使用 UIImageView 展示图片，不过和上边的第一种方案有以下不同： 使用 SDWebImageManager 的 loadImageWithURL: options: progress: completed: 方法下载图片； 根据得到的 data 判断图片类型: SDImageFormat format = [NSData sd_imageFormatForImageData:data];，基本原理是根据 data 的第一个字节来判断； WebP 使用专用方法 sd_imageWithWebPData: 解码； GIF 也使用专用方法 sd_animatedGIFWithData: 解码。 12345678910111213141516171819202122232425262728UIImageView *gifView = [[UIImageView alloc] initWithFrame:CGRectMake(10, 70+160+10+160+10+200+10, kScreenW-20, 200)]; gifView.backgroundColor = [UIColor lightGrayColor]; [self.view addSubview:gifView]; self.imgView = gifView; SDWebImageManager *mgr = [SDWebImageManager sharedManager]; __weak typeof(self) weakSelf = self; [mgr loadImageWithURL:URL_GIF // URL_WebP_Dynamic // URL_WebP_Normal // URL_Normal options:1 progress:nil completed:^(UIImage * _Nullable image, NSData * _Nullable data, NSError * _Nullable error, SDImageCacheType cacheType, BOOL finished, NSURL * _Nullable imageURL) &#123; SDImageFormat format = [NSData sd_imageFormatForImageData:data]; switch (format) &#123; case SDImageFormatGIF: weakSelf.imgView.image = [UIImage sd_animatedGIFWithData:data]; break; case SDImageFormatWebP: weakSelf.imgView.image = [UIImage sd_imageWithWebPData:data]; break; case SDImageFormatPNG: case SDImageFormatJPEG: weakSelf.imgView.image = image; break; default: break; &#125; &#125;]; 第三种方案自定义 HHImageView，在它的 init 系列方法中添加了对 GIF 的解析： 1[[SDWebImageCodersManager sharedInstance] addCoder:[SDWebImageGIFCoder sharedCoder]]; 这种方案对最终的调用方来说，非常简洁，不需要对各种图片区别对待。 123456789101112 HHImageView *customView = [[HHImageView alloc] initWithFrame:CGRectMake(125, 70, 160, 160)]; customView.backgroundColor = [UIColor lightGrayColor]; [self.view addSubview:customView]; [customView sd_setShowActivityIndicatorView:YES]; // 静态图// [customView sd_setImageWithURL:URL_Normal]; // WebP 动图// [customView sd_setImageWithURL:URL_WebP_Normal]; // WebP 静态图// [customView sd_setImageWithURL:URL_WebP_Dynamic]; // GIF [customView sd_setImageWithURL:URL_GIF]; 本文完整示例代码见: HHSDWebImageStudy 参考 Mac 命令行（终端）设置Lantern vpn代理 iOS SDWebImage加载webP]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>SDWebImage</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[MBProgressHUD 源码学习笔记]]></title>
    <url>%2F2018%2F12%2F16%2FMBProgressHUD-%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[前言MBProgressHUD 是 iOS 开发中经常会用到的一个加载动画库，本文就来简单学习一下源码。 视图层级在开始学习源码之前，先大概了解一下整个视图的层级结构吧，主要分这几个视图： backgroundView，位于最底层，是一个遮罩层，在 HUD 显示时，我们之所以无法点击后边的视图，都是因为它。 bezelView，位于 backgroundView 之上，承载着下边要说的几个视图，下边 4 个视图位于同一层级。 indicator，是 bezelView 的子视图，类型不定，他自己提供了 2 种类型 MBRoundProgressView 和 MBBarProgressView，不过也可以使用用户自定义视图 customView。 label，indicator 下方的提示标签。 detailLabel，label 下方的补充提示标签。 button，最底部的按钮，可以响应点击事件。 当然，以上这些视图不一定要全部显示，可以由用户（我们自己 O(∩_∩)O ）自由选择。 源码言归正传，现在开始读源码。 类结构虽然 MBProgressHUD 的文件数量非常少，只有 2 个：MBProgressHUD.h 和 MBProgressHUD.m，但还有几个相关的类和协议： MBProgressHUD我们主要研究主类 MBProgressHUD，首先看几个重要的属性： 123456@property (assign, nonatomic) NSTimeInterval graceTime; // 显示的宽限时间，即从显示方法被调用到真正显示之间的时间差@property (assign, nonatomic) NSTimeInterval minShowTime; // HUD 最小的展示时间 @property (nonatomic, weak) NSTimer *graceTimer; // 延迟显示的 timer@property (nonatomic, weak) NSTimer *minShowTimer; // 保证 HUD 最短显示维持时间 的timer@property (nonatomic, weak) NSTimer *hideDelayTimer; // 延迟隐藏的 timer 接下来看看 MBProgressHUD 为我们提供的 3 个类方法用于展示和隐藏 HUD 的方法： 123456// 展示 HUD+ (instancetype)showHUDAddedTo:(UIView *)view animated:(BOOL)animated;// 隐藏 HUD+ (BOOL)hideHUDForView:(UIView *)view animated:(BOOL)animated;// 返回最顶层未结束的 HUD+ (nullable MBProgressHUD *)HUDForView:(UIView *)view; 我们从展示的方法实现开始探究，实现代码如下： 1234567+ (instancetype)showHUDAddedTo:(UIView *)view animated:(BOOL)animated &#123; MBProgressHUD *hud = [[self alloc] initWithView:view]; hud.removeFromSuperViewOnHide = YES; [view addSubview:hud]; [hud showAnimated:animated]; return hud;&#125; 我们发现，在 initWithView: 方法中对入参 view 做了空判断，这里使用了 NSAsset，如果为空，会报错： 1234- (id)initWithView:(UIView *)view &#123; NSAssert(view, @"View must not be nil."); return [self initWithFrame:view.bounds];&#125; 接着看 showAnimated: 的方法实现。 123456789101112131415161718192021222324252627282930313233- (void)showAnimated:(BOOL)animated &#123; // 1.确保在主线程执行 MBMainThreadAssert(); // 2.关闭之前可能存在的 minShowTimer [self.minShowTimer invalidate]; self.useAnimation = animated; self.finished = NO; // 3.是否延缓 HUD 的展示 // 如果设置了 graceTime，则延缓 HUD 的展示 if (self.graceTime &gt; 0.0) &#123; NSTimer *timer = [NSTimer timerWithTimeInterval:self.graceTime target:self selector:@selector(handleGraceTimer:) userInfo:nil repeats:NO]; [[NSRunLoop currentRunLoop] addTimer:timer forMode:NSRunLoopCommonModes]; self.graceTimer = timer; &#125; else &#123; // 如果没设置 graceTime，则立即展示 HUD [self showUsingAnimation:self.useAnimation]; &#125;&#125;// 定时器 graceTimer 的响应方法- (void)handleGraceTimer:(NSTimer *)theTimer &#123; // 只有当任务还在进行的时候才会显示 HUD，否则什么也不做 if (!self.hasFinished) &#123; [self showUsingAnimation:self.useAnimation]; &#125;&#125; 此处做了 3 件事： ① 首先，为了确保在主线程执行，能够及时更新 UI ，这里使用了 NSAsset，如果当前线程不是主线程，就会报错，MBMainThreadAssert() 的定义如下。 1#define MBMainThreadAssert() NSAssert([NSThread isMainThread], @"MBProgressHUD needs to be accessed on the main thread."); ② 然后，移除了可能存在的 minShowTimer。 ③ 最后，根据 graceTime 有无值来决定是否需要延缓 HUD 的显示，如果有值，则启动 graceTimer，待时间到时再执行显示的操作；如果无值，则直接去显示 HUD。 graceTime 应该是针对耗时比较少的操作准备的，定时器 graceTimer 时间到的时候，操作有可能已经完成了 (self.hasFinished == YES)，这时就不需要展示 HUD 了，以免影响用户体验。 另外，minShowTime 是在隐藏的时候使用的，通过 minShowTimer 保证 HUD 展示时间 &gt;= minShowTime 从而避免 HUD 显示时间过短的问题，也是为了提升用户体验。 做完 ③ 的判断就该去显示了，即 showUsingAnimation: 的实现，它主要也做了 3 件事： 123456789101112131415161718192021- (void)showUsingAnimation:(BOOL)animated &#123; [self.bezelView.layer removeAllAnimations]; [self.backgroundView.layer removeAllAnimations]; [self.hideDelayTimer invalidate]; self.showStarted = [NSDate date]; self.alpha = 1.f; [self setNSProgressDisplayLinkEnabled:YES]; if (animated) &#123; [self animateIn:YES withType:self.animationType completion:NULL]; &#125; else &#123;#pragma clang diagnostic push#pragma clang diagnostic ignored "-Wdeprecated-declarations" self.bezelView.alpha = self.opacity;#pragma clang diagnostic pop self.backgroundView.alpha = 1.f; &#125;&#125; ① 取消之前可能存在的 animations 并停止 ‘延缓隐藏’ 的 timer。 ② 根据是否需要展示进度做相应的处理。此处传的是 YES，即需要展示：创建一个 CADisplayLink对象并启动（启动的代码写在了 progressObjectDisplayLink 的 setter 里边），在响应方法里将外界传入的 progressObject（NSProgress）的值 progressObject.fractionCompleted 赋给 progress（CGFloat），然后在 progress 的 setter 里更新控件的值。 12345678910111213141516171819202122232425262728293031323334353637// 创建 CADisplayLink 对象，用于更新- (void)setNSProgressDisplayLinkEnabled:(BOOL)enabled &#123; if (enabled &amp;&amp; self.progressObject) &#123; if (!self.progressObjectDisplayLink) &#123; self.progressObjectDisplayLink = [CADisplayLink displayLinkWithTarget:self selector:@selector(updateProgressFromProgressObject)]; &#125; &#125; else &#123; self.progressObjectDisplayLink = nil; &#125;&#125;// 为 progressObjectDisplayLink 赋值，并启动 CADisplayLink- (void)setProgressObjectDisplayLink:(CADisplayLink *)progressObjectDisplayLink &#123; if (progressObjectDisplayLink != _progressObjectDisplayLink) &#123; [_progressObjectDisplayLink invalidate]; _progressObjectDisplayLink = progressObjectDisplayLink; [_progressObjectDisplayLink addToRunLoop:[NSRunLoop mainRunLoop] forMode:NSDefaultRunLoopMode]; &#125;&#125;// 给 progress 赋值- (void)updateProgressFromProgressObject &#123; self.progress = self.progressObject.fractionCompleted;&#125;// progress 的 setter，并给展示进度的控件赋值- (void)setProgress:(float)progress &#123; if (progress != _progress) &#123; _progress = progress; UIView *indicator = self.indicator; if ([indicator respondsToSelector:@selector(setProgress:)]) &#123; [(id)indicator setValue:@(self.progress) forKey:@"progress"]; &#125; &#125;&#125; 外界给 _progressObject 赋值的方法实现如下。这里加了 if (progressObject != _progressObject) 的判断，避免了重复赋值。 123456- (void)setProgressObject:(NSProgress *)progressObject &#123; if (progressObject != _progressObject) &#123; _progressObject = progressObject; [self setNSProgressDisplayLinkEnabled:YES]; &#125;&#125; ③ 如果需要过渡动画，无论是 show 还是 hide 都会调用 animateIn: withType: completion: 这个方法，代码实现如下，详见注释。 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061- (void)animateIn:(BOOL)animatingIn withType:(MBProgressHUDAnimation)type completion:(void(^)(BOOL finished))completion &#123; // 确定缩放动画的类型 if (type == MBProgressHUDAnimationZoom) &#123; type = animatingIn ? MBProgressHUDAnimationZoomIn : MBProgressHUDAnimationZoomOut; &#125; CGAffineTransform small = CGAffineTransformMakeScale(0.5f, 0.5f); // x、y 方向的缩放倍数均为 0.5 CGAffineTransform large = CGAffineTransformMakeScale(1.5f, 1.5f); // x、y 方向的缩放倍数均为 1.5 UIView *bezelView = self.bezelView;// * 设置初始状态的值（show 的 过渡动画 的初值，hide 的初值就不用设置了） if (animatingIn &amp;&amp; bezelView.alpha == 0.f &amp;&amp; type == MBProgressHUDAnimationZoomIn) &#123; bezelView.transform = small; &#125; else if (animatingIn &amp;&amp; bezelView.alpha == 0.f &amp;&amp; type == MBProgressHUDAnimationZoomOut) &#123; bezelView.transform = large; &#125; // 执行动画的 block，作为后边方法的参数 dispatch_block_t animations = ^&#123;// * show 的过渡动画 的 终值 if (animatingIn) &#123; bezelView.transform = CGAffineTransformIdentity;// * 下边 2 个 if 均为 hide 的过渡动画 的 终值 &#125; else if (!animatingIn &amp;&amp; type == MBProgressHUDAnimationZoomIn) &#123; bezelView.transform = large; &#125; else if (!animatingIn &amp;&amp; type == MBProgressHUDAnimationZoomOut) &#123; bezelView.transform = small; &#125;#pragma clang diagnostic push#pragma clang diagnostic ignored "-Wdeprecated-declarations" bezelView.alpha = animatingIn ? self.opacity : 0.f;#pragma clang diagnostic pop self.backgroundView.alpha = animatingIn ? 1.f : 0.f; &#125;; // 此方法 iOS 7.0 之后才支持#if __IPHONE_OS_VERSION_MAX_ALLOWED &gt;= 70000 || TARGET_OS_TV if (kCFCoreFoundationVersionNumber &gt;= kCFCoreFoundationVersionNumber_iOS_7_0) &#123; [UIView animateWithDuration:0.3 delay:0. usingSpringWithDamping:1.f initialSpringVelocity:0.f options:UIViewAnimationOptionBeginFromCurrentState animations:animations completion:completion]; return; &#125;#endif // iOS 4.0 就开始支持 [UIView animateWithDuration:0.3 delay:0. options:UIViewAnimationOptionBeginFromCurrentState animations:animations completion:completion];&#125; 隐藏的逻辑与此类似，可自行参看代码，这里简单绘制了一张方法调用流程图作为小结，如下所示：]]></content>
      <categories>
        <category>源码学习笔记</category>
      </categories>
      <tags>
        <tag>iOS</tag>
        <tag>源码学习笔记</tag>
        <tag>MBProgressHUD</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello World]]></title>
    <url>%2F2018%2F12%2F15%2Fhello-world%2F</url>
    <content type="text"><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub. Quick StartCreate a new post1$ hexo new "My New Post" More info: Writing Run server1$ hexo server More info: Server Generate static files1$ hexo generate More info: Generating Deploy to remote sites1$ hexo deploy More info: Deployment]]></content>
  </entry>
</search>
